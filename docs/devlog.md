# Development log

## 2026-08-17 BASELINE-0031 retrieval-focus orientation and local freeze

- Resumed from clean public reconciliation commit `4d62ecf930130b938594a9b7bc70675f9d8122e1` in `C:/Projects/Active/runbook-sentinel-baseline-0031`; no OneDrive path is authoritative.
- Inspected the accepted 57-scenario, 171-attempt source result and anchored 261-event trace. Every required evidence ID is retrieved, but 87 of 153 eligible attempts include 123 additional records and mean expected-document share is 0.683006535948. Additional records are treated as untrusted-context burden, not declared irrelevant.
- Rejected development-only relative score rules at 0.50 and 0.75 and a first-score-tie rule because they drop frozen required evidence. Preserved the corrected GitHub tree-query observation and all three rejected candidates in `artifacts/verification/baseline-0031-orientation-probe-failures-001.json`.
- Froze one bounded candidate before implementation: unchanged within-tier lexical ranking, caps of two fresh project-evidence, one stale project-evidence, and one untrusted-guidance record, with no backfill. The independent development reference preserves required IDs and ranks while reducing additional records from 66 to 48 across three trials. Held-out candidate results remain unknown.
- Added the phase-aware contract validator, six focused tests, ADR 0026, milestone 0031, orientation receipt, and living records. The existing recent RAGChecker/RAGAs gate is reused narrowly; no external source or asset was newly accessed or imported. `freshness-priority-lexical-v3` remains the default and `bounded-trust-tier-lexical-v4` is absent until the exact freeze is public and remotely verified.
- The independent validator, six focused tests, governed milestone validator, compilation, and diff checks pass. The complete suite passes 86 tests in 100.239 seconds and all 38 inherited phase-correct side-effect-free validators pass in 4.159 seconds. Structured parsing covers 956 nonempty JSON files plus 28,512 records across 156 JSONL files with no parse error; the one known zero-byte historical evidence item and fourteen BOM-bearing historical JSON files are reported rather than rewritten. Exact changed-content credential and model-artifact scans find zero hits. The local receipt binds the freeze; public publication and remote-tree reconciliation remain before implementation.
- Retained one receipt-wrapper failure where unquoted `HEAD^{tree}` was misparsed by Windows PowerShell. The literal-string successor resolves starting tree `c36fd5b9d98c6b6049ee0e0f7ef99a1bb9fdfaa0`; no repository byte was changed by the failed probe.
- Published preimplementation seal `a7e42589b8e85d6f967128d32d7cea7a5111c3e9`, tree `22d186d9c5a558560abb6fe81c272f389e6571dd`, with parent exact at public reconciliation commit `4d62ecf...`. The first wrapper retained the repository's narrow-fetch upstream observation; an explicit branch fetch then proves local, tracking, `ls-remote`, anonymous GitHub ref and tree APIs, all 16 changed Git entries, and all 16 anonymous raw files exact. The repository is public and `main` remains its default branch. Candidate runtime and held-out results are absent at the seal.
- The first milestone lifecycle edit used `completed`; the installed validator requires exact status `complete` and rejected the edit without opening authority. The corrected successor uses `complete` for UNIT-001 and must pass before this public-reconciliation payload is admitted.
- Public freeze-reconciliation commit `401387f42940f78a037718eb41e1936d3b9f1d2c`, tree `e1ca96828ee311c2a5b9d5a4f498136533dcf12a`, is exact across refs, all nine changed Git entries, and all nine anonymous raw files after an explicit post-push branch fetch. Retained one compact PowerShell operator-token failure and the first stale-tracking comparison; neither changed repository or product bytes.
- Implemented exactly one runtime configuration in `src/runbook_sentinel/retrieval.py`: `bounded-trust-tier-lexical-v4` applies the frozen 2/1/1 tier slices without backfill after the unchanged lexical score and ordering. `freshness-priority-lexical-v3` remains the default; service, agent, decision context, policy, approval, executor, authority, scenarios, terminals, dependencies, credentials, services, models, and external assets are unchanged.
- The implementation validator matches the independent development reference exactly and reports `implemented_experimental`. Six focused tests pass. The complete 86-test suite passes in 107.216 seconds, and all 37 applicable phase-correct non-manifest validators pass in 2.463 seconds. The immutable predecessor manifest fails closed on exactly `src/runbook_sentinel/retrieval.py`, from SHA-256 `cdaa33d...` to `6eb5227b...`; no historical byte is repaired. No held-out candidate run, successor manifest, package, archive, image, or default promotion exists.
- Governed milestone validation, compilation, and diff checks pass. Structured parsing covers 960 nonempty JSON files and 28,512 records across 156 JSONL files with no parse error; the one known zero-byte and fourteen BOM-bearing historical JSON artifacts remain reported. Added-line and untracked-file high-specificity credential scans and the repository model-artifact scan find zero hits. Public implementation seal is the next gate.
- Published implementation seal `b13057ea924e3df002d86264c044b19c1a0f398a`, tree `1c898ae67574ba7a775d4ed158d2d1e9e2bd9fef`. Local, explicitly fetched tracking, `ls-remote`, anonymous GitHub ref and tree APIs, all 11 changed Git entries, and all 11 anonymous raw files are exact. Retained one first compact comparison with no admissible output. No held-out candidate evaluation exists at the seal.
- Froze exact control, candidate, receipt, trace, and comparison paths plus control-first publication, same-manifest, trial-ID, latency, cost, and default-change boundaries before results. Added an independent phase-aware result verifier and five fail-closed tests for required-evidence loss, latency regression, and premature default movement. No control, candidate, or comparison artifact exists.
- The manifest inventory delta is exactly three paths—`eval/retrieval-tier-cap-contract.json`, `scripts/verify_retrieval_tier_cap_contract.py`, and `scripts/verify_retrieval_tier_cap_result.py`—for 146 unique existing files, zero removals, and zero missing entries. The complete suite passes 91 tests in 106.341 seconds and all 38 applicable non-manifest validators pass in 2.501 seconds. Publish and remotely reconcile this inventory before writing `eval/manifest.json`.
- Published inventory seal `d96a1f03b010c00c44e36e5332e97ae37aee3e39`, tree `e3224af3cf8085ae8d6f319b63b0c2f1584134fc`. Local, explicitly fetched tracking, `ls-remote`, anonymous GitHub ref and tree APIs, all 11 changed Git entries, and all 11 anonymous raw files are exact. `eval/manifest.json` remains the 143-file BASELINE-0030 predecessor at that seal and every comparison artifact remains absent.
- Froze 146-file BASELINE-0031 manifest attempt 001 at 17,558 bytes and SHA-256 `99489ecb...`. Its internal verifier passed, but the 91-test suite failed three tests in 75.676 seconds: one literal still expected checkpoint BASELINE-0030, and two historical latest-report validators lacked a byte-exact companion after current manifest advancement. The exact failed manifest and failure receipt are retained; it is not positive evidence.
- The bounded correction adds the exact 17,202-byte BASELINE-0030 final manifest companion at SHA-256 `bc98beca...`, derives the current checkpoint assertion from `eval/manifest.json`, and adds the failed manifest itself to custody. The three focused regressions pass in 58.970 seconds. Corrected inventory is 148 unique existing paths, exactly two additions over the public 146-path inventory, zero removals, and no runtime, scenario, expected result, grader, selection rule, or held-out-result change. `eval/manifest.json` is restored byte-exact to the public 143-file predecessor before publishing the amendment.
- Against that restored pre-freeze state, all 91 tests pass in 106.638 seconds and all 38 applicable non-manifest validators pass in 2.498 seconds; the predecessor manifest rejects exactly the implemented retrieval path. The 148-path amendment is ready for publication, but no successor manifest may be written first.
- Public amendment `fa740b95442232cdcef72b8f5fd7dcbdb5b29bb1`, tree `c917dca34b5d1bd391943b0683f42047f3d5f8e3`, matches across local head, explicit tracking, `ls-remote`, and GitHub ref. Eleven of twelve changed anonymous raw paths are exact; `eval/manifest-0031-attempt-001-invalid.json` is not. Git converted its 153 CRLF sequences, changing 17,558 bytes at SHA-256 `99489ecb...` to 17,405 public bytes at SHA-256 `406676b1...`. The publication is retained as a failed amendment and does not pass `comparison_inventory_public`.
- Preserved the original failed-manifest bytes at `artifacts/verification/baseline-0031-manifest-attempt-001-invalid.json`, which is covered by the existing binary verification rule. The normalized eval copy remains in history and inventory as a semantic copy. Corrected inventory becomes 149 unique existing paths with zero removals; comparison and successor-manifest execution remain closed pending exact public reconciliation.
- The corrected inventory passes 91 of 91 tests in 106.266 seconds, including all three attempt-001 regressions, and 38 of 38 applicable phase-correct non-manifest validators in 2.651 seconds. Retained two no-mutation validator-wrapper stops: the first omitted the adversarial retrieval contract's implementation flag; the second omitted retrieval observability's explicit implementation-only mode. Exact successors pass. Governed milestone, compilation, diff, 149-path uniqueness/existence, v3 default, predecessor-manifest, and no-comparison-artifact checks also pass.
- Published corrected inventory commit `9e031aa3ddfc89a26b8d78452daa583201361fbd`, tree `70a102b6554ee35831441ca426fcc82907b73e3e`. Local head, explicitly fetched tracking ref, `ls-remote`, GitHub ref and tree APIs, all 11 changed tree entries, and all 11 unauthenticated raw files are exact. The binary custody copy is 17,558 bytes at SHA-256 `99489ecb...` publicly and locally. The successor manifest and all seven comparison paths remain absent; only the manifest-freeze gate opens.
- Froze BASELINE-0031 manifest attempt 002 at `2026-08-18T01:23:34Z`: 149 files, 17,981 bytes, SHA-256 `9cdb30aa49613fc9ca85be915d8efa91a5c98433d2bff57bf1d6e423a9c6c08c`. Internal and precomparison verifiers pass. The complete suite passes 91/91 tests in 107.021 seconds, and all 39 applicable phase-correct validators—including the manifest verifier—pass in 2.647 seconds. Attempt 001, the failed public amendment, and exact predecessor manifest remain preserved. V3 is default and no control, candidate, or comparison result exists; publish and reconcile this manifest before control execution.
- Published manifest seal `596a81dec106e508486a9c739a1c43fad053250a`, tree `2052251cf76f3ebd2fc8ee357f67707b585eedb4`. Local head, explicitly fetched tracking, `ls-remote`, GitHub ref and tree APIs, all eight changed tree entries, and all eight unauthenticated raw files are exact. Public `eval/manifest.json` is exactly 17,981 bytes at SHA-256 `9cdb30aa...`. The control-first gate now authorizes only the v3 control; v4 and the comparison remain absent and closed.

## 2026-08-17 BASELINE-0030 public release reconciliation

- Published audited closure `9bfa784feeb1e002c546326ce9db214c2c155fce`, annotated tag object `1e51f18e6023bd66b19ad11ca794c5a0c2c42885`, and public non-draft latest v0.0.30 release `372002788`. The tag peels exactly to the closure and GitHub reports it unsigned; no signature claim is made.
- Uploaded only the selected 646,081-byte zipapp at SHA-256 `b950672f64306d03d9806615cf50dedfad89d83c705b61f6dabe2224e35d9cbf` and 94-byte checksum at SHA-256 `5a27247f2757b44ad8c6d6d46efe8bf4ff984e62f76d085fb36696758e6f92b8`. Downloaded bytes and two fresh tag builds are three-way exact, the 43-entry package contract passes, and rendered repository/release/tag pages show the intended closure, assets, bounded product promise, and no-image-publication limitation.
- A fresh no-alternates HTTPS tag clone passes 80 tests in 114.633 seconds and all 37 phase-correct validators in 5.266 seconds. Fresh source/downloaded-package runs each pass 57 scenarios, 171 attempts, all 136 Boolean gates, and anchored 261-event traces. Replacing only the 17 declared launch-varying leaves produces exact 576,333-byte normalized reports at SHA-256 `06b1032d...`.
- Both three-tool MCP paths expose no approval or execution authority. Both validated Windows PowerShell API paths pass 131/131 checks, both native inspections pass 72/72, and source/package/container dashboards render completely at 1440 by 1000 with all 35 metrics, two persisted incidents, authenticated external operator, and real infrastructure disconnected.
- Two full tag-source builds and one independent no-alternates clean-tag build reproduce image `sha256:793cc009835350972ac30215cbdad2fb73f3802d47982d3bfddbd49af633fea3`. All 46 composed `container-runtime-v10` checks pass; the structured Docker Scout result has zero critical/high findings at observation time. No image was exported, pushed, published, or redistributed, and no verifier container survives.
- Retained the truncated evaluation-console response, first empty-pipe trace/hash wrapper, browser screenshot and page-wait timeouts after content load, corrected unsigned-tag wrapper, expected annotated-tag clone warnings, first-frame package/container dashboard viewer artifacts, unsupported `isLatest` field, transient release-view network timeout, and intentional 45-plus-1 container receipts. Exact successors pass; no unfavorable byte was rewritten.
- The reconciliation publication gate passes the governed complete-milestone validator without warning, all 37 phase-correct project validators in 2.741 seconds, and all 80 tests in 103.605 seconds. JSON parsing and Git diff checks pass.
- The 18,433-byte `artifacts/verification/public-tag-baseline-0030.json` at SHA-256 `bbef21fe6b56e60f579dd83e0a117d579330793eb3e580a9c3d707f3383ece7d` binds the result. BASELINE-0030 and UNIT-005 are complete; the next cycle begins only by running verified public v0.0.30 and selecting one measurable weakness from fresh traces and separated metrics.

## 2026-08-17 BASELINE-0030 final release audit

- Reconciled the verified merged-main candidate in exactly seven living-record paths at `2b61511941564dbccadf0433fdf4f654b18223ec`; all seven public paths match anonymous byte lengths and SHA-256 values. The complete 80-test pass, 37 project validators, milestone, parsing, diff, real-surface, and three-build container evidence remain valid because the reconciliation changes only lifecycle records.
- Published the sole-path 24,823-byte final audit at SHA-256 `aea637d35d0b1cca8eb79f5910929fea6479501e802335c861b520efcb0aa7a2` as commit `e5cc9f2cb75c6fc8cc20400eb0b286cbe44e0e05`; anonymous public bytes are exact. The release-truth evaluator reports `verified` with ten of ten checks, all 20 required real surfaces, active inherited authority, and no error, warning, stale, missing, unknown, or unrun surface.
- Public v0.0.29 remains latest; v0.0.30 tag and release remain absent. Exact closure, annotated tag, selected zipapp/checksum, downloaded-byte and rendered-page verification, and fresh public-tag source/package/local-container reproduction are next. The PowerShell 7 compatibility observation remains retained and the validated Windows PowerShell API path remains the admitted gate.

## 2026-08-15 BASELINE-0030 model-output conformance orientation and local freeze

- Resumed from reconciled public `main` commit `cc07fa2a0790a49b04b4352a004deb913bb12a60` in clean `C:/Projects/Active/runbook-sentinel-baseline-0030`; no OneDrive path is authoritative.
- Inspected the accepted 57-scenario deterministic run and 261-event trace, then selected the retained local-model interface mismatch: 67 of 84 attempts fail `diagnosis_code_invalid` because the schema omits the parser's lowercase bounded-identifier pattern.
- Verified live local compute without downloading or updating anything: Ollama CLI and loopback API both report 0.32.6; installed `llama3.2:3b` remains at manifest `a80c4f17acd5...`. `bge-m3` is installed but excluded from this checkpoint.
- Reviewed official JSON Schema Draft 2020-12 and exact-tag Ollama v0.32.6 documentation under the external-source gate. All sixteen criteria pass after retaining an initial exact-action-label validation failure. No external source, package, model, prompt, data, service, or documentation bytes were imported.
- Preserved v2 byte-exact, froze a one-field v3 projection, added an independent validator, five focused tests, ADR 0025, milestone 0030, threat/evaluation/research records, and six process-invalid attempts. The validator, source gate, governed milestone, all 76 tests in 87.931 seconds, all 36 side-effect-free project validators, compilation, 889 nonempty JSON files, 149 JSONL files with 26,865 records, diff check, and exact changed-content credential/model scan pass. The known zero-byte and fourteen BOM-bearing historical evidence files are reported, not rewritten. The local receipt binds every changed path and candidate absence; public freeze is next.
- `eval/model-contract.json` is still v2. No baseline-0030 model call, candidate report, package, archive, or image exists.
- Published preimplementation commit `f82e301d984548d5354d8093e245c17fe0419a1f`. Local, tracking, `ls-remote`, and GitHub ref API identities match; the remote tree is `b7c6cac16073901a3e3ce1753fac85c1b17d5ef1`, and all 18 changed Git blob identities and sizes match local `ls-tree` exactly.
- Retained one inconclusive all-path raw-byte wrapper that exceeded its yield without returning a usable session handle and one Git Trees API wrapper that received 404 because PowerShell interpreted `$treeSha?recursive` as one variable. The explicitly delimited successor passes 18 of 18. Publish the reconciliation receipt before the four-path implementation; model calls remain closed.
- Before changing `eval/model-contract.json`, identified that the focused test always invoked preimplementation validation and would necessarily reject implementation presence. Froze a phase-aware test-only correction that derives mode from exact v2 equality and does not change the validator, target, runtime, security, or authority. The correction must be public before implementation.
- The phase-aware correction passes five focused tests, the 76-test suite in 84.407 seconds, all 36 project validators, preimplementation validation, and diff checks. `eval/model-contract.json` remains exact v2; publish and verify this correction seal next.
- Published and remotely verified the phase-aware correction at `0a49729f28fa28f06fcbae0878e1d3558ea47abc`, tree `7d7386fc6b0f95966ed9a08b9f04dbd07cdbc429`, before changing the active model contract. All seven correction tree entries are exact.
- Applied only the frozen schema version, checkpoint, contract ID, and `diagnosis_code.pattern` projection. The implementation validator reports target object SHA-256 `4cd479f...` exactly, and five focused tests pass. The serialized active contract is 4,712 bytes at SHA-256 `8718971a...`.
- All 76 tests pass in 87.015 seconds. All 35 applicable non-manifest project validators pass. The immutable predecessor manifest reports only `eval/model-contract.json` stale from `b5ed65a...` to `8718971...`; this is expected fail-closed evidence and is retained until UNIT-003 is permitted to freeze a successor manifest.
- Retained a PowerShell revision-expression wrapper failure and the expected predecessor-manifest mismatch. No model call, candidate report, successor manifest, archive, image, external asset, secret, paid service, or real-infrastructure interaction exists. Publish and remotely reconcile the implementation seal before opening UNIT-003.
- Published implementation seal `34bc1a8b8f7d4070f0858581f01852e0643685d7`, tree `bd5b47df6068f8f4e9f588627238eaba2aa5f1d7`. Local, explicit remote-tracking, `ls-remote`, and GitHub ref API identities match; all 12 changed GitHub tree entries match local blob identity and size. The repository remains public.
- Retained two upstream-wrapper failures caused by the source clone's selected-branch fetch configuration. Explicitly reading the fetched remote-tracking ref proves the intended identity without changing product bytes. The public receipt preserves zero model calls and successor artifacts at the seal. UNIT-003 may now run the two frozen 171-call configurations.
- Preflighted the actual evaluation CLI before model execution. It hashes `eval/manifest.json` into every report, but the current 135-file manifest remains BASELINE-0029 and does not cover the preserved v2 archive, frozen conformance contract, or independent verifier. Added only those three paths to the project-authored inventory; exact successor count is 138. Retained a PowerShell scalar-count display defect, a guessed nonexistent predecessor public-receipt path, and an initial inherited-count assertion that expected 136 before the exact set comparison corrected it. No model call or successor manifest exists; inventory and manifest publication gates come first.
- Public inventory seal `c6eab065b17420d893d69781a8e7d20051ae9fa6` is exact across four refs and seven remote tree entries. Froze a 16,597-byte, 138-file BASELINE-0030 manifest at SHA-256 `882c552e...`; exact manifest verification passes. The first full regression retained one stale test assertion expecting predecessor label `baseline-0029`; only that current-checkpoint assertion advances to `baseline-0030`. No manifest-bound runtime file or model boundary changes.
- Published the deterministic same-manifest control at `5ad5dbec3d5181aab19dc044bd31939b40d66816`. Constructed an independent three-configuration comparison that binds the immutable v2, v3, and control reports/traces and directly computes the frozen development-improvement plus held-out policy/attack non-regression rule.
- The first comparison validation failed closed because its derived v3 error map included `diagnosis_code_invalid: 0`, while the immutable report projects only observed nonzero codes. Removed only that synthetic derived entry and retained the failure receipt. The corrected validator and four focused tests pass.
- The complete post-selection suite passes 80 tests in 83.820 seconds. The first 37-validator wrapper omitted `--require-implementation` for one phase-aware historical retrieval contract and retained its otherwise exact failure; the corrected inventory passes 37 of 37.
- V3 is selected only as `selected-experimental-remediate`: development parsing improves without held-out policy or attack regression, but diagnosis accuracy, utility, and pass-three do not improve and completion tokens rise by 584. The deterministic control remains the product default. UNIT-003 is locally complete; publish and remotely reconcile the selection seal before UNIT-004 freezes package or container work.
- Published selection seal `a77904b7e565bf9548a27e0e0e9d80c523499cb4`, tree `cb0bfd9d...`. Local, explicit remote-tracking, `ls-remote`, and GitHub ref API identities match, and all 17 changed remote tree entries match local blob identity and size. UNIT-003 is complete; UNIT-004 opens with archive and image absence preserved until its contract is frozen and public.
- Froze the 43-entry dependency-free v0.0.30 zipapp contract and `container-runtime-v10`. V10 preserves the exact v9 contract/receipt boundary and adds only `container_model_contract_selection_exact`; it requires 46 checks and still forbids image export, push, publication, or redistribution. Current/versioned contract pairs match exactly.
- Preserved the raw 16,597-byte precomparison manifest identity used by all three model reports and an LF-normalized semantic copy at `eval/manifest-0030-precomparison.json`; the v10 contract records both hashes instead of calling the representations byte-identical. Froze the authoritative 143-file final manifest at SHA-256 `bc98beca...`.
- Ran frozen deterministic source attempt 001: 57 scenarios, 171 attempts, all 136 Boolean gates, and the anchored 261-event trace pass. `latest.json` is byte-identical to the immutable report. The first source API attempt failed exactly one of 131 checks because `latest.json` still named baseline-0029; the failure is retained. After the passing baseline-0030 evaluation was promoted, the unchanged API passed 131/131.
- Reverified the bounded MCP at exactly three diagnostic/read tools with no approval/execution tool, and independently inspected the real persisted database, audit trail, hashed approval custody, logs, and anchored telemetry at 72/72 checks. Original-detail review accepted the complete 1440 by 1000 dashboard, including the external-operator execution boundary and disconnected-real-infrastructure statement.
- Retained eight package-freeze process failures covering an incorrect read-only path guess, trailing-blank-line copy defect, explicit CRLF/LF distinction, two focused-test fixture defects, a quoting-only search failure, a read-only hash-wrapper parser failure, a credential-scan option-separator failure, and an upstream-ref wrapper that stopped before staging. Corrected successors pass without changing immutable comparison evidence or security boundaries.
- The local pre-build seal passes with 80 tests, 37 phase-correct validators, exact contracts, exact manifest, source evaluation/trace, real surfaces, and Docker availability. No v0.0.30 archive, checksum, container receipt, or image exists. Publish and remotely reconcile these exact bytes before any build.
- Published pre-build commit `dda0c347c108a78ac0afe279f09bc074c73579e9`, tree `d5b70d325cc221f10beb4542273856ce7459caeb`. Live `ls-remote` and GitHub ref API immediately matched, while the separately stored local tracking ref remained at the parent; the first wrapper stopped and is retained.
- Fetched only the exact public branch into its explicit local tracking ref, then reran reconciliation. Local, tracking, `ls-remote`, and GitHub ref API all match; the repository remains public and all 42 changed remote tree entries match local Git blob identity and size. The archive gate opens from this exact checkpoint. Container construction remains closed until the package/source payload passes and is public.
- Built two independent v0.0.30 zipapps in `C:/Projects/Verification/runbook-sentinel-baseline-0030-package`; both and the canonical archive reproduce exactly at 646,081 bytes, 43 entries, and SHA-256 `b950672f...`. The package verifier passes and runtime dependencies remain empty.
- Retained an invalid PowerShell `Test-Path` parallel preflight that stopped both guards before evaluator launch. Corrected guards produced immutable final source/package attempt 001. Both pass 57 scenarios, 171 attempts, all 136 Boolean gates, and anchored 261-event traces; `latest.json` is restored byte-exact to the source report.
- Compared the entire reports recursively. All 407 raw leaf differences use exactly 17 frozen runtime-varying keys; replacing only those leaves produces identical 587,746-byte normalized reports at SHA-256 `51b921ce...`. No behavioral, split, gate, retrieval, generation, trajectory, policy, utility, attack, reliability, or cost difference remains.
- Repeated source and packaged MCP, authenticated API, approval/executor, persistence, telemetry, and dashboard surfaces. Each MCP exposes three diagnostic/read tools with no authority; each API passes 131/131; each native inspection passes 72/72. Both complete 1440 by 1000 dashboards pass original-detail review. Edge emitted its known fallback task-provider diagnostic after successfully writing each PNG; both commands exited zero and the observation is not treated as a failure or hidden.
- The post-surface regression passes 80 tests in 137.620 seconds. Retained a first validator wrapper that omitted `PYTHONPATH=src` and stopped at import after 22 passes; the corrected full phase-aware inventory passes 37/37 in 12.395 seconds.
- Retained a read-only parser wrapper that used unavailable `System.Text.Json` and invalidly marked every document. Its corrected PowerShell successor parses 935 nonempty JSON documents and 157 JSONL files containing 28,258 records with zero content errors, while retaining the one historical zero-byte JSON item and 14 BOM-bearing files. Package/trace/manifest/milestone/compile/diff, PNG, credential, and model scans pass. No container image exists.
- Retained a broad changed-file credential scan that matched one unchanged historical sentence documenting the private-key test literal. Its first corrected successor later encountered the artifact-scan receipt quoting that same classification. The final scope examines added tracked lines plus complete untracked files, classifies only those two evidence receipts as non-material, and has zero unresolved credential findings.
- Wrote `package-source-baseline-0030.json` and the package-phase scan receipt. Publish and remotely reconcile this exact payload before any container-runtime-v10 build.
- A final documentation-complete regression also passes 80/80 but takes 1,178.756 seconds rather than the earlier 137.620-second pass. The result is retained as an unusually slow verification-orchestration observation, not product latency evidence; no test failed and no gate was weakened.
- The first staging wrapper was blocked before process creation because its binary index comparison used shell redirection. The index remained empty. Its successor stages first and compares worktree/index Git blob identities without materializing binary output.
- Committed the exact 27-path package/source payload as `35a0f18f8e9cf54a56cec0eded620397a98f992f`, tree `f02c43956fd2852d8ad5c1f3804b4e2838af777a`. The first combined status-and-push wrapper then misparsed unquoted `HEAD^{tree}` and stopped before push; live `ls-remote` confirmed the public branch remained at `4e1d8384...`. Retain the failure in a successor commit, then push with separately quoted identity commands.
- Committed the retained pre-push failure as successor `27e9bb1a8467d2cc601ba5d10ba8e629b896e3fe`, tree `d9f3415568850314caeeaa89656b98197c734dd3`, and pushed both commits. The first public check found live `ls-remote`, GitHub ref API, public visibility, and all 28 entries exact but the separately stored local tracking ref stale; retain it, fetch only the exact branch, and rerun.
- After the explicit fetch, all four ref views and all 28 changed Git tree entries match `27e9bb1...`. The public package/source gate passes. Publish and verify this receipt and living-record update before running container-runtime-v10.
- Published the package/source reconciliation receipt and living records at `dc5e72594b7919dbd75bf4d66f35615551ace900`, tree `72084efc4836bb6ac27b35fb5e054cffd6795fae`. Retained a first read-only wrapper with invalid `foreach` spacing; the corrected successor proves four-ref identity, public visibility, a clean worktree, and all nine changed remote paths exact.
- Ran the frozen full container workflow from that exact public input. Two local builds reproduce image ID `sha256:793cc009835350972ac30215cbdad2fb73f3802d47982d3bfddbd49af633fea3`. The 46-check canonical receipt passes evaluation, trace, retrieval/model-selection projections, three-tool authority-free MCP, authenticated API, approval/executor, state, telemetry, hardening, dashboard, and structured critical/high scan gates.
- Cloned the public branch over HTTPS with `--no-local` into `C:/Projects/Verification/runbook-sentinel-baseline-0030-public-clean`. It starts clean at `dc5e725...`, has no object alternates, contains the exact selected archive, and independently rebuilds the same image ID. No image was exported, pushed, published, or redistributed.
- Preserved the generated full component receipt byte-exact even though its non-authoritative next-gate text says 43 checks. The frozen v10 contract declares 46 and both the canonical composition and independent validator pass 46/46; no component or verifier was weakened.
- Inspected the 1440 by 1000 container dashboard at original detail. It is complete and legible with all metrics, both persisted incidents, authenticated external operator, and real infrastructure disconnected. The PNG is 102,140 bytes at SHA-256 `9ef9bf6f...`; parsed Docker Scout SARIF has zero critical/high results.
- Recovered the lost post-container test process by rerunning once; 80/80 tests pass in 141.559 seconds. The first 37-validator wrapper attached the implementation flag to a shortened nonexistent filename, so the actual retrieval-stage contract correctly failed closed; the exact corrected inventory passes 37/37 in 10.036 seconds.
- A `--help` orientation call to `scripts/verify_baseline.py` discovered that the script has no argument parser only after it completed a second 80/80 regression and began a duplicate evaluation. Stopped it before report, anchor, or promotion. Its exact manifest and complete 261-event trace remain retained as an invalid incomplete attempt; `latest.json` stayed byte-identical to the accepted source report.
- Archive, source/package/container trace, final-manifest, governed-milestone, compilation, and diff gates pass. The first live-image tail guessed the clean tag and the first credential scan omitted ripgrep `-e`; their corrected successors verify all three live tags and zero high-specificity credential, raw approval-token, model-artifact, or SARIF findings.
- Full parsing covers 947 JSON files with 946 valid nonempty documents plus the one known historical zero-byte item, and 158 JSONL files with 28,519 valid records. UNIT-004 remains in progress until this exact container evidence and living-record update are committed, pushed, and independently verified across every changed public Git object.
- Committed the exact 17-path container payload as `77efe4eb85dd213b087cf6ad4e8de4aa502c0c13`, tree `9cc0c34cd8f8251c5d81ecbfeb6068d9df4033bb`, and pushed from a clean worktree after verifying all staged blob identities. Explicitly fetched the branch; local, tracking, `ls-remote`, GitHub ref API, and public visibility agree, and all 17 GitHub tree entries match local Git blob identity and size.
- The first anonymous raw-byte wrapper stopped before network access because Windows PowerShell had not loaded `System.Net.Http`. Retained the process-invalid attempt. Its unchanged successor explicitly loads the standard assembly and verifies all 17 immutable-commit public files without credentials by length and SHA-256.
- UNIT-004 is complete with disposition `pass`. The public reconciliation receipt binds the canonical/component receipts, dashboard, SARIF, artifact scan, classified incomplete attempt, exact local image, clean clone, and no-export/no-push/no-publication boundary. UNIT-005 begins only with the governed premerge release-truth audit.
- Verified that reconciliation head `fcc76ad299b6996ec63db4014a388e69d73d65f3` is public and exact on draft PR `#28`, with 113 local and paginated GitHub paths equal, no configured checks or reviews, and no v0.0.30 tag or release. Retained five read-only orientation-wrapper failures. Reconciliation also found stale controlling text that still requested publication of the already-public UNIT-004 receipt; this bounded evidence-only correction advances the next action to the exact-head premerge audit without changing product or release bytes.
- The corrected full suite passes 76 tests in 90.830 seconds and all 36 side-effect-free project validators pass, including exact successor-manifest verification. Compilation and diff checks pass. The manifest seal must now be public and remotely exact before either model run.
- Published manifest seal `1066b7fdef5bb0f2bb442db1a4122378c3ebab0a`, tree `ecc32b3af50502bf0f6e1d11e7e85d87cb239858`. Four ref views and all nine changed GitHub tree entries match. No baseline-0030 model call or comparison report existed at publication. The legacy-v2 run may now begin, followed by v3 under the same conditions.
- Live preflight reconfirmed Ollama CLI/API 0.32.6 and `llama3.2:3b` digest `a80c4f17...`; retained one PowerShell foreach parse failure that occurred before runtime invocation. Legacy v2 then completed one uninterrupted 171-call attempt. Thirty outputs parse, 141 are classified rejections, no proposal or execution occurs, and utility/pass-three remain zero. Policy is 1.0 and attack success zero only through fail-closed enforcement. Exact report/trace verification passes; publish this unfavorable result before v3.
- Published v2 at `199f46b4aac63bf944c56e768d64780703b8085f`; four refs and all nine changed remote entries match. A clean preflight then launched v3 against the same manifest and runtime. V3 completed 171 uninterrupted calls with 113 valid outputs and 58 classified rejections. The target diagnosis-code rejection count drops from 126 to zero, but diagnosis accuracy stays 3/171 and utility, pass-three, proposals, and executions stay zero. Exact v3 report/trace verification passes. Publish v3 before selection.
- Published v3 at `d65e04d172f1c668610a81fab2156162b9d55b11`; four refs and all nine changed remote entries match. Ran the third frozen configuration, the deterministic control, on the same manifest. A malformed non-terminating PowerShell output-absence preflight is retained; run_evaluation's own immutable check succeeded and the run completed. All 136 Boolean gates pass, utility/pass-three are 1.0, attack success is zero, model calls are zero, median latency is 45.606 ms, and the 261-event trace verifies.

## 2026-08-17 BASELINE-0030 reviewed merge and public-main verification

- Published the premerge control correction and audit-only receipt. After GitHub mergeability settled from transient `UNKNOWN` to `CLEAN`, PR `#28` became ready at exact head `cef8b3f...` and merged only under its expected-head lock as `053ff98...`; reviewed and merged trees are identical.
- A fresh HTTPS public-main clone with no object alternates passes 80 tests, all 37 phase-correct validators, two exact archive rebuilds, fresh source/package evaluations and anchored traces, bounded MCP, both real API/state/telemetry paths, and three complete dashboards. The verifier's tracked generated dashboard bytes were restored only in the disposable clone after exact external copies were bound.
- Retained two missing-import-path wrappers, one unavailable byte-array instance comparison, one viewer batch truncation, and one read-only historical-receipt parser failure. Corrected successors pass without changing protected evidence.
- Retained a PowerShell 7 compatibility observation: 129/131 live API checks pass, with only two null `WWW-Authenticate` values. The unchanged validated Windows PowerShell path passes 131/131 and observes both exact server challenges; all other API, approval, executor, state, telemetry, and dashboard checks agree.
- Two no-cache full builds and one independent no-alternates clean build reproduce image ID `sha256:793cc009...`; all 46 composed checks and the point-in-time zero-critical/high Docker Scout scan pass. No image was exported, pushed, published, or redistributed.
- Published the exact merged-main receipt plus retained-observation record as `211d7bdd2bc885f8568210fca7b60ff5c9aeb8da`. Local, tracking, `ls-remote`, GitHub ref API, and both anonymous immutable-commit downloads agree. Public v0.0.29 remains latest and v0.0.30 remains absent; final release audit is next.

## 2026-08-15 BASELINE-0029 public release reconciliation

- Published release closure `d90dbf36cad28e9738d5ca9c38f42967e557376d`, annotated tag object `2f54a5cc180f9a79962c5ac56a416444a4be5a58`, and public non-draft latest `v0.0.29`. The release uploads only the selected 645,064-byte zipapp at SHA-256 `d1417c22711a09b24c7a7bdf0fa36b18ac446320087eab4493f1cbd63725090b` and its 94-byte checksum; generated source archives remain GitHub-rendered, not project-uploaded assets.
- Controlled anonymous intake used a frozen no-replace contract and verified both promoted files, exact GitHub digests, checksum content, 43-entry ZIP integrity, and supported CLI help. Rendered repository, release, and tag pages visibly bind public visibility, latest status, version, closure, selected assets, research-informed synthetic-only scope, and the no-image-publication limitation.
- A fresh HTTPS public-tag clone with no object alternates passes 71 tests and all 35 project validators. Two independent rebuilds match the downloaded zipapp exactly. Fresh source/downloaded-package evaluations each pass 57 scenarios, 171 attempts, all 136 Boolean gates, exact retrieval-quality results, and anchored 261-event traces. Both three-tool authority-free MCP surfaces, both 131-check APIs, both 72-check native inspections, parsing, bounded secret/model scans, and complete original-detail dashboards pass.
- Two full tag-source image builds and one independent clean tag-source build reproduce exact local image ID `sha256:b2062da4db7e6d31fea9ddc5eb7794ff2458d243c6e2e951264cf73f841d7be6`. All 45 composed container checks pass; Docker Scout's structured result has zero critical/high findings at observation time. No image was exported, pushed, published, or redistributed.
- Retained intake-schema and cleanup-policy stops, unsupported CLI probes, a browser assertion parse error, validator/help invocation errors, asynchronous archive invocation observation, and the intentionally split 44-plus-1 container receipts. Exact corrected successors pass. The 18,936-byte `artifacts/verification/public-tag-baseline-0029.json` at SHA-256 `df84d6a4af7dc2cbac3f55eb659651d98ce54da82affcc02412e370a8fd58a9f` binds the complete result. BASELINE-0029 is complete; the next cycle must begin by running verified public v0.0.29 and selecting one measurable weakness from fresh traces and separated metrics.

## 2026-08-15 BASELINE-0029 final release audit

- Reconciled the verified merged-main candidate in exactly eight living/failure paths at `347bac688ceea78df1704b977155c3c22fd9200d`; all eight public paths match anonymous byte lengths and SHA-256 values. The complete 71-test pass, 35 project validators, milestone, JSON, diff, and real-surface evidence remain valid because the reconciliation changes only lifecycle and evidence records.
- Published the sole-path 22,495-byte final audit at SHA-256 `efc068d0e59e3d18311c07149583861cfc9b79a4a4fa9e391286d7c76e1cc58c` as commit `6186c3d95d5fcfa4c135c315805037ddee6e3fa2`; anonymous public bytes are exact. The release-truth evaluator reports `verified` with ten of ten checks, all eighteen required real surfaces, active inherited authority, and no error, warning, stale, missing, unknown, or unrun surface.
- A strict PowerShell wrapper treated GitHub CLI's expected no-checks stderr as an error; structured `statusCheckRollup` and `reviews` fields are both exact empty arrays. A redundant surface-matrix helper rejected the audit document without a separate profile after the applicable audit evaluator had already verified the embedded matrix. Both are retained as process-invalid. Public v0.0.28 remains latest; v0.0.29 tag and release remain absent. Exact closure, annotated tag, selected zipapp/checksum, downloaded-byte/page verification, and fresh public-tag reproduction are next.

## 2026-08-15 BASELINE-0029 retrieval-quality orientation and local freeze

- Resumed from public v0.0.28 at remote-truth reconciliation commit `90900b60661f77377cb568c3750bb69f5eb0f857`. The fresh public-tag source report and anchored trace pass 57 scenarios, 171 attempts, all 126 Boolean gates, and 261 exact events. The pre-existing `C:/Projects/Active/runbook-sentinel` worktree is dirty and stale on the BASELINE-0027 branch, so it remains untouched; a clean active worktree was created at `C:/Projects/Active/runbook-sentinel-baseline-0029`.
- Metrics and traces reveal the leading measurement gap. Existing retrieval recall is 1.0, but 87 of 153 expected-evidence attempts carry extra records and mean expected-document share is 0.683006535948. Exact declared attack documents occupy multiple rank buckets, including rank 1, while aggregate conditional policy and attack-success results remain exact.
- The first PowerShell attack-rank probe counted wrapped missing JSON properties as one null item and was excluded before authoring. A later split wrapper used invalid `-gt0` syntax and stopped. Both validation-only failures are preserved in `artifacts/verification/baseline-0029-orientation-probe-failures-001.json`; the corrected probes filter exact non-empty string IDs and use explicit comparisons.
- RAGChecker arXiv v2 and the peer-reviewed EACL 2024 RAGAs paper pass an eight-criterion-per-source gate for citation and narrow diagnostic principles. No source bytes, code, dataset, model, prompt, metric package, service, secret, or real infrastructure are imported.
- ADR 0024 and `retrieval-quality-observability-v1` freeze a measurement-only improvement. The recursive 57-case and terminal identity chain remains exact. The independent validator recomputes every frozen measurement from the retained v0.0.28 report, and seven focused tests reject missing, duplicate, unknown, over-top-k, non-bijective, and mixed-trial retrieval evidence.
- A guessed repository-local milestone validator did not exist; the correct governed-skill validator then rejected `pending` as a future-unit status. Both attempts changed no product state, are retained, and the corrected `planned` statuses validate with no error or warning. No evaluator or runtime implementation exists. Exact freeze publication and remote verification are next.
- All 68 tests pass in 88.759 seconds. A first validator-inventory wrapper used an unavailable Windows PowerShell process argument API; its successor passed 33 of 35 but omitted source import context for two validators. The corrected exact-source wrapper passes all 35 side-effect-free validators. These wrapper failures remain process-invalid and retained.
- Published the exact preimplementation freeze at `2ce5b814db09e9b4f66b47d7edc78a349ed9987a`. `ls-remote`, GitHub API, public visibility, and four critical raw files were immediately exact. Git's first `@{u}` shorthand failed because the source clone's fetch refspec covered only `main`; adding the exact branch refspec repaired local tracking and reconfirmed all heads without changing public state. UNIT-001 is complete and UNIT-002 opens with candidate reveal still forbidden.
- Implemented `retrieval-quality-observability-v1` as an additive report family with an independent algorithmic validator. Runtime and independent results are exact on the retained public-v0.0.28 evidence. The API projects the full report and the dashboard visibly renders expected-document share, extra-document attempt rate, guidance/in-band rank buckets, and rank-conditioned policy compliance.
- Retained five implementation-process failures: a wrong unittest class selector, an unsupported PowerShell timestamp flag, a 68-of-70 schema-compatibility run, the expected predecessor-manifest mismatch, and a read-only PowerShell wrapper parse error. The corrected full run passes 70 tests in 87.064 seconds; 34 substantive side-effect-free validators and compilation pass. The old manifest is intentionally not rewritten before the implementation is publicly sealed.
- Advanced only checkpoint/package identity and additive catalog/report schemas. All 57 scenarios and terminal states remain exact; agent, retriever, evidence boundary, policy, service, storage, and operator-authentication sources remain byte-identical to public preimplementation seal `0118cd1`. No new manifest, candidate evaluation, package, image, external asset, secret, paid service, or real infrastructure interaction exists.
- Published UNIT-002 at `80a8c945516ec926f7583402efe575b94f7fac4f`. Local, tracking, `ls-remote`, and GitHub API heads agree. An invalid first raw-byte wrapper used unavailable `Convert.ToHexString`, produced null hashes, and is excluded; the corrected SHA-256 comparison proves seven critical public files exact. UNIT-003 opens with manifest publication still required before candidate reveal.
- Froze the 133-file baseline-0029 manifest at 16,019 bytes and SHA-256 `c970ce963aa5d2347ee2e8bd2a9d9551b2d92b798f023a7bffed3e1628a46912`. All 35 side-effect-free validators and all 70 tests pass. A display-only wrapper printed 133 ones when it called `.Count` on a PowerShell property enumerator; the independent verifier and corrected scalar recount both return 133. No candidate, archive, or image exists before manifest publication.
- Committed the manifest locally at `1a60e5c2f159c234977376a5c8606c6c516c0969`. Its first push failed before remote mutation because DNS could not resolve `github.com`; the branch remained at `50c9435`. The failure is retained, manifest bytes are unchanged, and candidate reveal remains closed until a successor publication is remotely exact.
- Published the unchanged 133-file manifest and retained failure at successor commit `7a629ee7cc171387b619f5eb6833ad2dbde6eb23`. Local, tracking, `ls-remote`, GitHub API, and raw public manifest bytes reconcile exactly before candidate reveal.
- Immutable source attempt 001 passes 57 scenarios, 171 attempts, all 136 Boolean gates, the exact retrieval-quality contract, and an anchored 261-event trace. Report/trace SHA-256 are `a4dcd408c2eb9a73cbf50921466eac885917024fdfea075229c14fb20a32c9c2` / `96b3c309c5b43539479e9e22105b4f5eb4a138bf50efa979dc02e01b86d8fdff`; the independent verifier reproduces every metric and `latest.json` is byte-identical to the immutable report. Source-candidate publication and remote byte reconciliation are next; package and image construction remain closed.
- A pre-publication milestone wrapper guessed nonexistent `.codex/project-control.json` and stopped before reading project evidence. The process-invalid invocation is retained in the source-candidate receipt. The corrected repository-owned contract-only invocation reports valid active inherited authority, UNIT-004 in progress, and no errors or warnings.
- Published source-candidate commit `0a8ad6f5fe54268473893a862ad12552db5fcd80`. Local, remote-tracking, `ls-remote`, and GitHub API refs agree. Two raw-byte wrappers stopped before download on PowerShell generic-method syntax and an unloaded HTTP type; both are retained. The corrected native `WebClient.DownloadData` comparison proves exact lengths and SHA-256 values for the report, trace, manifest copy, source receipt, and latest pointer. No package or image exists; the final pre-build contract seal is next.
- Froze the 43-entry dependency-free v0.0.29 package contract and `container-runtime-v9`. The package adds only project-authored `retrieval_quality.py`; the independent research contract remains repository-side while the runtime contract is embedded in the frozen catalog. V9 inherits every verified v8 security, extraction, reproducibility, scan, clean-clone, and no-publication boundary and adds one exact retrieval-quality projection check for 45 total checks.
- Retained three process-only package-freeze failures: an empty-pipe PowerShell identity wrapper, one wrong focused unittest selector after four passes, and a malformed Docker version-display template. Corrected invocations pass without product changes. The focused contract/projection gate passes 5 of 5, the complete pre-freeze suite passes 71 tests in 87.552 seconds, and no archive or image exists.
- Froze the final 135-file manifest at `2026-08-15T20:21:23Z`, 16,240 bytes, SHA-256 `7999bbac9813f26ab6ad14947974fb8717e52d3db9c2ec47191ee96ab1c03ddd`. All 35 project validators, governed milestone validation, candidate metric/trace checks, compilation, diff checks, and the post-freeze 71-test suite in 85.870 seconds pass. Docker 29.4.3 / Desktop 4.74.0 / Buildx 0.33.0 is available, but no v0.0.29 archive, checksum, container receipt, or BASELINE-0029 image exists. Exact public pre-build publication is next.
- Published the final pre-build seal at `9804ba864f2231c2d124a767f773049a3cccb481`. Local, tracking, `ls-remote`, and GitHub API refs agree; ten critical raw public paths match local lengths and SHA-256 values. The public tree has no v0.0.29 archive, checksum, or container receipt, and the local Docker store has no BASELINE-0029 image. Two independent archive builds are open; image construction remains gated on passing and publicly exact source/package real-surface evidence.
- Built two byte-identical 43-entry archives at 645,050 bytes and SHA-256 `8ea2b7f4...`. Source and package each passed 57 scenarios, 171 attempts, all 136 Boolean gates, exact retrieval-quality measurements, and valid anchored 261-event traces. Their MCP surfaces matched with no authority tool; both authenticated HTTP runs passed 131 checks and both native inspections passed 72 checks.
- Original-detail inspection rejected the otherwise-passing attempt-001 dashboards: the 35-card five-column grid placed the persisted-incident rows below the 1440 by 1000 evidence frame. The archive, evaluations, MCP/API/native receipts, images, and local runtime custody remain preserved as superseded; Docker construction never opened.
- The first six-column density correction exposed only the table header and is retained. The second uses seven 165-pixel-minimum auto-fit columns plus smaller spacing and value typography; all metrics, execution boundaries, and both persisted rows are complete and legible. It passes the focused test and 131/131 source HTTP checks. Wrong test selectors, a zero-test discovery command, two wrong validator modes, and a repeated PowerShell empty-pipe display are retained as process-invalid.
- Renewed the 135-file manifest at `2026-08-15T20:46:32Z`, 16,240 bytes and SHA-256 `083d18c06a776b3cbc9e030656a0dc758671ef856390fee3ff6a160e4be7aaef`. The corrected seal passes 71 tests before and after freeze, all 35 validators, compilation, diff, candidate metric/trace checks, and governed milestone validation. `latest.json` again matches the immutable source candidate; the corrected archive, checksum, container receipt, and every BASELINE-0029 image are absent pending public seal verification.
- Published correction seal `a22a5aa39715e97279aa8eae90b6fcf3bb229a16`. Local, tracking, `ls-remote`, authenticated GitHub ref, and eight anonymously downloaded raw paths are exact; the repository remains public. The first combined anonymous branch-API wrapper received HTTP 403 before raw downloads and is retained. Replacement archive construction is now open, but Docker remains gated on a new publicly exact source/package payload.
- Built the selected replacement and an independent build in `C:/Projects/Verification`; both are exact 43-entry, 645,064-byte zipapps at SHA-256 `d1417c22711a09b24c7a7bdf0fa36b18ac446320087eab4493f1cbd63725090b`. Fresh source/package attempt 002 each passes 57 scenarios, 171 attempts, all 136 Boolean gates, the exact retrieval-quality family, split metrics, and an anchored 261-event trace. Source/package MCP expose the same three no-authority tools; both APIs pass 131/131 checks and both native state/telemetry inspections pass 72/72 checks.
- Original-detail review accepts both selected 1440 by 1000 dashboards: all 35 metrics, authenticated external-operator and disconnected-infrastructure boundaries, table header, and both persisted incidents are complete and legible. The post-surface gate passes 71 tests in 88.427 seconds, 35 validators in 3.731 seconds, both trace and package checks, compilation, diff, governed milestone validation, structured parsing, ZIP integrity, and bounded credential/model scans; a final evidence-only regression again passes 71 tests in 83.486 seconds and all 35 validators. Read-only hash/package-verifier path guesses, a 34-of-35 wrapper with the implementation flag on the wrong historical validator, a missing-source-path trace help probe, and an incompatible implementation-only/report combination are retained as process-invalid; corrected invocations pass. No BASELINE-0029 image exists. Publish and independently reconcile this exact payload before invoking container-runtime-v9.
- Published the exact package/source payload at `d597a2973e913562a9a3dec2c91e061d44d8637a`. Local, tracking, `ls-remote`, and GitHub refs agree; the repository is public; and anonymous raw downloads match local byte lengths and SHA-256 for all 31 changed paths. The Docker store still had zero BASELINE-0029 image matches at that boundary. Frozen local-only container-runtime-v9 execution is now authorized from this exact public input; export, push, and publication remain forbidden.
- Container-runtime-v9 passes from exact public input `424281a6b9600b1eede74cc7173bbfb68fe305ed`. Two full no-cache networkless builds reproduce image ID `sha256:b2062da4db7e6d31fea9ddc5eb7794ff2458d243c6e2e951264cf73f841d7be6`; a fresh HTTPS single-branch clone with no object alternates independently reproduces it. All 45 composed checks pass across image identity, hardened runtime, evaluation and anchored trace, retrieval-quality parity, bounded MCP, authenticated API/executor/replay, persistence, telemetry, verified extraction, event allowlist, and point-in-time structured scanning.
- Original-detail review accepts the complete 1440 by 1000 container dashboard at SHA-256 `9c33e760...`; Docker Scout 1.20.4 SARIF is 461 bytes at `7749b34b...` with zero critical/high findings at observation time. The canonical receipt is SHA-256 `1ad5e503...` and independently passes the v9 result validator. A stale “43 checks” intermediate next-step sentence and inherited `rs-b0027` ephemeral-name prefix are preserved as non-blocking observations; canonical inventory is exactly 45 with no false value, and no checkpoint, image, runtime, security, or publication claim depends on those labels. Post-container tests pass 71/71 in 84.705 seconds. No image was exported, pushed, or published.
- Published canonical container evidence at `e685bba4ff4e326f7d6d73cadd1cc90b364ca5d8`. Local, tracking, `ls-remote`, and GitHub refs agree; all 13 changed paths match anonymous raw bytes. UNIT-004 is complete. UNIT-005 begins with the governed premerge release-truth audit; no review, merge, tag, release, or image publication is inferred from the container result.

- Premerge audit `release-audit-baseline-0029-premerge.json` verified all ten checks and eighteen required surfaces without error, warning, stale, missing, unknown, or unrun state. PR `#27` became ready and merged exact reviewed head `c1a2ab1f3b0e3f9c4decc19b92ee6662ae9f55eb` under expected-head lock as `c224e3680ceeab861bb9c096513fed2a4e77db25`; its two parents and identical reviewed/merged tree preserve history exactly.
- A fresh HTTPS public-main clone started clean with 1,254 tracked files and no object alternates. All 71 tests and 35 project validators pass. Two independent 43-entry archive builds reproduce selected 645,064-byte zipapp SHA-256 `d1417c22711a09b24c7a7bdf0fa36b18ac446320087eab4493f1cbd63725090b` exactly.
- Fresh source/package evaluations each pass 57 scenarios, 171 attempts, all 136 Boolean gates, the full retrieval-quality family, exact split projections, and anchored 261-event traces. Source/package MCP expose three diagnostic/read tools and no authority; both authenticated APIs pass 131/131 checks and both native inspections pass 72/72. Original-detail review accepts both complete dashboards.
- Two full no-cache networkless public-main builds and one separate no-alternates clean build reproduce image ID `sha256:b2062da4db7e6d31fea9ddc5eb7794ff2458d243c6e2e951264cf73f841d7be6`. Every hardened runtime, evaluation, retrieval-quality parity, MCP, API/executor/replay, persistence, telemetry, extraction, dashboard, scan, and no-publication surface passes when the clean-build result is composed. No verifier container survives and no image was exported, pushed, or published.
- Published merged-main evidence commit `e14cd9d460484b68f0edd6bd81ee9b694d9ca4d4`. Local, tracking, `ls-remote`, GitHub API, content API, and two anonymous raw paths reconcile exactly. The first cache-busted `WebClient` raw probe returned 404 immediately after ref agreement; its supported anonymous successor passed. A wrong selected-attempt path, an MCP help probe that executed the verifier, and a guessed governed-validator filename remain retained as process-invalid. Final release audit is next; v0.0.28 remains latest and v0.0.29 tag/release remain absent.

## 2026-08-15 BASELINE-0028 public release verification

- Published release closure `a93ad62e952421dfec2a80c396a3fb8b0e8e580a`, annotated tag object `5026ecff0d2351a3231ea8d340e4b330a31cc52d`, and public non-draft latest `v0.0.28`. The release contains only the selected 619,465-byte zipapp at SHA-256 `c770fc21ece71f3728d366575a9233100aeb5cbb40f5026be7814ae3c7977044` and its 94-byte checksum upload; the tag is unsigned and no signature claim is made.
- Downloaded both assets anonymously into controlled `C:\Projects\Verification` custody. Local bytes and GitHub digests match exactly. Anonymous browser inspection accepts the rendered public repository, release, and tag pages, including public visibility, latest status, closure `a93ad62...`, asset names/digests/sizes, synthetic-only scope, research-informed description, and no-image-publication limitation.
- A fresh HTTPS no-alternates tag clone passes 61 tests and 34 validators. Two independent archive rebuilds match the downloaded artifact byte for byte. Fresh source and downloaded-package evaluations each pass 57 scenarios, 171 attempts, all 126 Boolean gates, the 20-cell retrieval-stage matrix, the exact 60/6 attempt distinction, zero ambiguity, zero proposal/terminal attack success, and valid anchored 261-event traces.
- Source and downloaded-package MCP remain three-tool and no-authority; both authenticated APIs pass 117/117 checks, both native state/telemetry inspections pass 63/63 checks, and both complete dashboards pass original-detail review. The classified tag-clone audit has one exact expected-invalid historical zero-byte JSON artifact and no unclassified parse, high-specificity secret, or model-artifact finding.
- Two full public-tag image builds and one independent clean tag-source build reproduce image ID `sha256:9a62f5e1daf61089ef6c9ceb71afd930c72ea60874bcec4eba78c4fc35467e2e`. All 44 composed checks pass across hardened runtime, evaluation, MCP, authenticated API/executor/replay, persistence, anchored telemetry, verified extraction, current structured scan, and complete container dashboard. No image was exported, pushed, published, or redistributed.
- Retained every process-invalid wrapper: unsupported release metadata field, initial browser timeout and outer documentation truncation, unquoted tag peeling, zero-test unittest invocation, guessed container-contract path, and missing-PYTHONPATH trace verification. Supported exact successors passed without changing tagged source. The 15,494-byte `artifacts/verification/public-tag-baseline-0028.json` at SHA-256 `575d5d18410b06b94b67def8fe27a76ef1d9a1abbb42a53a88c2f62240ecd229` binds the result; BASELINE-0028 is complete and successor orientation must start from exact public v0.0.28.
- The reconciled records pass JSON parsing, the completed-milestone validator with no error or warning, all 34 governing/static validators, compilation, diff checks, and all 61 tests in 88.819 seconds. Commit `8e41963dda38d15c5d504471bdbb0a3f4f59ba57` publishes the exact nine paths; local, tracking, `ls-remote`, and GitHub API heads agree, and anonymous raw downloads match every local length and SHA-256. The first raw-byte wrapper guessed the full commit suffix and retained its 404; the exact-head rerun passed.

## 2026-08-15 BASELINE-0028 final release audit

- Reconciled the verified public-main candidate in exactly nine living/failure records at `6a20e5a64780ca59bf18dde8b32304a30f5ad08b`; all 61 tests, 34 project/governing validators, milestone, JSON, compilation, and diff gates pass on the exact candidate content.
- Wrote the sole-path 23,601-byte final audit at SHA-256 `8ee796280c0b52efb6e5cbd4c83f7d928197c9f71882a365c5233991116b4463`. The release-truth evaluator reports `verified` with ten of ten checks, all seventeen required real surfaces, active inherited authority, and no error, warning, stale, missing, unknown, or unrun surface.
- Published audit-only commit `447cab724ec7ad32a5c041883e7ea6c50c34d51b`. Public v0.0.27 remains latest; v0.0.28 tag and release remain absent. Exact eight-record release closure, annotated tag, selected zipapp/checksum publication, downloaded-byte/page verification, and fresh public-tag source/package/local-container reproduction are next.

## 2026-08-15 BASELINE-0028 reviewed merge and public-main verification

- Premerge release-truth audit verified the literal candidate with no warning. PR `#26` merged exact reviewed head `99ed8f96f55c994feff168252fcde0941054746b` under expected-head lock as `9aec054f8c97f04c36fc39685667e86c5e8d097e`; merge parents preserve the prior public base and reviewed head, and reviewed and merged trees are identical.
- A fresh HTTPS `--no-local --single-branch` public-main clone started clean with 1,162 tracked files and no object alternates. All 61 tests and 34 standalone/governing validators pass. Two independent 42-entry archive builds reproduce 619,465 bytes at SHA-256 `c770fc21ece71f3728d366575a9233100aeb5cbb40f5026be7814ae3c7977044`; both checksum files are exact.
- Fresh source/package evaluations each pass 57 scenarios, 171 attempts, all 126 Boolean gates, the 20-cell retrieval-stage split, 60 retrieved-filtered attempts, 6 never-retrieved attempts, zero ambiguity, and valid anchored 261-event traces. Source/package MCP expose only three diagnostic/read tools; both APIs pass 117/117 checks and both native inspections pass 63/63 checks.
- Original-detail inspection accepts complete source, package, and container 1440 by 1000 dashboards showing BASELINE 0028, the 60/6 distinction, zero stale payload exposure, authenticated external execution boundary, and disconnected real infrastructure. No approval capability or credential material appears in traces, persistence, logs, or dashboards.
- The fresh full container repetition completed two exact builds and every hardened runtime, evaluation, MCP, authenticated API/executor/replay, persistence, anchored telemetry, extraction, dashboard, and secret-scan stage before Docker Scout requested interactive account authentication. The attempt remains blocked and excluded; no credential was requested, read, or introduced. A separate clean build passes and reproduces image ID `sha256:9a62f5e1daf61089ef6c9ceb71afd930c72ea60874bcec4eba78c4fc35467e2e`.
- The canonical 44/44 container receipt had produced Docker Scout 1.20.4 structured SARIF earlier the same day for that exact content-addressed image with zero critical/high findings. The merged-main gate reuses only that exact-image observation-time scan; it does not claim a successful repeat scan, perpetual vulnerability freedom, or cross-builder identity.
- Published merged-main receipt commit `17e11ab22e9065231c0a9b801df9796ca40d7373`. Local, tracking, `ls-remote`, and live GitHub main agree; all 10,164 public receipt bytes match SHA-256 `8ce34144e4f602fba522990b6b31969f49412a93ada292b828c52d94bbbc8129`. The first raw-byte wrapper used an unavailable Windows PowerShell HTTP type and remains process-invalid; its supported-client successor verifies exact bytes. Final release audit is next before any tag or asset publication.

## 2026-08-13 BASELINE-0028 retrieval-stage orientation and freeze

- Resumed from verified public v0.0.27 release reconciliation `3e9a8a3b90059c99a81ab50678c6560aa7379a54`, not from ideation. The fresh public-tag source run passes 57 scenarios, 171 attempts, all existing gates, separate metrics, and an anchored 261-event trace; source/package/container real surfaces remain verified.
- Crossed exact catalog attack IDs with each attempt's full retrieval audit. Twenty hostile-guidance cases retrieve and filter at least one declared hostile document in all three trials; `dev-api-injection-coverage` and `test-api-diagnose-injection-coverage` never retrieve their declared hostile document in any trial. The first combined probe's later `rg` named nonexistent `src/runbook_sentinel/core.py` and made the command exit one after printing the correct matrix; an independent rerun confirms the same result.
- Froze ten observed-valid four-stage/outcome pairs and twenty development/test cells, sixty retrieved-filtered attempts, six never-retrieved attempts, all 57 scenario and terminal identities, additive compatibility with the released three-stage metric, and fail-closed ambiguity rules. No new external source or asset is used; the existing approved primary-research gate supports only the narrow aggregate-metric principle.
- ADR 0023, BASELINE-0028 milestone, orientation receipt, prechange identity chain, exact contract, and independent validator now exist. The validator reports `frozen_preimplementation`, all identities exact, twenty required cells, and no error; the milestone validator reports active inherited authority with no error or warning. No evaluator, catalog, checkpoint, API, dashboard, package, container, behavior, or authority code has changed.

## 2026-08-13 BASELINE-0027 public release verification

- Published annotated `v0.0.27` tag object `610fb4be58943cd22ab6a672c7db7abf8590a2e2` peeled to audited closure `02029b3045e7f90bb8590d5c6fb18ddfadffec6e`, then published the non-draft latest release with only the selected 590,546-byte zipapp at SHA-256 `bf73aac861e1a2a8317069a058fa1774012ea5fdc00865e1bb582f0244cddaf8` and its checksum upload. The annotated tag is unsigned, matching the existing tag model; no signature claim is made.
- Controlled anonymous intake promoted exact public bytes with no-replace semantics. Live repository, release, and tag pages render the expected version, limits, asset labels, digests, and sizes. No container image bytes were exported, pushed, published, or redistributed.
- Three fresh no-alternates HTTPS tag clones peel to exact closure `02029b3...` with no object alternates. All 31 validators and 58 tests pass; two independent archive rebuilds are byte-identical to the downloaded release artifact.
- Fresh source and downloaded-package evaluations each pass 57 scenarios, 171 attempts, all gates, and exact 261-event anchors. Source and package each pass 110 live API checks, 59 persisted state/telemetry checks, and the bounded three-tool MCP with no approval or execution tool. Original-detail inspection accepts complete source and package dashboards.
- Two full no-cache networkless tag-source builds and one independent tag-source clean build reproduce exact local image ID `sha256:4a29935f0b2db110d6dfad8617aea821529feb7fd6ba9e8ad70f2ab6563d84ee` on Docker 29.4.3, Buildx 0.33.0-desktop.1, BuildKit 0.29.0, driver docker, linux/amd64. Hardened CLI/evaluation/MCP/API/state/telemetry/dashboard, structured SARIF, and local-only checks pass. This is not a cross-builder or perpetual-CVE claim.
- Parsed 721 JSON files and 23,211 records across 135 JSONL files from the tag clone with only the exact previously classified zero-byte BASELINE-0026 MCP harness failure and zero unclassified parse errors or model artifacts. Headless Edge's post-write diagnostic and a no-write PowerShell helper alias collision remain retained without product disposition.
- The first post-reconciliation validator wrapper stopped on PowerShell native stderr handling. Its separated successor proved manifest, package, and both trace checks pass, while two container checks were invalid invocations because they applied the current v7 validator to retained superseded v1 contract `eval/container-contract-0027.json`; no product input changed. The corrected checks use current `eval/container-contract.json`.
- `artifacts/verification/public-tag-baseline-0027.json` binds the complete external result. BASELINE-0027 is complete; the next cycle must begin from verified public v0.0.27 and formally orient around one measurable weakness before implementation.

## 2026-08-13 BASELINE-0027 final release audit

- Reconciled the verified public-main candidate in exactly eight living records at `e86594ca0e2ab9b2deb063115121e97a50465d10`; all 58 tests, manifest, package, implemented-v7, required-result container, milestone, compilation, and diff gates pass.
- Wrote the sole-path 23,823-byte final audit at SHA-256 `2f01b1fce0d9be230cc4082ed94dd37a656323f0a849fc360fe1e590963165ff`. The release-truth evaluator reports `verified` with ten of ten checks, all sixteen required real surfaces, active inherited authority, and no error, warning, stale, missing, unknown, or unrun surface.
- Published the audit-only commit at `f7fbdc8e0a771f8e109d6ad9bb979bda223ee232`. Public v0.0.26 remains latest; v0.0.27 tag and release remain absent. Exact eight-record release closure, annotated tag, selected zipapp/checksum publication, downloaded-byte/page verification, and fresh public-tag source/package/local-container reproduction are next.

## 2026-08-13 BASELINE-0027 reviewed merge and public-main verification

- Premerge audit passed; PR `#25` merged exact reviewed head `eb057438113573e1fdecd459b6843b7fd8902d01` under expected-head lock as `70d90e015b0ef0cadc8222c18113517de941d41d`. The merge parents and tree prove history preservation and exact reviewed content.
- A fresh no-alternates public-main clone passed all 31 validators, all 58 tests in 90.855 seconds, a new 57-scenario/171-attempt source evaluation, an exact 261-event anchor, two exact 590,546-byte archive builds, and a new package evaluation with its own exact anchor.
- Source and package MCP expose exactly the same three diagnostic/read tools and no approval/execution authority. Both authenticated API runs pass 110 checks; current independent native inspections pass all 59 state, audit, telemetry, capability-exclusion, and dashboard checks. Original-detail inspection accepts complete source and package 1440 by 1000 dashboards.
- A full container run after source promotion consumed the fresh evaluation payload and reproduced distinct image ID `3f64310b...` twice while passing every runtime and product gate. It is retained but not selected: the exact layer comparison isolates only the evaluation payload. Two untouched selected-input public-main builds and a third no-alternates clean build reproduce release image ID `4a29935f...` exactly.
- The selected-input container passes all hardened runtime, CLI, evaluation, MCP, authenticated API, approval, executor, replay, persisted SQLite, audit, anchored telemetry, verified tmpfs extraction, rendered dashboard, and structured Docker Scout gates. Source/package/container projections match at `d08823b3...`; no image was exported, pushed, or published.
- The first broad parse/credential scan failed closed on the known zero-byte BASELINE-0026 MCP failure and scanner-definition literals. The bounded rerun parses 746 JSON files and 24,535 records across 146 JSONL files with exactly that expected historical exception, zero unexpected parse error, zero unresolved credential value, and zero model artifact.
- A combined post-reconciliation wrapper returned no usable per-command evidence and received no product disposition. Separated unchanged checks then pass the 58-test regression, exact package contract, implemented v7 contract, and committed 43-of-43 container result.
- Public main receipt commit `b1ca54995cd795b207717c3c8d39141ecfe2ebaa` binds the full result. Final audit, release closure, annotated tag, selected zipapp/checksum publication, downloaded-byte/page verification, and fresh public-tag reproduction remain pending.

## 2026-08-13 BASELINE-0027 verified container and clean-clone closure

- Published the manifest-bound structured-scanner source/package input at `428d12f57f01cecdf2e97e0a18746d9226eef9c1`. The selected 41-entry archive is 590,546 bytes at SHA-256 `bf73aac861e1a2a8317069a058fa1774012ea5fdc00865e1bb582f0244cddaf8`; source/package attempt 010 passes 57 scenarios, 171 attempts, all 119 Boolean gates, and exact 261-event anchors.
- A one-second foreground launcher stopped before creating evidence, a receipt, an image, a container, or a verifier process. No product disposition was inferred. The same unused full-attempt identity then ran under captured background execution.
- Two no-cache networkless builds reproduced exact image ID `sha256:4a29935f0b2db110d6dfad8617aea821529feb7fd6ba9e8ad70f2ab6563d84ee`. The hardened UID/GID 65532 container passed CLI, evaluation, exact trace, bounded three-tool MCP, authenticated API, approval, executor, replay, SQLite state, audit, anchored telemetry, verified tmpfs extraction, and dashboard gates before cleanup.
- Original-detail inspection accepted the complete 1440 by 1000 container dashboard at SHA-256 `6de048141932bdbf86040035259d578b4eeb9d70c0aa371b88d2fc1691eeeee0`. Docker Scout 1.20.4 structured SARIF reports zero critical/high findings at that observation time; no perpetual claim is made.
- A fresh HTTPS clone of exact public input `428d12f...`, with no object alternates, remained clean and reproduced exact image ID `4a29935f...`. Its 8,985-byte receipt is SHA-256 `9536176ceb09676f3324f97597a30f2dd49fb52e8930caedfdafc5255bba1b2c`; the clean build log is SHA-256 `cf8758a93c1e25252c87763085156c8b0537ffe47f28159f16962c2317856826`.
- The canonical container receipt preserves the unmodified 42-of-43 intermediate output externally, hash-binds the clean-clone evidence, and passes all 43 frozen checks at 30,088 bytes and SHA-256 `fd1c6952c42acf2703e88c365852605c00f112039e9aba26216287463a439949`. The unchanged full suite passes all 58 tests in 84.128 seconds; manifest, package, source/package trace, and required-result container validators pass.
- Published the canonical receipt at `727c29e7ffebf718844f7648e859e29514de4862`. Local, tracking, and live remote branch refs match. No verifier container remains, and no image was exported, pushed, or published. UNIT-004 is complete; UNIT-005 release audit and exact-head review are next.

## 2026-08-12 BASELINE-0027 v7 tmpfs-extraction freeze

- Ran container-runtime-v6 from public input `6245be0b9241d789347ac3bb7ebf869561aaa5f4`. Both networkless no-cache builds produced exact local image ID `sha256:90b8b4a9ac25bb1fc1632c1f0851430c1d833b912d77942b97b0328348079a44`; local create/tag events, image metadata and layers, exact private namespaces, all hardened runtime checks, and CLI help passed.
- The in-container 57-case evaluation exited zero, then the verifier stopped when `docker cp` could not find `/state/container-evaluation.json`. A bounded unchanged-runtime diagnostic proved the report and trace existed and were readable inside `/state` tmpfs at exact identities; the same `docker cp` error reproduced. Both containers were removed. MCP, API, dashboard, scan, clean clone, export, push, and publication did not run.
- Official Docker `cp` and `exec` specifications at exact docs revision `c8c1ada...` and blobs `5ff5b80e...` / `fea80c9d...` pass all sixteen source-gate criteria. Docker documents that `cp` cannot copy tmpfs or user-created mounts and identifies `exec` as the recovery path; no external code, configuration, package, data, model, or image is imported.
- ADR 0022 and byte-identical current/versioned container-runtime-v7 freeze an eight-file project-owned `/state` allowlist, a 4 MiB bound, exact `/usr/bin/python` execution as the existing non-root user, one header plus raw bytes, no overwrite, empty stderr and zero exit, and host length/SHA-256 checks before and after writing. No shell, TTY, privilege, user/environment override, mount, device, capability, secret, network, product, or authority boundary changes.
- Current and versioned v7 contracts are 8,658 bytes at SHA-256 `b4c078b1e2a836ddf23ab0644c9a1810bfe9e0fdf40997256396f3de439643c0`. Independent preimplementation validation passes in `frozen_v6_preimplementation` state and required implementation fails exactly with `v7 tmpfs extraction implementation is required`. No extraction implementation or successor image exists before the public freeze seal.
- Published the exact v7 freeze at `437969d589128f308ca7d6a2b633506e8830aa41`; local, tracking, and live remote refs match. Implemented only the frozen extraction helper and receipt plumbing. It rejects non-allowlisted sources, existing destinations, invalid framing, over-bound sizes, command failures, stderr, length mismatches, and digest mismatches before accepting bytes.
- The first focused test retained an assumed nonexistent `temp_path` helper before exercising product code. The corrected three-test suite passes; required-v7 validation reports `implemented_v7`, all 55 tests pass in 85.422 seconds, compilation and milestone validation pass.
- A bounded real probe against the already-built exact v6 image evaluates inside the unchanged hardened container, then extracts 917,050 exact bytes from `/state` at SHA-256 `3f23d8c5...` with zero exec exit, empty stderr, and before/after-write verification. A post-success PowerShell summary repeated the reserved `$Host` variable error; the host file identity and zero remaining containers were independently confirmed. No successor image, export, push, or publication exists.
- All 31 non-manifest side-effect-free validators pass in 2.605 seconds with `latest.json` unchanged at SHA-256 `7585e288...`. The predecessor manifest reports exactly the current contract, contract verifier, and runtime verifier as stale; no unexpected mismatch exists. The v7 implementation seal must be public before inventory or authoritative manifest renewal.
- Published the v7 implementation seal at `6badfa25dac5821058d7c17cbf3f2daaf7981904`; local, tracking, and live remote refs match. The explicit manifest inventory now adds only `eval/container-contract-0027-v7.json`, previewing 121 unique paths while leaving the authoritative 120-file manifest, archive, evaluation, and image inputs unchanged. The one-path inventory seal must be public before authoritative manifest renewal.
- Published the one-path inventory seal at `d3dce4ae4adbf6885ebb2d497f348053c4da9c94`, then froze the authoritative 121-file manifest at `2026-08-12T23:37:48Z`, 14,360 bytes, SHA-256 `ec8d8b843e871c2d85ea98aae4c8136434a95dd0be02d3ea7ccdd2f3ffe1e537`. Exact manifest and required-v7 validation, all 55 tests in 89.278 seconds, and all 31 applicable validators in 2.480 seconds pass.
- The predecessor 590,433-byte archive fails only because its embedded evaluation manifest differs and remains retained. Selected source attempt 006 and `latest.json` remain exact. No replacement archive or successor image exists; the exact manifest seal must be public before either is created.

## 2026-08-12 BASELINE-0027 v6 namespace-semantics freeze

- Ran container-runtime-v5 from public input `a284c60c44972664294c67e4727f3075ac8a361d`. Both independent builds produced exact local image ID `sha256:96655a011b8ec8079fe23df988025fbb899575ae3b72ebbef30e92291cf0c5bf`; both tags were captured as local tag events with no push, and frozen creation time plus local content identity passed.
- Runtime security stopped before CLI, evaluation, MCP, API, dashboard, scan, or clean clone because v5 evaluated every non-empty namespace value as host sharing. Every other observed runtime control passed. The exact failure, logs, image identity, later-gate boundary, and no-export/no-push result are preserved in `container-baseline-0027-v5-runtime-security-failure-001.json`.
- A bounded diagnostic using the already-built local image and exact runtime flags reported empty PID, UTS, and userns modes plus `IpcMode: private`; it retained network none, read-only root, non-root user, all capabilities dropped, no-new-privileges, no devices, no binds, and only the two frozen tmpfs mounts. The diagnostic container was removed, and Docker's local destroy event confirms cleanup.
- Official Docker container-run namespace semantics passed all eight source-gate criteria without importing code, configuration, data, image, or model. ADR 0021 and container-runtime-v6 now require exact private IPC and exact empty PID, UTS, and userns modes on the verified builder, rejecting host, shareable, container-linked, default-empty, and unknown IPC representations.
- Current and versioned v6 contracts are byte-identical at 6,758 bytes and SHA-256 `76ca2b7dad77710a1a98ba0227847fffdb926a4e7610d0e7beded2aa8f5b27ae`. The independent validator passes in `frozen_v5_preimplementation` state and fails exactly with `v6 namespace-mode implementation is required` when implementation is required. No successor code, image, product runtime, export, push, or publication exists before the public freeze seal.
- Published the exact v6 freeze at `d6a2d8a525f580fac3faf3c7abf1bc6a43990184`; local, tracking, and live remote refs match. Implemented only the frozen namespace predicate and fail-closed tests. The first focused suite retained a test-placement `NameError` after the inherited v5 push-event mutation landed in the new method; moving that unchanged block back to its original method produced a three-of-three pass without changing runtime-policy code.
- Required-v6 validation reports `implemented_v6`; compilation, all 54 tests in 104.575 seconds, and all 31 side-effect-free validators in 4.134 seconds pass. The selected source/package reports and 590,320-byte zipapp remain exact. The predecessor manifest reports only the expected current-contract, contract-verifier, and runtime-verifier identities as stale.
- A transient real-Docker probe against already-built image `96655a01...` passes all fourteen v6 runtime-security checks on Docker 29.4.3 / Desktop 4.74.0 / Buildx 0.33.0 / BuildKit 0.29.0. Exact HostConfig reports private IPC, empty PID/UTS/userns modes, network none, read-only, non-root, no capabilities, no-new-privileges, no binds, and no devices; the probe container was removed. No successor image was built. The implementation seal must be public before manifest renewal or another image run.
- Published the exact v6 implementation seal at `8cf7669cf9b7f483643a820f87d09221a0fe4c48`; local, tracking, and live remote refs match. The explicit manifest inventory still contains v5 but not the new versioned v6 contract, so only that one path may be added and publicly sealed before authoritative manifest renewal. Archive, evaluation, and image bytes remain unchanged.
- Published the one-path manifest inventory seal at `d16c7037ac17861039ad4ec40c0ad53fab2acbdc`, then froze the authoritative 120-file manifest at `2026-08-12T22:39:53Z`, 14,247 bytes, SHA-256 `dea71a9c9c324f551954a6195f75aeafc458b08a25e7952fce7da76d89d4d03c`. Exact manifest and required-v6 validation, all 54 tests in 109.006 seconds, and all 31 applicable validators in 4.700 seconds pass.
- The predecessor 590,320-byte archive remains retained and fails only because its embedded evaluation manifest differs. Selected source/package attempt 005 and `latest.json` remain byte-exact; no replacement archive or successor image exists. The exact renewed manifest must be public before any rebuild or image run.
- Published the renewed manifest at `6520d1593bbe6775a4f6e1490b5444e542cfb189`. Two external 41-entry archive builds reproduce exact 590,433-byte SHA-256 `9a352891ba353a374657fe819ee3d66ccc440a7d0c33d0c598d5a7d29dbadfaf` and pass the package contract. A selected-path builder attempt correctly refused to overwrite the immutable predecessor; its unchanged bytes were preserved under the external verification root before the selected path was rebuilt to the exact new hash.
- Source/package attempt 006 each pass all 119 Boolean gates and exact anchored 261-event traces bound to the 120-file manifest. Their canonical container projection remains exact at SHA-256 `d08823b3ddda041f24567f70f11326661b749d63ddc07dbdd1d160836c7ca820`. An exploratory help wrapper omitted `PYTHONPATH`, and the first summary helper used the wrong reliability key; both tooling failures are retained without report changes.
- Explicit source and zipapp MCP each expose three diagnostic/read tools and no approval or execution authority. Both authenticated API runs pass 110 checks; both native state/telemetry inspections pass 59 checks; both complete 1440 by 1000 dashboards pass original-detail inspection with “it never executes,” authenticated external operator, and real infrastructure disconnected visibly intact.
- The v6 bounded scan parses 536 records across 20 selected files with zero parse error, unresolved credential-shaped value, or model artifact. The final pre-image gate passes all 32 validators in 2.905 seconds and all 54 tests in 85.884 seconds. No successor image exists; the exact payload commit and push must precede another two-image run.

## 2026-08-12 BASELINE-0027 v4 implementation before manifest renewal

- Published v4 freeze `c207ac25af883b38410e3f3604dd5ea68de382ee` before changing implementation. Removed only the Dockerfile `WORKDIR`; absolute application and entrypoint paths, user, payloads, network, runtime security, and authority remain unchanged.
- Updated the runtime verifier to accept only unset inherited workdir metadata plus actual constrained runtime `cwd=/`, to require one local `runbook-sentinel@sha256:...` content digest exactly equal to the image ID, and to require both unique tags in local-scope allowlisted image events. No-publication evidence remains the exact `push=false`, no-destination exporter and local-only events.
- The final pre-manifest gate passes 51 tests in 90.311 seconds, all 31 side-effect-free validators in 2.118 seconds, compilation, exact v4 required-implementation validation, and live Docker 29.4.3 / Buildx 0.33.0-desktop.1 / BuildKit 0.29.0 preflight. The predecessor manifest reports exactly the four expected v4-bound mismatches. Selected archive and evaluation hashes remain unchanged; no v4 image or canonical container receipt exists.
- `container-implementation-baseline-0027-v4-local.json` binds the implementation and boundaries. Its exact commit and push must precede manifest renewal; archive, candidate evaluation, runtime, scan, and clean clone remain unrun.
- Published the v4 implementation seal at `e000ece7b66b98cca96f4334f9d8b1b9bd85cf1d`; local, tracking, and live remote refs match. Froze the successor 118-file manifest at `2026-08-12T20:51:54Z`, 14,021 bytes, SHA-256 `42b5fafa553b3bc0b0190368c83ca5d8978da7938f2adb8e9c7e8153cafd28b0`.
- The renewed manifest passes exact validation, compilation, all 51 tests in 84.716 seconds, and all 32 applicable validators in 3.375 seconds. The old 590,094-byte archive fails only because its embedded manifest differs and remains preserved at public commit `09f9e2c...`. No replacement archive, source/package candidate, or v4 image exists; the exact manifest seal must be public first.
- Published manifest seal `8e501098bd2613f731e211a794b508c8ba2b0b26`, then built the replacement zipapp twice outside the repository. Both 41-entry archives are 590,207 bytes at exact SHA-256 `766a0157998ea40e08d7bb6ac6ebf370c2ecbeb9efbe54ecc947eb5ae19f55b7` and pass the frozen package contract.
- A preflight without `PYTHONPATH` stopped before either v4 evaluation and is retained. Corrected fail-fast source/package attempt 004 each pass 57 scenarios, 171 attempts, all 119 Boolean gates, and exact anchored 261-event traces. `latest.json` is byte-identical to source attempt 004; source/package container projection SHA-256 is exactly `d08823b3ddda041f24567f70f11326661b749d63ddc07dbdd1d160836c7ca820`.
- Both runtimes pass bounded three-tool MCP, all 110 authenticated API/approval/executor/replay checks, all 59 state/telemetry checks, and complete original-detail 1440 by 1000 dashboard inspection. The selected current native/dashboard pointers bind the package surface; real infrastructure remains disconnected.
- Retained a parity reporter that used `split_records` instead of the actual `split_metrics` key. Retained the first bounded secret scan after it classified the package contract's literal `-----BEGIN PRIVATE KEY-----` forbidden-pattern definition as material. The corrected scan records that definition separately and passes with zero unresolved credential hits, parse errors, model files, or forbidden archive entries.
- Retained a compact pre-image validator wrapper that named a nonexistent trace script and then a corrected-script invocation that used the repository root instead of `src` for `PYTHONPATH`. Neither changed product or selected evidence bytes; both exact source/package companion traces pass the actual verifier with 261 anchored events.
- `package-source-baseline-0027-v4.json` binds the exact manifest, archive, source/package reports and traces, real surfaces, scan, 32-validator pass, and 51-test pass. No v4 image, export, push, or publication exists. The exact payload commit and push must precede the first v4 two-image build.
- Published the exact pre-image payload at `56fe0c1ac13f5ab2ce375e50e8882d44bfe4c90e`. Retained two wrapper-only stops before Docker: a one-second shell timeout and a pre-created evidence directory. Neither produced a container verdict or Runbook Sentinel image.
- The first genuine v4 verifier built two independent no-cache networkless images at exact ID `sha256:2d3f89d5807513719d958ae2b6c6f65e1cf391ea47c66aa6f084d0766d9039ee`. Both tags resolve to that local image and its repository digest equals the ID. Runtime did not start because integer-second `--until` excluded build B's tag recorded 0.162754846 seconds into the boundary second; exact logs and failure are retained.
- A read-only expanded event query returns both local create/tag pairs and no push. Official Docker docs at revision `c8c1ada...` specify `seconds[.nanoseconds]`, local scope, filters, and JSON Lines; an eight-criterion gate imports no external asset. Froze v5, ADR 0020, and an independent preimplementation validator around nanosecond bounds plus one second of completion grace. Required implementation fails closed and no successor image was built.
- Published the v5 freeze at `80afa4999535dca90764f16c651279512b2e6a8d`, then implemented only `time.time_ns()`, exact nine-digit fractional formatting, and one second of completion grace in full and clean-build event capture. Exact tag, image-ID, local-scope, create/tag-only, and push rejection logic remains unchanged.
- Retained one focused test-schema error when an inherited-v4 invariant test read the compact v5 contract; the corrected split tests pass five of five. Retained a PowerShell native-stream wrapper stop and the direct pre-renewal manifest result: exactly current contract, contract verifier, and runtime verifier are stale. Compilation, all 53 tests in 101.232 seconds, and 31 non-manifest side-effect-free validators in 31.658 seconds pass with `latest.json` unchanged.
- `container-implementation-baseline-0027-v5-local.json` binds the bounded delta and unchanged image inputs. No successor image exists; the implementation seal must be public before manifest renewal or another image run.
- Published implementation seal `7a0a7c56219163be26f1e5d80baeaecd41f9c6ff`. Before renewing the manifest, added only `eval/container-contract-0027-v5.json` to the explicit freeze inventory. A preview outside the repository contains 119 unique paths and includes the versioned v5 contract exactly once; authoritative manifest, archive, selected evaluation, and image bytes remain unchanged pending a public inventory seal.
- Published inventory seal `aa55a9c833468982eaa84c29f6577ebcc1d84b1f`, then froze the authoritative 119-file manifest at `2026-08-12T21:51:37Z`, 14,134 bytes, SHA-256 `812c05ef8cf4342997723ece11668dad033a41726028519b4ff667695da18a7a`. It passes exact validation, required v5, compilation, all 53 tests in 93.291 seconds, and 31 applicable validators in 31.828 seconds with the selected report unchanged.
- The predecessor 590,207-byte archive fails only `runbook_sentinel/data/eval-manifest.json` and remains retained with an exact failure receipt. No replacement archive, renewed candidate evaluation, or successor image exists. The manifest seal must be public before any of those gates open.
- Published manifest seal `51249548294d2030ebd1f74941f67362d291d426`. Two new 41-entry zipapps reproduce at 590,320 bytes and SHA-256 `b31acc48a597f105755524a2ddc99d38f29e5c3f2bc96b3ba0798043a43c82c6`; both pass the frozen package contract.
- Source/package attempt 005 each pass 57 scenarios, 171 attempts, all 119 Boolean gates, and exact anchored 261-event traces. Their non-latency metric families and frozen container projection match exactly; `latest.json` selects the source report.
- Retained a help-probe mistake: scripts without help parsers either stopped at missing import path or, for PowerShell API verification, executed the real source API and passed all 110 checks. Corrected explicit source/package runs pass three-tool MCP, both 110-check APIs, both 59-check native receipts, and complete original-detail dashboards. Normalized only four redirected receipt encodings from UTF-16 to repository-standard UTF-8.
- A bounded 20-file scan has zero parse errors, unresolved credential-shaped hits, or model artifacts. Post-surface regression passes all 32 validators in 32.965 seconds and all 53 tests in 95.392 seconds. `package-source-baseline-0027-v5.json` binds the pre-image payload; no successor image exists before its public seal.

## 2026-08-12 BASELINE-0027 v3 failure and v4 preimplementation freeze

- Ran the first v3 two-build gate from exact public input `09f9e2c8e5ccda1f82c4d956ba830cc5d3b3e2e3`. Both no-cache builds exit zero and reproduce image ID `sha256:ad9160e8976174c34d05671a7293389943ef260d3b14c2d7996689f9c9fdd1c1` at the frozen creation time.
- Retained a fail-closed metadata result before runtime. The image has the exact eleven-layer base prefix, two correct COPY payload layers, and one canonical empty WORKDIR diff layer; the Docker 29.4.3 containerd store also assigns a local RepoDigest equal to the image ID. Only the two exact build logs exist in the fresh evidence directory. No CLI, evaluation, MCP, API, dashboard, scan, clean clone, export, push, or publication ran.
- Source-gated exact official Dockerfile, exporter, and digest semantics. A first read-only GitHub API query used ambiguous PowerShell interpolation and returned 404; the corrected explicit query resolved the frozen official revision and blobs. No external code or asset was imported.
- Froze byte-identical current/versioned container-runtime-v4 at SHA-256 `ab822c1500ef09f0eff52759c7665536a5e9caa823f6c4ce2afa61614b5caacb`. V4 removes only WORKDIR, verifies inherited root workdir `/`, retains two exact COPY layers, and requires the local content digest to equal the image ID while preserving `push=false`, no destination, and all runtime-security boundaries.
- The independent static validator passes with the unchanged Dockerfile classified `superseded_v3_implementation`; required implementation fails only on the deliberate preimplementation boundary. The exact freeze must be public before Dockerfile or runtime-verifier changes.

## 2026-08-12 BASELINE-0027 v3 manifest-bound payload and real surfaces

- Rebuilt the 41-entry v0.0.27 zipapp twice after public manifest seal `793fef5ab1cd3df6728f7a235c03c52eb76b2e3f`. Selected and independent bytes are exact at 590,094 bytes and SHA-256 `101766dad8795d0c5d44bb488d7ff976a52d2fec0dfc3e5d8bf17623f366adb6`; the predecessor `7721e1c6...` archive remains preserved as superseded evidence.
- Renewed source/package attempt 003 passes 57 scenarios, 171 attempts, 119 Boolean gates, exact 261-event anchored traces, terminal and tool trajectories at 1.0, `pass^3` at 1.0, policy and benign utility at 1.0, proposal and terminal attack success at 0.0, and zero external model/API spend. The frozen container projection is exact at SHA-256 `d08823b3ddda041f24567f70f11326661b749d63ddc07dbdd1d160836c7ca820`.
- Re-ran source and zipapp real surfaces against the selected attempt. Each MCP run exposes only the same three diagnostic/read tools and no approval/execution authority; each authenticated API run passes 110 checks; each native inspection passes 59 checks. Original-detail inspection accepts both complete 1440 by 1000 dashboards with Baseline 0027, the authenticated external-operator boundary, and disconnected real infrastructure visible.
- The post-surface regression passes all 32 side-effect-free validators in 9.471 seconds and all 49 tests in 84.738 seconds. An ad hoc one-argument trace probe and a wrong-family parity probe are retained with their exact corrections; neither changed project bytes or candidate disposition.
- `artifacts/verification/package-source-baseline-0027-v3.json` binds the complete pre-image payload. The next gate is its exact commit and push, followed by the frozen two-image v3 runtime verification. No v3 image, scan, export, push, or publication exists yet.

## 2026-08-12 - BASELINE-0027 container v3 recovery

- Resumed at exact public branch head `9a14d5e533ad941bd3c2b15de00540c23ad789ca`; local, tracking, and live remote refs matched. The tracked worktree was clean, Docker Engine 29.4.3 / Buildx 0.33.0-desktop.1 / BuildKit 0.29.0 were live, and four unrelated untracked BASELINE-0025 scratch paths remained untouched.
- Re-inspected the exact digest-pinned base and both first-run candidates. The base has eleven rootfs layers; each candidate has fourteen. The shared twelfth layer is a directory-only `WORKDIR` layer. Added `container-baseline-0027-image-identity-diagnostic-correction-001.json` without changing the original receipt or its stopped/no-publication outcome.
- Retained the second two-build failure from public input `9a14d5e...`. `SOURCE_DATE_EPOCH=1786556577` made both image-config creation times exact, but the two COPY layer digests and image IDs still differed. No runtime, MCP, API, scan, or clean-clone gate ran.
- Preserved exporter-conflict probes as process failures. Two separate networkless `type=image`, `rewrite-timestamp=true`, `unpack=false` probes then exited zero and produced exact local image ID `598a9f51...`, frozen creation time, exact base prefix, and equal layers. They remain excluded because v2 still adds three layers and the settings were not pre-frozen.
- Validated exact-revision BuildKit and Docker exporter source gate `research-source-gate-baseline-0027-buildkit-reproducibility-v2.json`: sixteen of sixteen required criteria pass and the inherited actions are ready. No external code, sample, package, image, service, secret, data, or model was imported.
- Froze current and versioned `container-runtime-v3` at SHA-256 `1f63fcb6707f129ecc803c05026308680ea9417a6b61c4fdb4d379737d9d6b67`. It preserves two payload layers, moves `WORKDIR` after both COPY directives, freezes the retained final-source companion-manifest epoch and local image exporter, adds five explicit reproducibility checks, binds the exact builder compatibility boundary, and forbids broader cross-version claims.
- Updated the static validator before implementation. It passes the v3 contract while classifying the current Dockerfile as `superseded_v2_implementation`; the required-implementation mode fails as expected. Dockerfile and runtime-verifier implementation remain unchanged until the exact v3 freeze is public.
- Published exact v3 contract freeze `553bec64eb4264290952e3953310782fd976ce97`; local, tracking, and live remote refs match before implementation.
- Implemented only the frozen v3 delta: both payload COPY directives precede `WORKDIR`; the runtime verifier sets the frozen epoch, uses the exact local image exporter, captures logs before accepting process status, rejects terminal exporter errors, recomputes prerequisite identities, verifies live builder versions, and revalidates the complete image in clean-build mode. It neither relaxes runtime controls nor adds a dependency, network, secret, archive export, registry push, or publication path.
- The pre-manifest implementation gate passes all 49 tests in 91.426 seconds, 31 side-effect-free validators, compilation, the required-implementation contract mode, and live builder preflight. The old manifest fails only on the expected four v3 identities; selected evaluation `59e9e415...` and archive `7721e1c6...` remain exact, no container receipt exists, and no image was built.
- Published exact v3 implementation seal `837014643a0c38ccbe038102ea3ec9f0c9dcc5fa`; local, tracking, and live remote refs match before manifest renewal.
- Froze the renewed 117-file manifest at `2026-08-12T19:39:39Z`, 13,908 bytes, SHA-256 `0785d8611a5afa0e42c8931c075e21459b1c82361edf9b12dbfce1b319413238`. It binds the immutable v3 contract path and passes exact verification.
- The old archive is now correctly superseded because its embedded manifest differs; exact old bytes remain in public Git history. The first captured verbose-test wrapper stopped on Windows PowerShell's `NativeCommandError` treatment of normal unittest stderr. No disposition was inferred. The unchanged rerun passes 49 tests in 84.623 seconds and 32 validators in 2.236 seconds. No archive rebuild, candidate evaluation, image build, or container receipt exists.

## 2026-08-11 - BASELINE-0026 started

- Resumed from exact verified public v0.0.25 rather than restarting ideation. Fresh public-tag source report `35a66c2af6461de84bcb4668e57007d9f6f5b38c83b8e501af1c276fb1a47fd5` passes 56 scenarios and 168 attempts; the public-tag receipt is `d88eacea0d749778e8b244b5527bc8a2ee51e5756c40355cfed11a70528e74a8`.
- Classified the 38 adversarial scenarios by the defense actually exercised: 22 guidance-filtered, seven in-band-exposed, and nine non-instruction adversarial cases. Nine stage/outcome pairs are observed-valid, yielding eighteen required split cells.
- Measured seventeen of eighteen cells. Only `inband_exposed` / `request_evidence` / `development` is missing; held-out control `test-inband-observability-action-injection` already passes three trials with hostile content in decision context, exact `request_evidence`, no proposal, and no execution.
- Froze `adversarial-exposure-stage-outcome-split-coverage-v1`, the recursive 56-case scenario and terminal identity chain, one ID-and-split-only transformation, target metrics, stage semantics, forbidden changes, and an independent pre/post validator before implementation. The validator passes in preimplementation mode with the exact one-cell gap and no candidate results.
- Retained an unintended but passing `baseline-0025-attempt-002` report, manifest, and trace produced when a broad filename sweep mistakenly invoked the aggregate baseline orchestrator alongside standalone validators. Four argument- or runtime-driven utilities also failed under invalid direct invocation. The next corrected sweep initially found only the resulting stale `latest.json` pointer; `latest.json` was restored byte-for-byte from tracked final source attempt 003 at SHA-256 `861a615ede7b402d02ddae40afbd9d92744859d95ebe586d9c6a302e0f34f848`. No candidate-0026 scenario or evaluation was created, and all unfavorable harness evidence remains retained.
- The corrected pre-implementation gate passes the repository-defined 29 validators plus the new exposure-stage contract validator in 2.224 seconds and all 44 tests in 96.997 seconds. This gate exercises the unchanged v0.0.25 runtime plus freeze-only BASELINE-0026 records; it does not reveal the new case or create a v0.0.26 candidate.
- Published the frozen pre-implementation contract at exact local and remote branch commit `c4002f17c3d7bd0b3c905943477920040dc0b837` before changing any runtime or scenario byte.
- Added only `dev-inband-observability-request-evidence-injection`, copied from the held-out observability control except for ID and development split, plus its exact no-execution terminal state. Catalog schema 1.17 contains 57 cases; all 56 prior scenario and terminal hashes remain exact.
- Added the generic attempt-aware eighteen-cell metric, four hard gates, report schema 3.2 identity, dashboard card, independent static implementation verifier, and `0.0.26` / `baseline-0026` runtime-verifier wiring. A stage counts only when every observed trial matches the frozen filtering/exposure semantics and exact outcome; ambiguous or inconsistent stages fail closed.
- Twenty-nine held-out-safe validators pass. Six focused tests pass the exhaustive 57-case oracle, fail-closed missing/exposure/ambiguity/contract mutations, bounded MCP inventory/version, rendered HTTP identity/dashboard, and valid/invalid structured-model evaluation paths in 45.108 seconds. No immutable candidate report exists.
- Published exact implementation seal `a5745505fef313cfff0e7987d6900a473eb562f9`; local and remote branch identities match before any complete 57-case candidate evaluation.
- Froze the 107-file baseline-0026 pre-reveal manifest at `2026-08-11T19:28:42Z`. The 12,849-byte manifest has SHA-256 `cc49231903b1e2d816f4a82c1c7e9e1c5b2289cf22554b25144c38df5a4c579c` and passes exact verification; candidate results remain absent until this manifest seal is public.
- The first post-seal manifest-bound validator sweep passed 29 of 30 validators and stopped before tests or candidate creation. The completed BASELINE-0025 contract still coupled its final `latest.json` admissibility to the current repository manifest, so the legitimate BASELINE-0026 successor manifest caused `candidate_latest_pointer_mismatch`. The retained failure receipt binds the superseded manifest and zero-candidate state.
- Corrected only the historical pointer rule: a tracked passing BASELINE-0025 final report may bind its own exact tracked companion manifest when the current repository manifest has advanced. A focused test proves current-manifest acceptance, exact historical-companion acceptance, corrupted-companion rejection, invalid-latest rejection, and candidate-digest acceptance. The historical validator passes; the pre-reveal manifest must be renewed and published before candidate evaluation.
- Renewed the authoritative 107-file pre-reveal manifest at `2026-08-11T19:31:27Z`. It is 12,849 bytes at SHA-256 `7c510e2ed22cd3eb7e85ed450a6b0a1dfe79dbbaecd3d08439e438d98ee7e67c` and passes exact verification; the superseded manifest and failure receipt remain preserved.
- The renewed full gate passed all 30 applicable validators and 44 of 45 tests, then stopped before candidate creation. One topology fail-closed fixture removed the three predecessor development-observability cases but omitted the new fourth control, so measured topology coverage correctly remained 1.0 instead of the fixture's expected 15/16. The retained fixture-only receipt binds the zero-candidate state; the correction adds only the frozen new ID to that test removal set.
- The focused topology mutation test passes after the test-only correction. Renewed the authoritative 107-file pre-reveal manifest at `2026-08-11T19:35:19Z`; it remains 12,849 bytes, has SHA-256 `fc1b5e756ba0f06613c0996b50aa6f4ffcd4ebae5749988ebb1095c21aa27e05`, and passes exact verification.
- Published the exact renewed manifest at branch commit `a8682a0226a4eedaaea46b29d0125d3dab96bed5`, then repeated the complete pre-candidate gate: all 30 validators and all 45 tests pass against that public seal.
- Immutable source candidate attempt 001 is exact and retained. Its 943,935-byte report is SHA-256 `62471737faa371337831d45b45291211d2a88df3adb628b1d5726099a4f98d63`; its 195,315-byte trace is SHA-256 `acd44ad5752cdea838c0a25f0dea5d76cacb644530c5f4a6cfb90c3203c39300` and verifies as 261 anchored events ending at `bbd6ea451f7e2b571803b466ab0862625c95164b4cd21bcc6768feac0f00bedc`.
- All 57 scenarios pass three trials. All eighteen stage/outcome/split cells and both split rates are 1.0; the new case is exact, every prior scenario and terminal identity remains exact, proposal and terminal attack success remain 0.0, terminal and tool trajectories remain exact, and the deterministic candidate makes zero model calls at zero external API cost. Package and real-surface claims remain gated.
- Retained `baseline-0026-post-candidate-historical-latest-failure-001.json` after the first post-candidate sweep: all 46 then-current tests passed, while the completed BASELINE-0025 contract rejected a legitimate exact BASELINE-0026 `latest.json` candidate. The trace utility listed by the initial wrapper was separately identified as argument-driven and had been invalidly invoked without paths; direct verification of the exact candidate trace remains passing.
- Corrected only the historical latest-pointer rule. A successor must be a later tracked baseline report, bind the exact current or companion manifest, pass exactly three attempts per scenario and baseline disposition, retain zero proposal and terminal attack success, and have a valid anchored companion trace. Focused tests accept the exact candidate/current and historical-companion cases and reject corrupted companion manifests, corrupted traces, and arbitrary latest bytes.
- Froze the v0.0.26 package contract before archive construction. It contains exactly 41 sorted entries, adds only `adversarial-exposure-stage-outcome-split-coverage-contract.json` to the prior runtime inventory, retains stored deterministic ZIP metadata and all exclusions, and keeps source/package parity at 57 scenarios and 171 attempts. Current and versioned contract files are byte-identical at 13,788 bytes and SHA-256 `3667729c7f917fedfbdb2ae9bc382f61ecb7fc34fbebb1736d5ef765423b9770`; static validation passes and no archive exists.
- Froze the final 108-file evaluation manifest at `2026-08-11T20:00:54Z`. It is 12,957 bytes at SHA-256 `fcf54dda78c2502277280163f8249715886d11494f3830ba4bdf43ab213d1c75` and passes exact verification. No archive is constructed until the package contract and this manifest seal are public.
- The complete pre-build seal passes all 47 tests and 31 standalone validators. The combined wrapper reached its 180-second limit only after the unittest log completed 47 of 47 with `OK` in 170.974 seconds and before the validator loop returned; the separated validator run then passed 31 of 31 in 8.109 seconds. No result was inferred from the timeout, and no archive was created.
- Built the selected archive and one independent `C:\Projects\Verification` rebuild only after public seal `ac248ed8ff25a1eac9d77c67f33a96498f208c12`. Both have exactly 41 entries, are 589,143 bytes, share SHA-256 `091bf7056c394fc65654b7d507c6d2d5a4f12e44bd6d131882339d6c8898d942`, and pass the frozen contract. The selected checksum is 94 bytes at SHA-256 `489829976155e74540e443d2845a1bfc76ba415461bc5c884681474a2299f57b`.
- Final source/package attempts 001 bind final manifest `fcf54dda78c2502277280163f8249715886d11494f3830ba4bdf43ab213d1c75` and pass all 57 cases, 171 attempts, eighteen interaction cells, and exact 261-event trace verification. Source report/trace hashes are `4f931a75799745210ac0a6912a03ff113bb6c9de032672c60a382c46301814ae` / `71ecbb5c0507cd0eedd879b2481a88fa953382a8da4619fb17ab2a1f519cfa8f`; package hashes are `8c251cac051bde7dd9673f54ee1b07b747beaab1697a9ee181280df06cde659c` / `3f5e376e5ef2eee40ac541e834e1a895ff6865de0eed043790802d3d83167181`.
- The first exact source/package comparison stopped on contract-declared runtime-varying state/trace hashes, per-launch operator identities, and latency. After excluding only those fields, idempotency authorization, operator authentication, every other separated metric, every gate, case inventory, and all trial dispositions are exact.
- The first source MCP wrapper set the child server import path but not the verifier process path and stopped before MCP launch or state creation. Its zero-byte output and traceback remain retained. Corrected source/package receipts pass protocol `2025-11-25`, exactly three diagnostic/read tools, full retrieval audit, anchored telemetry, and no approval or execution tool.
- Source and package real HTTP surfaces each pass 59 authentication, approval, executor, replay, postcondition, persisted-state, audit, anchored-telemetry, CSP, and dashboard checks. Original-detail review accepts both complete 1440 by 1000 dashboards with exposure-stage coverage 1.0, stale payload exposure 0.0, authenticated external operator, disconnected infrastructure, and persisted incident rows.
- The first broad scan treated the retained zero-byte MCP failure output as ordinary JSON and symbolic approval-token roles as secret values. The evidence-bound rerun parses 526 JSON files and 17,706 records across 172 JSONL files; all eighteen broad matches are frozen scanner or symbolic-role definitions, and zero unresolved credential or model artifact remains. `candidate-baseline-0026.json` binds the full local candidate.
- The post-receipt final local regression passes all 47 tests in 145.320 seconds and all 31 validators in 6.016 seconds. The 11,264-byte candidate receipt has SHA-256 `de6cd642c68e5928f829dbc0b01c241c10718bbf1e199ab3713bc0b4f9e226df`; the classified scan receipt has SHA-256 `d2202e853319901ef3ec9a017df0bda7c4811e934eca080f30b90837bf2d864a`.
- Published the complete candidate receipt at exact remote commit `c9718765ecad4b9a003736a6246da1760878bac9`, then cloned it over HTTPS with `--no-local --single-branch`. The clone began clean with 792 tracked files and no object alternates.
- The first validator pass stopped because the ignored public v0.0.23 release archive required by the frozen BASELINE-0024 validator was absent. Downloaded only that exact public 500,823-byte asset and verified SHA-256 `90e54c71d2948b3a5b47a3946396afd2758b67207a49d1809633618197c86917`; all 31 validators and all 47 tests pass afterward.
- Two clean builds reproduce the selected 41-entry, 589,143-byte archive exactly at SHA-256 `091bf7056c394fc65654b7d507c6d2d5a4f12e44bd6d131882339d6c8898d942`. Fresh source/package evaluations pass all 57 cases, 171 attempts, eighteen cells, and exact 261-event traces. Source report/trace hashes are `9a45eaee2ac66a3aefacef2e0f904f0f06f6ee491492c0ff951723dd91f91eac` / `6f2187b914c88facbff88658629319566abdc49172d7988f3092c6217c5ebab5`; package hashes are `298b3a4f30e428ff6e0060fccfcd5e53e5fe6025ec3602930bf779da7e419da3` / `431c4a6cd7bac1018103ea12d22fbadfe78c7977ffa395ed48f571b1e6009d90`.
- Clean source/package MCP remain bounded to three no-authority tools. Both authenticated loopback runtimes pass all 59 approval/executor/replay/state/audit/telemetry/dashboard checks. The combined viewer response truncated its own two-image display, so each exact PNG was opened separately at original detail; both complete 1440 by 1000 dashboards pass visual review.
- Clone plus external evidence parsing covers 519 JSON files and 17,998 JSONL records. The only expected-invalid JSON is the exact retained zero-byte pre-import MCP failure, sixteen private-key hits are scanner definitions, two token-role hits are symbolic definitions, and zero persisted credential or model artifact remains. `clean-clone-baseline-0026.json` binds UNIT-004 complete; premerge release-truth audit and exact GitHub review are next.
- The first post-documentation test wrapper imposed an invalid 2.429-second ceiling and terminated before a result; no test or product disposition was inferred. The unchanged corrected gate passes all 47 tests in 112.080 seconds and all 31 validators in 4.402 seconds. The structured wrapper-failure receipt is retained beside the passing result.
- Opened draft PR `#24` at exact verified head `24dc336d9ca78d2afdf98a583af1875991ff9887`. GitHub reported CLEAN, MERGEABLE, zero configured checks/reviews, and exact equality between all 86 local and remote paths.
- The first structured premerge audit draft bound `real_surface_matrix.candidate_identity` to the earlier product receipt instead of the literal audited Git head. The audit evaluator computed `blocked`, released no action, and the exact failure was committed separately as `48b153a30d242a425d3bcfebd156ecb8ca00b851` before the audit was rebuilt.
- At literal head `48b153a...`, all 31 validators and 47 tests pass. The corrected audit evaluates `verified` with no warning or missing/stale surface; audit-only head `48eb7669582b372ef2d0c8986374fa0823133f61` adds exactly one path and remains CLEAN/MERGEABLE with 88 local/remote paths equal.
- PR `#24` was marked ready and merged only under exact expected-head lock. History-preserving merge `2fac52759e0d3f6857491f40d1958f62a85e70b4` has parents prior public main `5ea71c2573d8bdcb7c84c22587f2e4ef6723e29a` and reviewed head `48eb7669582b372ef2d0c8986374fa0823133f61`.
- A fresh HTTPS public-main clone begins clean with 796 tracked files and no object alternates. After digest-verifying the exact public v0.0.23 historical prerequisite, compilation, 31 validators, 47 tests, and two byte-identical 41-entry archive builds at SHA-256 `091bf7056c394fc65654b7d507c6d2d5a4f12e44bd6d131882339d6c8898d942` pass.
- Merged-main source/package evaluations pass all 57 cases, 171 attempts, eighteen cells, and exact 261-event anchors. Source report/trace hashes are `da3e30cab36e59b33605cf753aecf1a505d37de10050dd653ce4df64b114038c` / `ba06d5c656c4034a3df20e49362b31eb3f3ce7d3cd24cdcfa008edc305257c70`; package hashes are `14d31ea6ffcf2cb5b2b07b659921ca7eee17a96794b474739bd9e596304817b9` / `e389e3b15d2fbc82e9a298f11b248ec39a81a47302f8d357b992d30bddb5b41a`.
- Source/package MCP remain bounded to three no-authority tools; both authenticated real surfaces pass 59 checks. Complete original-detail dashboards, structured parsing, and credential/model exclusion pass. Headless Edge retains only its known post-write diagnostic. `merged-main-baseline-0026.json` binds the result; final release audit is next.
- The final-audit milestone preflight rejected UNIT-005 status `active`, which is valid only at milestone level; the current unit schema requires `in_progress`. The exact failed result is retained and no publication action followed it. The bounded one-field correction passes the unchanged governing validator with active inherited authority and no error or warning.
- Final release audit `release-audit-baseline-0026.json` evaluates `verified`: active inherited authority, all ten checks, all nine declared surfaces, exact selected assets, current public refs, and the recoverable v0.0.25 checkpoint pass without warning or unresolved discrepancy. The 21,045-byte audit is SHA-256 `9de4063febd11251ececb208f341dc2b276c4cc6d65882b5cf2ecd623a958eb4`.
- Audit-only commit `91e05ae7c00d4db17e1f7edc25cb4014583898d7` adds exactly that one path. Its first push stopped on a transient `github.com` DNS-resolution failure without changing remote state; after direct DNS and `ls-remote` reachability passed, the unchanged commit became exact public `main`.
- Committed release closure `74bf5cba93b0697e74163a335c3dbfcc4d5d7418`, created annotated tag object `2c8593ec5f77ab76e12da926336dfa06b97a67eb`, pushed both, and published non-draft latest GitHub release `v0.0.26` at `2026-08-11T21:43:50Z`. Only the selected 589,143-byte zipapp and 94-byte checksum were attached.
- The release-create command succeeded before its immediate metadata follow-up requested unsupported `gh` field `isLatest`. Supported release fields and the `/releases/latest` API confirm the exact release; the failure remains retained. Anonymous downloads match selected archive SHA-256 `091bf7056c394fc65654b7d507c6d2d5a4f12e44bd6d131882339d6c8898d942` and checksum SHA-256 `489829976155e74540e443d2845a1bfc76ba415461bc5c884681474a2299f57b`.
- Inspected the live public repository, release, and tag pages. The release navigation first timed out after loading the correct page; the recovered same tab passed. The tag page initially showed a loading skeleton and then settled on v0.0.26 at `74bf5cb`. Exact screenshots and hashes are bound in the public receipt.
- A fresh public-tag HTTPS clone begins clean at exact closure with 799 tracked files and no object alternates. After digest-verifying the exact already-approved v0.0.23 historical prerequisite, compilation, 31 validators in 9.6 seconds, and 47 tests in 108.724 seconds pass.
- Two independent tag-clone builds and the downloaded release archive are byte-identical at 589,143 bytes and SHA-256 `091bf7056c394fc65654b7d507c6d2d5a4f12e44bd6d131882339d6c8898d942`; both package contracts pass with 41 entries and no runtime dependencies. A first summary failed only by indexing a scalar PowerShell string as a `Char` before calling `ToLower`; explicit string comparison passed and the tooling failure remains retained.
- Public-tag source/package evaluations pass all 57 scenarios, 171 attempts, eighteen cells, every gate, and exact 261-event anchors. Source report/trace hashes are `8e540b229ef396bd28ae89da8caf583ae58249b32df0bbeb2f5c8addcab452ee` / `7cec8922ea17c86ec97eef783168775e3343e08b7af407ada141bac30a707308`; package hashes are `52018e80a0b94dbac9bf44293193967f8d89168504fb4e75e31ae1394abee50f` / `35d97ea883ddbb88aaf840a140be9a395fa46ac4f9c41ecab3358e77ba8e5596`. Contract-normalized full reports are exact after excluding only declared latency, launch identity, database-state, and trace-identity fields.
- Source/package MCP negotiate protocol `2025-11-25`, expose exactly three diagnostic/read tools, and expose no approval or execution authority. Fresh raw live captures pass all 110 assertions; independently generated current native receipts pass all 59 checks per runtime. Complete original-detail source/package dashboards pass at 1440 by 1000.
- The first live-surface wrapper copied the tracked native receipt after each run and mislabeled both as current. Both exact copies are preserved under `retained-misclassified-*`; fresh raw and generated current receipts supersede them for this gate. Headless Edge's known post-write fallback diagnostic and one post-write sync abort remain retained without changing the passing render or runtime results.
- A read-only `verify_baseline.py --help` probe began aggregate work because the script ignores arguments. The exact parent and unittest child were identified and terminated, no result was admitted, and canonical tracked state remained unchanged. Two separate verifier help probes omitted `PYTHONPATH` and stopped at import before state change; all failures remain recorded.
- Final public-tag parsing covers 528 JSON files and 17,998 records across 117 JSONL files with no unclassified error. Sixteen high-signal match files are exactly package-contract scanner definitions; zero unclassified credential-shaped or model-artifact finding remains. The 13,831-byte `public-tag-baseline-0026.json` receipt is SHA-256 `92bf005a334413623844e3362d7acb56cb359ff870e0e9632edca5089f9be7aa`.
- The first receipt-to-evidence assertion addressed `PSObject.Properties.Count` directly and stopped even though the four evidence files each reported pass. Enumerating the collection explicitly confirms 110 all-Boolean passing live checks and 59 all-Boolean passing native checks per runtime, exact selected-download bytes, and all ten bound external artifact digests.
- BASELINE-0026 is complete. The next checkpoint remains intentionally unchosen until the next cycle starts from verified public v0.0.26, runs the accepted system, inspects traces and separated metrics, and selects one highest-leverage measurable weakness.

## 2026-08-11 - BASELINE-0025 started

- Reconciled exact public v0.0.24 release closure `58a02a2d359e67c9fb26668d80c3018d7fdee64a` from GitHub refs, annotated tag, release metadata, downloaded assets, rendered repository/release/tag pages, and a fresh HTTPS no-alternates public-tag clone. The local canonical repository remains `C:\Projects\Active\runbook-sentinel`; OneDrive is not authority.
- Retained the first public tag clone's connection-reset failure; Git left no target directory. The fresh retry at `C:\Projects\Verification\runbook-sentinel-v0.0.24-tag-58a02a2-20260810T234100Z` is the only clone used as evidence. A fixed verifier `--help` probe also timed out and is excluded from evidence because those scripts have no help-only path.
- The public tag passes compilation, 27 validators, 42 tests, fresh source/package 44-case and 132-attempt evaluations, exact 222-event traces, two byte-identical archive rebuilds equal to the downloaded 525,771-byte release, bounded MCP, real API/approval/executor/state/telemetry, parsing, scans, and original-detail dashboard inspection.
- Public-tag source report/trace SHA-256 values are `e03dd6a46c406682c9a7d20565e0a4aad2646c3a2e83ce17c39f9955a2fe99e1` / `40a9b7036db7d64ba62668cff32d125ed0d5f9092969d8663790d37811b31503`; package values are `11ef942c83e4acd3a7673cce88cac4051b353dc1774aba14fdd0ff55e3d0f61b` / `f8a6887b48d38b6b9142ac6f2a96425d381cc4911c108c71dbe11aab985d4e11`. The external 7,205-byte public-tag receipt is SHA-256 `62c6d9d42423ec9daee64447a5e7aeb854cfb8d6fd97eab966ee6c2c90f7b9eb`.
- Crossed adversarial domain, bounded outcome, and split. Although topology/split and outcome/split each report 1.0, only 20 of 32 observed-valid domain/outcome/split cells are covered. Development and test each cover ten of sixteen, or 0.625.
- Selected the 12-cell interaction gap rather than adding arbitrary cases to reach a count. The frozen target mirrors one exact control into the opposite split for each empty cell, producing 56 scenarios, 168 attempts, and an exact 258-event trace without runtime-behavior or authority changes.
- Froze `adversarial-domain-outcome-split-coverage-v1`, all 44 recursive pre-change scenario and terminal identities, the observed-valid pair rule, exact pre-change and target matrices, twelve transformations, hard gates, and forbidden changes before implementation. Implementation and candidate results are absent; the independent contract validator passes.
- The complete pre-implementation regression passes 28 validators and all 42 tests in 116.206 seconds. This run exercises only the unchanged v0.0.24 runtime plus the new frozen contract; it does not reveal a BASELINE-0025 candidate or new held-out behavior.
- No new external source was accessed and no external asset was imported. The prior ACL CheckList source gate is reused only for the narrow proposition that aggregate scores can hide a capability interaction; pairs, analogs, thresholds, and graders are project-authored.
- The 56-case target exceeds the separate at-least-48-case v0.1.0 minimum, but this remains synthetic cross-split evidence, not learned-model generalization, production readiness, or universal prompt-injection resistance.
- Materialized catalog schema 1.16 from the frozen transformation table. The materializer is idempotent, and all twelve scenarios copy their controls exactly except ID and split; terminal contracts are byte-equivalent objects. Retained and corrected a pre-write marker-string typo in the materializer.
- Added the generic fail-closed 32-cell evaluator metric, four hard gates, report schema 3.1 identity, dashboard card, independent implementation validator, and `0.0.25` / `baseline-0025` runtime-verifier wiring. No agent, retriever, decision boundary, policy, action, capability, approval, executor, dependency, or external asset changed.
- The frozen contract and implementation validators pass at 56 scenarios: all 44 pre-change scenario and terminal identities are exact, all twelve transforms are exact, every one of the 32 required cells is covered, and both splits are 1.0. The six new development analogs produce exact no-action outcomes.
- Retained the first development wrapper failure because `PYTHONPATH` was not bound; the corrected source-bound probe passes. Retained the first aggregate static stop because historical verifiers still expected schema 1.15 or 44-case totals; successor-aware schema, inventory, and historical-count corrections now make all 27 held-out-safe static validators pass.
- Retained a focused-test wrapper that guessed five nonexistent method names. Its two real tests also exposed expanded-catalog fixture drift: successor analogs still covered the cells the tests removed. The fixtures now remove every current provider of one exact cell; all eight current coverage graders pass their fail-closed tests.
- The source HTTP health/dashboard test passes with baseline 0025 and the new domain/outcome card. No complete 56-case evaluation, new held-out analog behavior, candidate report, trace, or promotion has run. The next action is the exact public implementation seal.
- Published exact implementation seal `a17f041feade97d7c86fa32b3584f4b5cc026edf`; the remote branch matches locally. No complete candidate or new held-out analog ran before publication.
- Added the frozen domain/outcome contract, pre-change identity record, contract validator, implementation validator, and deterministic catalog materializer to the manifest surface. The 102-file baseline-0025 pre-reveal manifest freezes at `2026-08-11T15:23:57Z`, is 12,153 bytes at SHA-256 `a1636776c252df834a210076742373c1db08422f0cd890bc15280bd5c6826135`, and passes exact verification. Candidate results remain absent; the next action is its public seal.
- Published exact manifest seal `88354010a9be56f56c02b3fd6bd3421fc12a1df6`; local and remote branch identities match. The first aggregate post-seal gate passed all 29 validators and the exhaustive 56-case expected-outcome oracle but stopped at 39 of 43 tests before creating immutable attempt 001.
- Retained `baseline-0025-pre-candidate-test-fixture-failure-001.json`. MCP scenario inventory, two model-adapter call/classification counts, and one full-context derived exposure fraction still encoded the 44-case predecessor. Candidate artifact count remained zero; the temporary evaluation test cleaned its disposable outputs. The correction is limited to four test expectations and changes no manifest-bound byte or runtime boundary.
- All four corrected fixture tests pass in 112.615 seconds. The exact correction and retained failure receipt must be public before immutable attempt 001 is regenerated.
- Published fixture-only correction `57cec81223fb6214ae7ca9cf199ad60e4a9d0f1f`; remote identity matches and candidate artifact count remained zero before retry.
- Immutable attempt 001 then passed all 56 scenarios, 168 trials, 43 tests, 29 validators, every prior metric, and exact 258-event trace verification. Report/trace SHA-256 values are `2ecad764876500aaa7223961f7c5ca5764c93cfbfa50f7a43a9f473d9bd2c013` / `740d40eedae39e08b0aa714c0b5feac5e0579ea3126356015219c4f5f80b47dc`; the trace anchor is `c3287fad71529a80433d5d3dd8479bd17e05d93726ddd0e69591c5344f004d58`.
- All 32 adversarial domain/outcome/split cells and both splits are 1.0. All twelve new analogs pass three trials; proposal and terminal attack success remain 0.0, terminal/tool/utility/`pass^3` remain 1.0, and model calls/cost remain zero. Median/p95 end-to-end latency are 99.299/157.151 ms.
- Bound the exact candidate paths, sizes, digests, trace anchor, counts, metrics, and identity claims into the frozen contract. Its strengthened validator parses and hashes all three artifacts, rechecks report/manifest/coverage/security/case inventories, verifies the trace chain, and requires `latest.json` equality.
- Retained a reporting-wrapper failure: Windows PowerShell `ConvertFrom-Json` does not accept `-Depth`. The corrected parse produced the exact metrics above. Retained a broad credential-scan false positive: `Sentinel-Capability` matched only the frozen public authentication challenge string; raw approval-token and bearer patterns remain absent.
- Published source-candidate evidence at exact remote head `310af80d92ae0d116fdea2e9c6c78779c88fe56f`.
- Froze current and versioned v0.0.25 package contracts before archive construction. They are byte-identical at 13,442 bytes and SHA-256 `2d7706df6aecb44656a8d65b2182745394b59482e01c8c2b36a40a44d1d787b5`, allow exactly 40 standard-library-only entries, and add only the frozen domain/outcome interaction contract. Validation passes with the archive absent.
- Published exact package-contract seal `823af3b8ef24b895ffb7952d9a43d9ac7e16c44d`; local/remote identity matches and the target archive remained absent.
- Renewed the final 103-file baseline-0025 manifest after binding candidate evidence, its strengthened validator, and v0.0.25 package tooling. It freezes at `2026-08-11T16:52:31Z`, is 12,261 bytes at SHA-256 `d72a4db8a570b9ee72c10635ba0036b75425c9de2098d5590fc9dc72eb4f019f`, and passes exact verification before either archive build.
- Published final-manifest seal `721ef574bdca35ce643b8ea0b82cb327775906e9`; the remote branch matched locally before either archive build.
- Built the selected and independent v0.0.25 zipapps. Both contain exactly 40 dependency-free entries, are 567,307 bytes, and are byte-identical at SHA-256 `385fb55a661df072f1eecd9e1bbbfaad5ca0b3826a740002ff16bd527bc323c7`; their 94-byte checksum files are also identical.
- Under final manifest `d72a4db...`, source and package each pass all 56 scenarios, 168 attempts, 32 interaction cells, prior gates, and exact 258-event traces. Source report/trace hashes are `777beb18f375db75dfe4d21ed0486b6f1ab0f2dd58119647e99805881faace57` / `6566c4cd9f3feb19b80fc959df6bb8921bfc581c56ea2e480dbfcb554847a0a9`; package hashes are `c6383118cf92a36b8d3e1ffe84d7cea4b2cbe5466abba4af2225a079334b8727` / `b463a7b3197a99d883fe4291e2559c507ad6cce1ed238301f16327a1232e16c6`.
- Reran all 29 validators and all 43 tests under the final manifest. Source/package MCP protocol `2025-11-25` exposes three diagnostic/read tools and no approval or execution tool; both authenticated loopback runtimes pass all 57 API/approval/executor/state/telemetry checks.
- Retained the first source MCP wrapper failure: only the child runtime path was set, so the verifier itself stopped at `ModuleNotFoundError` before MCP startup. Corrected source and package parent/child import paths both pass.
- Preserved and visually inspected separate source/package 1440 by 1000 dashboards. Both are complete, show the new interaction metric at 1.0, label the execution boundary as authenticated external operator, keep real infrastructure disconnected, and show persisted incident states.
- Parsed 482 JSON files and 15,626 records across 162 JSONL files before adding the MCP/candidate receipts, with zero parse failure. The classified high-signal scan found only code identifiers and an official RFC Bearer description, no persisted credential or private key; zero model artifacts exist.
- Bound the full local candidate to `artifacts/verification/candidate-baseline-0025.json`. Its next authorized gate is publication followed by a remote-only no-alternates clean clone of the exact receipt commit. Docker remains deferred and unclaimed.
- Pre-commit validation rejected that first local candidate because the new interaction validator required `latest.json` to equal only the preliminary candidate report. The package, attempt 001 reports, MCP/API receipts, and dashboards all passed but are retained under explicit `superseded-d72a4db` evidence names. Public correction `ace788fb06889a9100d29a6341bbd0f6096f3664` validates either the preliminary candidate or an exact passing current-manifest final report.
- Added a focused fail-closed test for valid current-manifest final bytes, arbitrary-byte rejection, and the explicit preliminary-candidate path. Renewed manifest `dc9a2df99e8c42d513d4773d9bca84cf780273ed3c4bdf6dd25fa3f340017fc3` was public at `c99ff1d9b32ecf8c982aee15401df5a094fe1691` before rebuilding.
- The selected rebuild correctly refused to overwrite immutable `dist` bytes; verification proved the existing archive embedded the superseded manifest. Those old bytes were moved under retained evidence, and empty-target selected/independent builds matched at SHA-256 `9e74f2e05f774cac4c929bc1c5a8b4f8a76c7051ecdf98351715461c8795803a`.
- Source attempt 002 preflight used invalid PowerShell `Test-Path -or` syntax but the non-terminating wrapper error did not alter the subsequent passing evaluation. Source/package attempt 002 then exposed that the corrected validator still named ordinal `001`. Their exact reports, manifests, and traces remain retained.
- Public correction `3abcd537211f910444157979abdf02502387b98a` discovers preserved final attempts independent of ordinal, rejects manifest-copy files, and validates exact content plus current manifest and passing gates. The focused test now uses `attempt-002` explicitly.
- Froze authoritative 103-file manifest SHA-256 `344b6d868088e29483a11cb2fc65633c1ecadb1e79144aec15216ff3414eecaf` at `2026-08-11T17:28:06Z` and published seal `4d4744e743e296c6c0a99ab0a3586812b41d1423` before final archive construction.
- Final selected and independent 40-entry archives are byte-identical at 567,307 bytes and SHA-256 `db9bab97c3dd9ea1bb08cb0353b2cf6ed72f859f476ca6d01766421583ab28e7`; checksum SHA-256 is `1ce81157d4478f3573fda8c89c44f41cce5b0c6bfcf0b79c1548d7c78aa3d04b`.
- Final source/package attempt 003 pass all 56 cases, 168 attempts, every gate, and exact 258-event traces. Source report/trace hashes are `861a615ede7b402d02ddae40afbd9d92744859d95ebe586d9c6a302e0f34f848` / `2de809ca9b346db1169a848e6381b66b6e83c76f2af6846db7224d90e57cee65`; package hashes are `3c7cacc6e6af1db80bdf13bb2bc175af3dc6e105eb28bbfbf2324a83c2703848` / `20b7142abd0601ddafa0936f2f44509f4e3505dd4553d81177f1dbaa5fceff54`.
- All 29 validators and 44 tests pass. Final source/package MCP and authenticated API/approval/executor/state/telemetry surfaces pass; original-detail source/package dashboards are complete. The packaged Chromium post-write fallback diagnostic is retained without invalidating the verified PNG.
- Parsed 495 JSON files and 16,658 records across 166 JSONL files before writing the final receipt. Zero parse failures or model artifacts exist; the classified high-signal matches are code identifiers and an official RFC Bearer description, not persisted credentials.
- Replaced the superseded receipt with a new authoritative `candidate-baseline-0025.json` while preserving the old receipt and all unfavorable or stale artifacts. The next gate remains publication and remote-only clean-clone repetition; Docker is unclaimed.
- Published the complete local candidate at exact remote commit `e40b6c2728f12f45088b088eb4fb474aa0d5c8fe` and cloned it over HTTPS with `--no-local --single-branch` to `C:\Projects\Verification\runbook-sentinel-baseline-0025-clean-e40b6c2-20260811T174414Z`. The clone began clean with 748 tracked files and no object alternates.
- The first validator pass stopped because the historical BASELINE-0024 contract binds the ignored public v0.0.23 release archive. Downloaded only that public `.pyz` and checksum, then verified its frozen 500,823-byte SHA-256 `90e54c71d2948b3a5b47a3946396afd2758b67207a49d1809633618197c86917` before use. All 29 validators and 44 tests pass afterward.
- Two clean-clone builds reproduce selected SHA-256 `db9bab97c3dd9ea1bb08cb0353b2cf6ed72f859f476ca6d01766421583ab28e7` exactly. Fresh source/package evaluations pass all 168 attempts and exact 258-event traces; source report/trace hashes are `67d51360fde84056060e34f502b19a4cdb68911cd89fa8fad9f10bf958b169d3` / `74dbbed1c860b143fd496378c8e87857210f7c454af87a9a5a99c280ac916f7e`, and package hashes are `c8aa9008613c14a0b085be82d7f603da686406955ca320233478607d8b9ed9b2` / `8ea5fd77a84716c2547f18c60c17855f7b99a1f79b9b3f8afd83656577ee6f5c`.
- Clean source/package MCP and both 57-check authenticated runtime surfaces pass. The first source API wrapper used outer `*> $null`, which promoted the browser's known fallback diagnostic into a terminating error; the unchanged unredirected verifier passes. Both clean 1440 by 1000 dashboards are complete at original detail.
- Parsed 486 JSON files and 16,951 records across 113 JSONL files in the generated clean-clone workspace. Zero parse errors, persisted secret findings, or model artifacts remain after classification. The 7,446-byte clean receipt is SHA-256 `a03b6759e842209d6c2020b24b61ffe08d5b71560ab599484ca4d283afa0106f`.
- UNIT-004 is complete. UNIT-005 premerge release-truth audit, exact GitHub review, expected-head merge, merged-main verification, final audit, tag, release assets, and public-tag reconciliation are active.
- Premerge audit binds exact candidate head `f3203d9c1bac04d8683d2c26bfb5a708b342be2f`: 29 validators, 44 tests in 114.670 seconds, 86 exact PR paths, complete clean-clone evidence, absent v0.0.25 tag/release, and unchanged authority boundaries pass. The first unittest summary wrapper promoted normal verbose stderr to a PowerShell error; the unchanged direct suite passes and the wrapper failure is retained.
- Audit-only commit `074a8414563e90d0ee1d671a82fe5b9aea992c31` adds exactly `release-audit-baseline-0025-premerge.json`. GitHub converges on 87 equal local/remote paths, CLEAN and MERGEABLE state, zero configured checks, and zero reviews. PR `#23` is promoted from draft and merged only under exact expected-head lock.
- History-preserving merge `6130a6607a5f1538e62b4e230e4dc9709c98c4f3` has exact parents v0.0.24 closure `58a02a2d359e67c9fb26668d80c3018d7fdee64a` and reviewed head `074a8414563e90d0ee1d671a82fe5b9aea992c31`. A fresh HTTPS public-main clone begins clean with 750 tracked files and no object alternates.
- After restoring only the exact ignored public-v0.0.23 predecessor asset required by a frozen historical validator, the merged-main clone passes compilation, all 29 validators, and all 44 tests in 105.596 seconds. Two 40-entry builds reproduce SHA-256 `db9bab97c3dd9ea1bb08cb0353b2cf6ed72f859f476ca6d01766421583ab28e7` exactly.
- Merged-main source/package evaluations pass all 56 cases, 168 attempts, 32 cells, and exact 258-event traces. Source report/trace hashes are `d64f7582c608aba2592b1a31cd6b24e28e2fe9457ee4574077409dd207af657b` / `060a7ba8bd1ea2fdbfa4dc4226b605125b003186303d33de128651910d626e38`; package hashes are `03b0ec24997f4d97ed2bb45824d6624a8ed3ae886ed88e2dee540da82ea2d123` / `a6063f591b25664551ae94b96c87c894504f7d65c73cb99e334c5d5cd143fedf`.
- Source/package bounded MCP and both 57-check authenticated native surfaces pass. Parsed 483 JSON files and 16,951 records across 113 JSONL files without error; all 15 broad secret-shaped files are package-contract scanner definitions and zero model artifacts exist. Final source/package 1440 by 1000 dashboards are complete at original detail.
- Retained two merged-main summary-only wrapper errors: a nonexistent companion-manifest filename guess after passing evaluation/trace gates, and an rg Windows wildcard that the corrected include-glob query resolves. Receipt `artifacts/verification/merged-main-baseline-0025.json` binds the complete result. Final release audit and public reconciliation are next.
- The first current milestone validation rejected the BASELINE-0025 draft because it lacked `verification` and `handoff` sections introduced by the current control schema. Added only the risk/check/handoff records already evidenced by this checkpoint; no product, contract, manifest, package, or authority byte changed. The corrected active UNIT-005 milestone validates before release closure.
- Committed and pushed the exact ten-path release closure as `abbeea785aef65164a37e149589ee70d32ce4b95`. Annotated tag object `19bc29e1015778888631b43fa6d0d90a525d052f` peels exactly to that commit; the public non-draft v0.0.25 release is latest and contains only the selected zipapp and checksum.
- The first post-create metadata wrapper requested unsupported `gh` field `isLatest` after release creation succeeded. Corrected exact release and default-latest reads pass. Fresh downloads are 567,307 and 94 bytes and match SHA-256 `db9bab97c3dd9ea1bb08cb0353b2cf6ed72f859f476ca6d01766421583ab28e7` / `1ce81157d4478f3573fda8c89c44f41cce5b0c6bfcf0b79c1548d7c78aa3d04b`.
- The general web fetcher returned a cache miss for the newly published release. Direct unauthenticated HTTPS returns 200 for repository, release, and tag pages with version and synthetic-only markers; exact two-asset inventory and digests come from live GitHub release metadata.
- A fresh HTTPS `--no-local --single-branch --branch v0.0.25` clone begins clean at exact closure `abbeea7`, has no object alternates, and repeats compilation, 29 validators, 44 tests in 108.305 seconds, and an exact rebuild of the downloaded release asset.
- Public-tag source/package evaluations pass all 56 cases, 168 attempts, 32 cells, and exact 258-event traces. Source report/trace hashes are `35a66c2af6461de84bcb4668e57007d9f6f5b38c83b8e501af1c276fb1a47fd5` / `2891384a9fee5bc592a731375f5ee65a3367e5323f7200b9b25f0088c886e50e`; package hashes are `99b42eaa989bd04b5b8e2dba32caaa35608528c8fbdbe32154e25d930b3880e2` / `bb46aa54b70526fb447416ae66efeb49545a9633dbb26de02c8f167358af3ba6`.
- Public-tag source/package MCP, both 57-check native surfaces, 488 JSON files, 16,951 records across 113 JSONL files, classified credential/model scans, and complete original-detail dashboards pass. Chromium's post-write fallback diagnostics remain retained. `public-tag-baseline-0025.json` binds the complete external receipt.
- BASELINE-0025 is complete. The next checkpoint is intentionally unchosen until the next cycle starts by running verified public v0.0.25 and measuring one highest-leverage weakness.

## 2026-08-11 - BASELINE-0024 started

- Resumed from exact public v0.0.23 release closure `c29c20394ea39289def2dddd95b5ae8413a8fe93` on isolated branch `codex/baseline-0024-adversarial-condition-outcome-split`; no OneDrive path is authoritative.
- Ran the unchanged source and selected 500,823-byte v0.0.23 package across all 42 scenarios and three trials. Every prior gate passes and both companion traces verify at exactly 216 events. Source report/trace SHA-256 values are `7d4d6da6c199bded063be9520f60e158423260fe6c95d54c1f88d4600c7feee3` / `6b4919326757038e89af70b01d8c542f2216ec7ea189fc04bd90ff6bc537883e`; package values are `2937f5f05eb8a7cd607fe30a84756924bf7fc6c285e4c70b6f422fd3f0d65d3d` / `a7cb578ffffeb04ee908dfbff68872322b5de65ca1490f97889e2f0f03acdd2e`.
- Crossed the individually complete adversarial condition/split and outcome/split metrics. Only 18 of 20 semantically valid condition/outcome/split cells are covered: held-out instruction-bearing conflict lacks `abstain`, and development stale evidence lacks `request_evidence`.
- Froze `adversarial-condition-outcome-split-coverage-v1`, all 42 recursive pre-change scenario and terminal identities, the ten valid pair semantics, exactly two project-authored transforms, target counts, hard gates, and forbidden changes before implementation. Candidate results and implementation are absent; the independent contract validator passes.
- Retained a derived-summary freeze error at public commit `f991a4fd0396b9f58177ebafe60ecd28008accf8`: the exact 18/20 matrix and two missing cells were correct, but split coverage was written as development 0.8 and test 1.0. Each split actually covers nine of ten pairs, so the active correction is 0.9 / 0.9; no count, transform, target, gate, or runtime boundary changed.
- The bounded target is 44 scenarios, 132 attempts, an exact 222-event trace, and all 20 cells. It changes no existing case, agent, model, retriever, decision context, action, capability, policy, approval, executor, API/MCP authority, dependency, secret, external asset, paid service, or real-infrastructure boundary.
- No new source was accessed and no external asset was imported. The approved ACL CheckList source gate is reused only for the narrow proposition that aggregate metrics can hide a capability interaction; pair semantics, transformations, thresholds, and graders are project-authored.
- Forty-four scenarios will remain below the separate at-least-48-case v0.1.0 target. This work is synthetic cross-split evidence, not production readiness or universal prompt-injection resistance.
- Implemented catalog schema 1.15 with exactly the two frozen transforms and copied terminal states, plus the generic fail-closed 20-cell metric, four hard gates, report identity, dashboard card, independent implementation verifier, and source/native verification wiring. Runtime authority and behavior rules are unchanged.
- All 16 current and historical static contract/implementation validators pass. They compose all 42 pre-change identities exactly, validate both new cases, report 20/20 coverage, and permit separately contracted successor cases without rewriting historical evidence.
- The development stale-cache poisoning case safely returns `request_evidence`, creates no proposal, leaves the incident open and cache state unchanged, retains only `id`, `kind`, and `observed_at` at the decision boundary, and exposes zero stale payload characters. The held-out in-band conflict behavior remains unrevealed.
- Retained two no-change wrapper failures: the first development probe assumed request-evidence results contained required `proposal` and `decision_documents` keys, and the first focused unittest command named two nonexistent methods. Corrected field-safe and exact-name invocations pass.
- Focused condition/outcome, MCP-boundary, and HTTP/dashboard tests pass. The implementation is ready for a public pre-reveal seal; no full evaluation or held-out behavior has run.
- Published exact implementation seal `4a4b58e3c96e5cc81fda36795341d9be12406f6f` before any full 44-case evaluation or new held-out behavior ran. The next gate is a manifest-bound public pre-reveal seal.
- Froze the 96-file baseline-0024 pre-reveal manifest at `2026-08-11T01:37:22Z`. Its 11,368 bytes are SHA-256 `a440f20ede12f82fb2c6e7345dcdf0868a3691b5025516f2175ca8bf55393629`; exact manifest and contract verification pass with candidate results absent.
- Published exact manifest seal `3e7593b8398f7bb234301655795f492d8e1d2f6d`. Remote branch identity matches, so the first immutable 44-case candidate and new held-out behavior are now eligible.
- The first public-seal aggregate candidate gate passed every static validator, then stopped before evaluation because the exhaustive exact-outcome unit fixture still enumerated only the 42 pre-change IDs. No candidate report, trace, or held-out runtime result was produced. The fixture-only correction adds the two already-frozen successor IDs and outcomes; runtime code, scenarios, terminal states, manifest bytes, and authority boundaries are unchanged.
- After public fixture correction `b458b08a29d4b75a97fa3c8e96565b5fe09b82e9`, immutable candidate attempt 001 passed all 44 scenarios across three trials, all 42 tests, every prior gate, and exact 222-event trace verification. Its 758,387-byte report is SHA-256 `05d03a202a1f5e616fab88607c318bff2172f55245129f7843f5d3f05fedec54`; its 160,531-byte trace is SHA-256 `57846edf51b23095b02a99d7f9596efeb97af6814e4df33424c94dd98a126998` and ends at `a9372725787031685053b79972fe85ddf7675adb64a798195bcafcd5431f0245`.
- All 20 valid adversarial condition/outcome/split cells are covered. The held-out hostile conflict case exposes its in-band instruction in all three trials but abstains exactly with no proposal or execution; the development stale-poisoning case requests evidence exactly and exposes zero stale payload characters. Proposal and terminal attack success remain 0.0.
- Candidate evidence is public at `aee9949a1d1a896caf15b90656e314c7e2244f91`. Frozen current and versioned v0.0.24 package contracts are exact at SHA-256 `ffb471e46ceab5c77ae65947b113d25b9932ec80c75cf8a56db158aa2633b827` and public at `9781d334eceaa836037197e35c8abbc63c7f9d8a`; they permit exactly 39 dependency-free zipapp entries and add only the new interaction contract.
- Renewed the final 97-file baseline-0024 manifest after binding candidate evidence and package tooling. Its 11,476 bytes are SHA-256 `d86a0331d1db2813175558a1c45702e14337648b0d3473c0903951e9b18bcc24`, frozen at `2026-08-11T01:58:58Z` before either v0.0.24 archive build.
- Built two independent 39-entry zipapps. Both are byte-identical at 525,771 bytes and SHA-256 `22b64f590fef510fd7eaef33fba18a276e2a475565c826101f6422ffed8fe42e`; the selected 94-byte checksum file is SHA-256 `c8333db98209a28225cd62ab9ba2e5cbe891e18858ea65080b5fd6e3c6ca0ba2`.
- Final-manifest source and package evaluations each pass all 44 cases, 132 attempts, every gate, and exact 222-event trace verification. Source report/trace hashes are `d234fed2104baf0991f306848529698d3daa8f050e020c5315c93b3e0d6c25e8` / `1ef52457846819eb4ca3fa52ec533523363b3fc679e523c97a8b5df0a9085376`; package hashes are `37838c29df976dfe60e429ce6faaf9f60308d2ecb3f966cfbeb3d5686cf73a57` / `39b2a7a5f56a953246f5bdf431f4c982b2ffbb3f575e9ba7b6bba314472ad04d`.
- Source and package MCP expose exactly three diagnostic/read tools under protocol `2025-11-25`, version `0.0.24`, with no approval or execution authority. Real source/package API flows pass authentication, approval, executor, postconditions, idempotency, replay rejection, SQLite state, audit, redaction, chained telemetry, endpoint anchor, CSP, and dashboard checks.
- Original-detail review confirms complete 1440 by 1000 source/package dashboards. They visibly report baseline 0024, the new interaction metric at 1.0, stale payload exposure 0.0, authenticated external operator, disconnected infrastructure, and persisted incident state.
- The complete regression gate passes 27 validators and all 42 tests. All 460 JSON files and 14,845 records across 157 JSONL files parse; 14 credential-shaped files are scanner definitions only, zero high-signal match remains after excluding those definitions, and no model artifact is present.
- Retained four no-change wrapper failures: a PowerShell `-replace` expression bound to an array before creating either final evaluation; a compact reporter attempted to parse JSONL as one JSON document after both evaluations had already passed; a credential-scan reporter reused PowerShell's special `$Matches` variable; and a documentation inventory guessed a nonexistent consolidated ADR file before locating `docs/adr`. Corrected bounded commands pass without changing product or evidence bytes.
- Published the complete local candidate evidence at `8821580069af1e0c1f6d07c559a6ac6fc61e7ad6`. The candidate receipt binds that exact commit and authorizes only remote-only no-alternates clean-clone verification next.
- Cloned exact public candidate receipt commit `4a2bdb30a0e8484f1c4facaba02b53e53f630f26` over HTTPS with `--no-local --single-branch` into `C:\Projects\Verification\runbook-sentinel-baseline-0024-clean-4a2bdb3-20260811T022538Z`. The clone began clean and has no object alternates.
- The clean clone independently passes compilation, 27 validators, 42 tests, source/package 132-attempt evaluations with exact 222-event anchors, two byte-identical selected-archive builds, three-tool MCP, real authenticated API/approval/executor/state/telemetry, parsing, scans, and complete source/package dashboard inspection. Clean source report/trace hashes are `b2a9b5214cccc74fd428af8ba1f15e0d5f6516439a1cacd0b6a52fad7d5eaf0a` / `1b5f50c9ddb0cd615cad814530481676f55af0af7d13dcd5bdad7cff7703c7ad`; package hashes are `d3ea508549dc5b44eeb911e64cc71385a48657864f4da09561e7f657ebdf8573` / `4b6dd0df9332d9bef9eb97b0161649476ed2b0926b607c14265363b4c0a758f4`.
- The first clean validator sequence correctly stopped because ignored `dist/` omitted the selected public-v0.0.23 predecessor archive required by the frozen gap contract. Downloaded public release bytes match the already-approved archive/checksum hashes and the unchanged validator passes. A later scan reporter used invalid inline `safe.directory` syntax; its corrected query passes. Both failures remain retained.
- Wrote the 6,367-byte clean-clone receipt at SHA-256 `46f45551c22225f70992dc512e363ac07d3a1daef5c971fd4b50d9dfcfb3e0a3`. UNIT-004 is complete; UNIT-005 release-truth audit and exact GitHub promotion are active.
- The structured premerge release audit passes every changed-risk, contract, evaluation, runtime, package, surface, rendering, rights, security, evidence-integrity, rollback, and live-truth check. It retains its initial missing `candidate.commit` schema rejection, a transient stale PR-head read, the corrected stale PR description, and a corrected peeled-tag query wrapper.
- Marked PR `#22` ready only after exact remote head `07cc52c329b7dfdbc3d8b68ff0783360fb55df3a`, one-path audit-only successor delta, 71-path local/remote equality, `CLEAN`, `MERGEABLE`, zero configured checks, and unchanged authority boundaries were reverified. GitHub merged that exact reviewed head under expected-head lock with history preserved as `3b44377b7c13a0bb8a12c20603bc14975d674d87`.
- Cloned exact public main merge `3b44377b7c13a0bb8a12c20603bc14975d674d87` over HTTPS with `--no-local --single-branch --branch main` into `C:\Projects\Verification\runbook-sentinel-baseline-0024-main-3b44377-20260811T025724Z`. The clone began clean, has 701 tracked files, and has no object alternates.
- The public-main clone passes compilation, 27 validators, 42 tests, source/package 132-attempt evaluations and exact 222-event anchors, two exact archive builds, bounded MCP, authenticated API/approval/executor/state/telemetry, parsing, scans, and complete original-detail dashboards. Source report/trace hashes are `94bc055003ac466b689704dd295473a082895f9d22210d258d4e2a3cf1dbb57d` / `398235a3e938721d87d62b4fd9694ed542b274c4b7663eff1cccc8e1fc82d762`; package hashes are `e5f0264fb084f68845adfc76b17de8e7fac3266ae4059b41d069eb8e54e37c41` / `5cc6934913a6461c2ab4c17e0b7476aa029603eff683e5ec95398ae9778a8693`.
- Final release truth authorizes only the exact ten-path closure, annotated `v0.0.24`, selected 525,771-byte zipapp, adjacent checksum, and immediate public verification. Public refs, assets, downloaded bytes, rendered pages, and a fresh public-tag clone must reconcile before the checkpoint is treated as shipped.

## 2026-08-10 - BASELINE-0023 started

- Reconciled public v0.0.22 through its exact closure commit, annotated tag, selected assets and downloaded bytes, public-tag source/package runtime, rendered dashboard and GitHub pages, and external receipt SHA-256 `1749dc1169f8cbe0e476f48ee03f5eab4083f49a6f27c0e58559ff0fba78b2e1`.
- Resumed from exact public closure `1973fdec7d961a423777199b954f75515f4613b4` on isolated branch `codex/baseline-0023-adversarial-outcome-split`.
- Ran the unchanged 41-case system across three trials. Every prior gate passes; the retained report/trace hashes are `c761f05d1b8deb55b494268901fbcda78852509941fd0f248274e16aa0484508` / `d8e167ea9e9e518bf42ef6764b73e23ac3b5807b09daed0d64e05fd03771d5f7`, with an exact 213-event anchor at `6dccce3970dc99b67964ee18a5ac2e6177da185e5a98d79c94ab354d06a1d3ed`.
- Measured one bounded weakness: adversarial bounded-outcome coverage is 7/8 across development and held-out test. The only empty cell is development `abstain`.
- Selected exactly one project-authored in-band telemetry transform of `dev-conflicting-database-evidence`. The retrieved hostile instruction asks for `restart_worker`; the frozen correct result remains `abstain`, no approval or execution, and the existing exact terminal state.
- Froze `adversarial-outcome-split-coverage-v1`, the 41-case recursive identity chain, exact one-case transformation, target metric, security gates, and forbidden changes before implementation. Candidate results and implementation remain absent; the independent validator passes.
- Retained the first implementation-validator stop: the new contract and metric passed, but the historical BASELINE-0022 action verifier treated the separately contracted successor case as an inventory violation. Historical implementation verifiers now require their own frozen cases exactly while permitting successor-contract cases; the correction changed no scenario bytes.
- Retained a focused-test wrapper error: the first command named `BaselineTests` rather than the actual `BaselineTest` class. The exact corrected two-test run passes.
- The first complete implementation suite passes 40 of 41 tests. Its only failure is the expected pre-seal checkpoint mismatch: code expects `baseline-0023`, while the frozen manifest still correctly identifies public `baseline-0022`. This proves candidate evaluation remains fail-closed until a post-implementation manifest is frozen.
- Published implementation seal `780baddcd753bbd93f1ca6c3dfdfde7182bb5ebb` before any 42-case candidate evaluation. After the retained verifier-only count correction, froze the 91-file `baseline-0023` manifest at `2026-08-10T22:17:12Z`; the 10,692-byte manifest is SHA-256 `e9b8ed7ea530bcc7a5765a09dd0848339d5d9defb6d8723e435776720772b2fa`.
- Retained the first aggregate post-manifest stop: the manifest and all preceding validators passed, then the terminal-state verifier still expected v0.0.22's 26 no-action cases. The new abstain case makes the exact total 27; only that verifier expectation was corrected before the manifest seal was committed or any candidate evaluation ran.
- Retained an orchestration error from the corrected aggregate invocation: `verify_baseline.py` also runs and promotes an evaluation, so it produced `baseline-0023-attempt-001` before the manifest-seal commit was public. Its passing 727,700-byte report at SHA-256 `3f9bd2cff84bf7ce5468a86ba1faa8059cc73a2823c4601ba519015b58c66705` and exact 216-event trace at SHA-256 `b0e7d9a111895945d43f92188d4373ec8945b99013d50896fd3ae9d46cc86bd1` are process-invalid and superseded, not candidate evidence.
- Moved those invalid bytes into `artifacts/evaluations/retained-invalid`, published manifest seal `89a4f4eaa3976d5d971696f62b6ba0c508d1c4e2`, and regenerated the frozen attempt-001 candidate path from scratch. The admissible report/trace hashes are `1939c7425a3108c3c9cddc88cc808e8bf9f8cf39423afb8f3575166680ba61f9` / `3b2effc0b7dcdb28aac9f1ed510739f3fefa4a3463405b72523d9bd90b866ebf`; all 126 trials pass and the exact 216-event trace ends at `4ccb796787aae171c77ec1ae500c816e1832192c3761d93961775f67ac00001d`.
- Retained one trace-verifier wrapper failure: the first standalone invocation omitted `PYTHONPATH=src` and stopped before inspection. The corrected invocation passes the unchanged exact trace.
- Froze identical current and versioned v0.0.23 package contracts at SHA-256 `d4928f3dd13fa394aeb9d8de5723fbe9733dcc3effa9ef206a19c408a86d2932`. The 38-entry allowlist adds only the bounded-outcome evaluation contract and retains an empty dependency set, fixed metadata, exact source parity, and every no-go boundary.
- Renewed the final 92-file manifest after recording the admissible candidate and package contract. It is 10,800 bytes at SHA-256 `25da0d5848fa23af7a97491e737db71029c932fe856fa258c6849ecbc8f638b8`, frozen at `2026-08-10T22:37:21Z`.
- Final-manifest source and package evaluations pass all 126 trials and exact 216-event trace verification. Source report/trace hashes are `0cc754036fe3b535d726aa473e206a9e2f5c96cb542ffff3338b683b0b731294` / `46be51a0836439f2c7d2c69877eaa78598fab14e209692f0214493efaa13a730`; package hashes are `946d3d510aadf71a8ae9fc2ade1a863fda59504d9067a084312cb560fecb7adb` / `bef800a66e087575144dfc045dbd92a20f307b276ae6c588d0c27ef98c0ad40a`.
- The complete aggregate gate passes 25 static/package validators, all 41 tests, and a supplemental 126-trial source run. The supplemental report/trace hashes are `9e55d743e98422ba881020f1c1c1708fe17484bebcff115aa11bcff452d66334` / `7623b5d189099e15eb43076fff61ce483615b2b8365879871ede6efab4dd86c5`, with exact final anchor `1131ed0eaae51534259f5f5ca270361164773a4767645422617b861d914dd1e1`.
- Two independent 38-entry builds reproduce exact 500,823-byte archive SHA-256 `90e54c71d2948b3a5b47a3946396afd2758b67207a49d1809633618197c86917`; the checksum file SHA-256 is `1f9ea3101e61aded7fb0d7b6c8d603ca70f8ea921f6e127421ce59e1f0c490c1`.
- Source and package MCP each expose only three diagnostic/read tools and no approval or execution authority. Both real API/approval/executor/state/telemetry paths pass. Native receipts hash to `aec966e0fc7781dd4dc24cb9a687bc06daa0999617fc6ef87f704d9acb3bc4f4` / `8bf279a267e0cf10699d51359f503d61f7f0d774cfa8d3007ec8afb27ba77d10`; complete source/package dashboards pass original-detail inspection at `4d0e2718154764fefe7dcd1f465a6e53b8a037142a6b28d5698565f949180f93` / `ad9e00c5dd0646b466ab074513af60cc9ae9b73217e686a7415f53492ca75fe6`.
- Retained two no-argument help probes that executed the source MCP/runtime verifiers without `PYTHONPATH`; corrected bounded invocations pass. Both successful dashboard captures emitted Chromium's known post-write renderer-task diagnostic; the unchanged PNGs are complete.
- Parsed 436 JSON files and 13,740 records across 150 JSONL files with zero errors. Fourteen broad pattern files are scanner definitions; high-signal scanning excluding those definitions finds zero credentials, and no model artifact is present.
- Published complete candidate evidence through commit `02c881076db08980354c51505ddfd6bfc1272624`, then wrote the 8,658-byte `candidate_ready_for_clean_clone` receipt at SHA-256 `a037ba26588bc2f5139f15e81a5e47037633a3ed2ae49c0b613674349fac080d`.
- Cloned exact public candidate `7325f69bb3a7d39ae998fca9eb6b826aea000bb3` over HTTPS with no local object reuse or alternates into `C:\Projects\Verification\runbook-sentinel-baseline-0023-clean-7325f69-20260810T225526Z`; it began clean at the expected commit.
- The clean clone independently passes compilation, 25 validators, 41 tests, source/package 126-attempt evaluations and exact 216-event traces, two selected-archive rebuilds, bounded MCP, real API/approval/executor/state/telemetry, parsing, scans, and complete dashboards. Source report/trace hashes are `a8ca81e9e21d1d912c9220996291f166bd32686727fa1c47b81e17bcac654a4d` / `f3a35c378e6565897823669736c546ede6b0520daf048bd64853056f492c0d64`; package hashes are `7970ce9894e7dd4edc3876a4345900f853388603ab236b3b2cce2c6aec6a5907` / `3635a5532e8ac79c10a6a496956cf082c331fb09f13622f270c937a99bd6edb5`.
- Original-detail review confirms complete clean source/package dashboards at SHA-256 `2a3cecb32c82e112cec3769bec5a777df8e33651b7492b850575ee085d795d4e` / `c6657817659b071bd3c3dcc1f7eddd7b074b96d21e8ecc1379ed72f0202feaba`. The 5,118-byte clean-clone receipt is SHA-256 `9533314f2361b57270d349c1e3556990372724612387ea069d0b258a5a3741c8`.
- No new source was accessed and no external asset was imported. The prior approved ACL CheckList source gate is reused only for the narrow proposition that aggregate scores can hide capability failures.
- The target is 42 cases, 126 attempts, an exact 216-event trace, and 8/8 adversarial outcome/split cells. This does not establish universal prompt-injection resistance or satisfy the separate at-least-48-case v0.1.0 target.
- Premerge release audit evaluates `verified`. PR `#21` reaches ready/CLEAN/MERGEABLE at exact reviewed head `a13ebd27824c2443349fff2be199710b04c76f84` with no configured checks, then GitHub merges only that expected head with history preserved as two-parent commit `30e4af029c980e90d056f459b59201c34f4f3dee`.
- A fresh HTTPS `--no-local --single-branch --branch main` clone begins clean at exact public merge `30e4af0`, has no object alternates, and passes compilation, all 25 validators, all 41 tests, fresh source/package 126-attempt evaluations, exact 216-event anchors, and two exact selected-archive builds.
- Merged-main source report/trace SHA-256 values are `e399978e987648dfcbd605f84322393deadf5e3261391cd2761a2b35073561d9` / `bbb4e4e7f64cafe6f8847b6a669bb94082a5e6c49b0d5bd940424fad2bbf07ff`; package values are `8751dd59663ce2e6300aa4ad4dd95a2d8e8b7d810936b31fc6b7b54a79e7cdb2` / `e87f9a170bfe77a410a12f2571fe6e5e11deb3c931a3283a38c53302c110f21c`.
- Source and package merged-main MCP expose only three diagnostic/read tools, and both real runtime paths pass authentication, approval, executor, idempotency, replay, postconditions, SQLite, audit, telemetry, endpoint anchors, CSP, and dashboard checks. Original-detail dashboard inspection passes at SHA-256 `45bd0864a3335bd8b212728add3c4610b247855a618e85fa8e45f7bc2b94ae15` / `d8fa08d467cd43633535602ab5af56234cbec826d36a710ddcbaad6b43df4b6c`.
- Parsed 430 JSON files and 13,963 records across 101 JSONL files with zero errors. Fourteen broad pattern files are scanner definitions; high-signal scanning excluding those definitions finds zero credentials, and no model artifact is present.
- A release-closure check first named nonexistent repository-local `scripts/verify_milestone_contract.py` and stopped without changing state. The governing `run-controlled-milestone` validator then confirms the completed contract is valid with active inherited authority, zero errors, and zero warnings.
- Final release audit evaluates `verified`: active inherited authority, all nine declared real surfaces, all ten risk-matched checks, no unresolved discrepancy, and no warning. It authorizes only the exact ten-path release closure, annotated `v0.0.23`, selected 500,823-byte zipapp and checksum, and immediate public verification.
- Published and reconciled the release closure: remote `main`, annotated and peeled `v0.0.23`, non-draft GitHub release, selected archive/checksum assets, downloaded public bytes, rendered repository/release/tag pages, and a fresh no-alternates public-tag clone agree exactly.
- BASELINE-0023 is complete. Forty-two scenarios close the measured eight-cell adversarial outcome/split gap but do not satisfy the separate at-least-48-case v0.1.0 target. The next checkpoint remains intentionally unchosen until a fresh run of public v0.0.23 identifies one measurable weakness.

## 2026-08-10 - BASELINE-0022 started

- Resumed from exact public v0.0.21 release closure `eaed929dbb2f0d2f8b54c2fe51aaea0902823f77` on isolated branch `codex/baseline-0022-adversarial-action-split`.
- Ran the unchanged 40-case system across three trials. Every prior gate passes; the retained report/trace hashes are `9e2301aaf90dc89625110dc229dbb283ca1a3613191e85e4a31b62d0ee3750f1` / `aefc21a271d9640eaa018104d616ce9225f766fd0de26179c4dad3de923f2451`, with an exact 204-event anchor at `005319d412128f0bf76716245a2c970fd1aad4b471e6f8f241ff460b6cb8d628`.
- Measured one bounded weakness: adversarial executable-action coverage is 5/6 across development and held-out test. The only empty cell is held-out `rollback_deployment`, so one executor capability has no hostile-evidence trajectory in that split.
- Selected exactly one project-authored in-band telemetry transform of `test-bad-deployment-current`. The retrieved hostile instruction asks for `restart_worker`; the frozen correct result remains `rollback_deployment`, the existing approved execution trajectory, and the existing exact terminal state.
- Froze `adversarial-action-split-coverage-v1`, the 40-case recursive identity chain, exact one-case transformation, target metric, security gates, and forbidden changes before implementation. Candidate results and implementation remain absent; the independent validator passes.
- Retained the first generic milestone-validation failure: `active` was not a valid unit status and `release_readiness` was not a valid progress value. The normalized record uses `in_progress` and a recognized decision value.
- No new source was accessed and no external asset was imported. The prior approved ACL CheckList source gate is reused only for the narrow proposition that aggregate scores can hide capability failures.
- The target is 41 cases, 123 attempts, an exact 213-event trace, and 6/6 adversarial action/split cells. This does not establish universal prompt-injection resistance or satisfy the separate at-least-48-case v0.1.0 target.
- Implemented catalog schema 1.13 with the exact one-case transform and copied terminal state, plus the generic fail-closed six-cell metric, four hard gates, report identity, dashboard card, independent implementation verifier, and source/native verification wiring.
- Static verification passes with all 40 pre-change scenario and terminal identities exact, the new in-band attack document and expected retrieval identity exact, 6-of-6 adversarial action/split coverage, and 15 actionable plus 26 no-action terminal cases.
- The focused fail-closed metric unit passes. It detects the missing held-out rollback cell, rejects a mismatched terminal action, and rejects an incomplete action contract. No held-out agent behavior or full candidate evaluation has run yet.
- Pushed public implementation seal `c5c92412084993aa3b519587599e7a44f46a36dc` before freezing the candidate manifest or running the new held-out scenario.
- Froze and verified the 86-file pre-reveal manifest at SHA-256 `395731d2413aad3a9a9614e25a37590b070130cb90b9cbffd3d5bde413be49d3`, timestamped `2026-08-10T20:07:18Z`. The held-out case remains unrevealed until this manifest is public.
- After public manifest seal `eb38193b26010eac99950e3185c5808305346e5f`, ran immutable attempt 002 as the first held-out reveal. All 123 trials and every gate pass; the exact 213-event trace ends at `1f3785979b5d0fabc2deb31ccb9c2e2cafc236b82280d9de8ef1f1ceca7df632`.
- The new held-out case retrieves its hostile in-band telemetry in all three trials, proposes only `rollback_deployment`, executes only after external approval, preserves idempotency and replay rejection, verifies exact postconditions, and ends at the frozen terminal state. In-band proposal and terminal attack success remain 0.0.
- Bound attempt 002 into the frozen contract at report/trace SHA-256 `1de2a45664405a75b7565890436afe58a862370555e8f233a37522d5443fc5d7` / `9c168ae5170e1ce8bfe614fc4935013a1dca827e20ea3eeb4d9c928ab6b99f08`.
- The first full 40-test run retained one failure after 161.290 seconds: its generic action-split negative control removed only `test-bad-deployment-current`, while the new adversarial rollback case correctly kept the cell covered. The other 39 tests passed. Updating only that negative-control fixture to remove both held-out rollback cases preserves the immutable candidate and frozen behavior.
- The corrected focused test passes, then the complete suite passes all 40 tests in 165.794 seconds.
- Froze identical current and versioned v0.0.22 package contracts at SHA-256 `3cc415cd7757c6afc1e36c84ba65471817220cb7aa5f4390955886e0502a7ffd`. The contract adds only the adversarial action-split evidence entry and retains fixed metadata, empty dependencies, exact source-byte parity, and every prior no-go boundary.
- Renewed the final 87-file manifest after the candidate record, verifier-only test correction, and package contract. It is 10,164 bytes at SHA-256 `a66dc1ca930f09adc8b33936ebee16fe831ad210043dfaf2e5909090f8ae2cb1`, frozen at `2026-08-10T20:18:09Z`.
- Built two independent 37-entry, 484,945-byte zipapps. Both are exact at SHA-256 `aae2fe8bacb04c022d3234cd4ab305fe976ddd7fef7130dc83528bcd8d62dd42`; the selected checksum file is SHA-256 `3246bc0f091a3082caf75f6ba8e3f4d1d6bac3177aadedac88b238dbc4119640`.
- Final-manifest source and package attempts each pass 123 scenario trials and every gate. Source report/trace hashes are `5d2adb5d7e6ca280ef9b8e2258a2a85c901e747310d5838344074e023af78a41` / `9aa2186597563476e8f87b90171bb70f648d2c07c9d1667d4ec9b80fbe672fc3`; package hashes are `75fa9c799980f6b3fa28619b61b3e5b5e3084c70f7eb53550361f6bd007bb9fc` / `9bd3bba0927ac430d01090f6e18bebc1a7b3597765d4a7e86b9e6e91526a864d`. Their exact 213-event anchors end at `edce500fa03164ab1bfc572a1d71f632df7d1356ba706c83361b4e0cb517328b` / `95aa3095766daf1b7c7189811fa1901cabc3f29aab1f616af8dfee18c1539528`.
- Source and package expose only the three bounded MCP diagnostic/read tools and pass real loopback authentication, approval, executor, postconditions, idempotency, replay, state, audit, telemetry, endpoint-anchor, CSP, and dashboard checks.
- Accepted complete 1440 by 1000 source/package dashboards at SHA-256 `75860f579b5d4a1c5721f5a059783f0e0938ec09763eac8d6a74a807473549a9` / `e3b4f6ca4f2cf69cb4cde0e7971445cdcda7df2ed70316bee09e72a6607e6103`. Native receipt hashes are `b05749c2290416f8f84fb029ba251c545e1437ee6e3f1fc03b3753019d3f6f0e` / `c5640a8b42dda82b81d0024d9170528ad9904cdcbdfbf51c0f9a4f996d9ff030`.
- The final local regression passes all 40 tests in 83.804 seconds and all 23 independent static/package validators. Both selected and independently rebuilt archives pass their exact package contract, and source/package traces verify at 213 events.
- Retained two no-change wrapper failures: the package verifier first rejected positional arguments because its current interface requires `--contract` and `--archive`; the next trace-verifier wrapper omitted `PYTHONPATH=src` and stopped at import. Corrected commands pass. The successful source dashboard capture also emitted a Chromium diagnostic after writing a complete inspected PNG; the receipt and unchanged image remain retained.
- Parsed 410 JSON files and 12,440 records across 142 JSONL files before the candidate receipt. Twelve broad credential-shaped matches are only frozen package-contract scanner definitions; the high-signal scan excluding those definitions has zero findings, and there are no model artifact files.
- Wrote the 7,248-byte `artifacts/verification/candidate-baseline-0022.json` receipt at SHA-256 `c8aeb0a116570a76d0b0aeb644e9d7485f2802df7e87124fc3cf0714818de08b` with disposition `candidate_ready_for_clean_clone`. The next gate is an HTTPS remote-only clone of the exact committed candidate with no object alternates.
- Committed and pushed exact public candidate `85390c29f512617e330fbe00764cac0a973ce6b1`, then cloned it over HTTPS with `--no-local --single-branch` into `C:\Projects\Verification\runbook-sentinel-baseline-0022-clean-85390c2-20260810T203503Z`. The clone began clean and has no object alternates.
- The first clean-clone wrapper ran compileall and tests in parallel; both tried to replace the same ignored `__pycache__` file and compileall received `WinError 5`. The detached test result was not used. Sequential compilation and all 23 validators then passed.
- The next governance wrapper applied the current milestone validator to every historical contract. Retained milestone-0013 lacks fields introduced by the current schema and was correctly rejected. The active milestone-0022 record validates without errors or warnings, and no historical evidence was rewritten.
- A clean captured suite passes all 40 tests in 93.977 seconds. Fresh source/package evaluations each pass 123 attempts; source report/trace hashes are `cacbc2637b4d8428a73a58313b5e804a04adb2d7c6c851a0a08dc634f4e7cad7` / `1ae89efc2cf0d7800f0ec75506c089ac7989b83232048ae41100c96f4c065285`, while package hashes are `9d2100a7d1e30a64f5509e577d0263aab0d838fcf83078d7b76a3c241b4764bc` / `5fc0ebd9d629e94276504a67cb05cee5c26bdada73c1f10e9cc0eaa21d189413`. Exact anchors end at `fced30293e952f59e69fa1357c42fb3a7c3b66b6a68ddec3f0e258cc84d01682` / `ea672733d0231814cd903bea9abe8d428b49694da71fc8f7b6ce8111d83c7faf`.
- Two clean-clone builds reproduce the exact 37-entry, 484,945-byte selected archive. Source and package repeat the three-tool MCP boundary and the real authenticated API, approval, executor, replay, state, audit, telemetry, endpoint, CSP, and dashboard gates.
- Original-detail inspection confirms complete source/package dashboards at SHA-256 `2481f9888717b020deb6eb3debd4fe267624b1c79fa409cbe2d8a4f85b621ec5` / `b2d161cc1fbbb71545528e333a421b9645d8529b7a07aecb70297606fe6d8b63`. Both successful captures printed the known Chromium fallback-task diagnostic after writing the images; the observation is retained.
- Parsed 403 JSON files and 12,664 records across 95 JSONL files after verification. Twelve broad credential-shaped matches are package-contract scanner definitions, the high-signal scan has zero findings, and no model artifact is present.
- Wrote the 7,428-byte `artifacts/verification/clean-clone-baseline-0022.json` receipt at SHA-256 `69ab956519f5c99e03d27586b47ac7c58005a741c0e8457a749f63a61c7e1f2d`. UNIT-004 is complete; release-record reconciliation and the premerge audit are next.
- Premerge release audit evaluates `verified`. Draft PR `#20` contains six commits and 64 paths before the one-path audit-only commit; at exact seven-commit, 65-path head `0a1b77b889d6fa6a39f3e2ec407feda32d00a02b` it reaches ready, CLEAN, and MERGEABLE with no configured checks.
- GitHub merges only the expected reviewed head with method `merge`, preserving exact parents `eaed929dbb2f0d2f8b54c2fe51aaea0902823f77` and `0a1b77b889d6fa6a39f3e2ec407feda32d00a02b` as `a336d2be7f0a288c459d9c5f0b6de9c5cfd06458`.
- A fresh HTTPS `--no-local --single-branch --branch main` clone begins clean at exact public merge `a336d2b`, has no object alternates, and passes compilation, all 23 validators, all 40 tests in 94.433 seconds, fresh source/package 123-attempt evaluations, exact 213-event anchors, and two exact selected-archive builds.
- Merged-main source report/trace SHA-256 values are `62423396e9a2fc435a82362dc9398fbf6cfb3e147408770905cd4ab5926693c6` / `390678af8abeee7e1d5b20399e64f57990e2ad553ed9da083cd58763de444a9a`; package values are `e6639c1b21984808b3240842451c049e7fe4b40bd164d5b6accc4b02314021fd` / `4444634ee328be0c126e2344208bbe4ab07df99cf1e98e5d54f26ab4bf6d421f`.
- Source and package merged-main MCP expose only three diagnostic/read tools, and both real runtime paths pass authentication, approval, executor, idempotency, replay, postconditions, SQLite, audit, telemetry, endpoint anchors, CSP, and dashboard checks. Original-detail dashboard inspection passes at SHA-256 `8620814ab062f25cf035c5cdd01e0f05a3b853558d332ba8f429d7074253e480` / `9628083e43feff40dbf8046b5ef7e310884c116b5fb7007f562726936614d4a5`.
- Parsed 408 JSON files and 12,668 records across 97 JSONL files. The high-signal credential scan and model-artifact inventory are empty. Only regenerated evaluation, dashboard, and native-receipt pointers differ in the verification clone; no source, contract, policy, manifest, or authority path changed.
- Retained merged-main tooling observations include a no-argument help probe executing source verifiers, a malformed two-path guard, refusal to overwrite the tracked candidate package attempt, unavailable `System.Text.Json`, two corrected Git status allowlists, and Chromium's known post-write diagnostic. The corrected bounded gates pass without rewriting any unfavorable result.
- Final release audit evaluates `verified`: active inherited authority, all nine declared real surfaces, all ten risk-matched checks, no missing/stale/unknown truth surface, and no warning. It authorizes only the exact ten-path release closure, annotated `v0.0.22`, selected 484,945-byte zipapp and checksum, and immediate public verification.
- Published and reconciled the release closure: remote `main`, annotated and peeled `v0.0.22`, non-draft GitHub release, selected archive/checksum assets, downloaded public bytes, rendered repository/release/tag pages, and a fresh no-alternates public-tag clone agree exactly.
- BASELINE-0022 is complete. Forty-one scenarios close the measured six-cell adversarial action/split gap but do not satisfy the separate at-least-48-case v0.1.0 target. The next checkpoint remains intentionally unchosen until a fresh run of public v0.0.22 identifies one measurable weakness.

## 2026-08-10 - BASELINE-0021 started

- Closed the previously incomplete external public-v0.0.20 receipt at `C:\Projects\Verification\public-release-baseline-0020.json`. Public refs, release metadata, selected assets and downloads, rendered pages, fresh public-tag source/package runs, MCP, API/approval/executor/state/telemetry, dashboard, and scans reconcile at release closure `f149ac2`.
- Ran the unchanged 31-case system across three trials. Every prior gate passes; the retained report/trace hashes are `b6c5c1221cf88833f26e3f875fc13af66fd3eba56d8ceccb341d86450aaffe6c` / `287a87239c0c00b5fca94eda0f0873a67f2b2056006a47c3ae03d27c67c51697`, with an exact 165-event anchor.
- Measured one bounded weakness: aggregate adversarial split coverage is 1.0, but adversarial domain-by-split coverage is only 7/16. Nine cells are empty; development adversarial coverage is confined to worker, while API and database are absent from both splits.
- Selected exactly one project-authored adversarial transform for each missing cell and froze `adversarial-topology-split-coverage-v1` before implementation. The independent validator composes all 31 prior scenario and terminal identities exactly, confirms candidate results are absent, and binds the 9-case target.
- Retained the first generic milestone-validation failure caused by old repository status vocabulary and missing current-schema terminal gate fields. The normalized milestone passes without warnings; `UNIT-003` is the sole authorized ready unit.
- No existing case, terminal state, runtime behavior, action, authority, dependency, secret, external asset, model call, or real-infrastructure boundary changed. Forty cases will remain below the separate v0.1.0 target of at least 48.
- Implemented catalog schema 1.12 with exactly the nine frozen transforms and copied terminal records, plus a fail-closed adversarial domain-by-split metric, missing-pair output, per-split coverage, four hard gates, and a dashboard card. No action or authority changed.
- The independent implementation verifier passes: all 31 prior scenario and terminal identities are exact, all nine transforms are exact, and adversarial topology split coverage is 16/16 with both splits at 1.0.
- All seven development transforms pass their exact expected retrieval, diagnosis, action, trajectory, approval/execution, postcondition, replay, audit, trace, and terminal-state checks. The two held-out transforms and full candidate evaluation remained unrevealed.
- Retained three pre-seal harness observations: the terminal validator still expected the 31-case 12/19 action split; a focused unittest selector used `BaselineTests` instead of `BaselineTest`; and a bare stale-payload invocation omitted `PYTHONPATH=src`. Corrected fixtures and invocations pass.
- Pushed public implementation seal `73ed1a14aa65580d57a176175f863e46921f3e05` before freezing the candidate manifest or revealing held-out behavior.
- Froze and verified the 81-file pre-reveal manifest at SHA-256 `1ecc472b11b2bb82d6d8c72b3cc70a02ab0b12ceb1620dd8de3386a2233f0354`, then pushed it before the full candidate run.
- Full-reveal attempts 001 and 002 used undersized shell command windows and stopped before producing artifacts. Attempt 003 is the first completed immutable result and remains the selected source candidate.
- Attempt 003 passes all 120 scenario trials and every gate. Both held-out transforms are exact across three trials; adversarial topology split coverage is 16/16; the 204-event trace is anchored at `ffb96f88dffb77128d66568af84e1819de8879795cf63d5f133279e9c0826d5c`. Report/trace hashes are `bbf655a2b67ec06d8e10f733dcd4eaae4a435f8759ab9435628b18430bff7211` / `feac17a494310863d03e086e893f3d5a70335d736fd759422c5e59c13e4807e2`.
- The first complete suite retained two evidence-fixture failures: an empty-topology test removed only one of two development observability cases, and the legacy full-context comparison expected 15/15 hostile-document retrieval although the frozen healthy-service API prompt retrieves only healthy telemetry. Corrected assertions pass all 39 tests in 167.074 seconds.
- Preserved the 14/15 observation as a release limitation. The checkpoint measures adversarial scenario-label topology; it does not claim every appended hostile document was retrieved.
- The first final-manifest source HTTP run passed every runtime and product check but failed its rendered checkpoint assertion because `verify_live_api.ps1` still searched for `Baseline 0020`. The screenshot correctly contained Baseline 0021. The failure and first archive pair remain retained; only the stale verifier identity is corrected before renewing the final manifest and package.
- Renewed the final 82-file manifest after correcting only the stale live-verifier identity. It passes at SHA-256 `f7fee20f2f748c25749255429fd362e3dd94dea7176b0285ce8509465da93bb8`; the first archive pair remains recoverably retained and superseded.
- Selected source attempt 005 and package attempt 002 pass all 120 trials and exact 204-event traces. Source report/trace hashes are `729f24aa1c038cc2d40c88927a3fabc1197fb815d3f3e6e542cb23af2396b7f8` / `28bf85127c81be0d517260e14ae4f817c3eff7ab9aebc0bfa1834fc6b6599355`; package hashes are `dde39cbc5f99e85e56e7e5a464dd7ff53692d41fdf5d13b57dd808027b315dc7` / `b22f81e4c9912d83d47e651115ff1276ba28c2a17d935c69886892e096471d33`.
- All 39 tests and 21 static/package validators pass. Source and package expose exactly three diagnostic/read MCP tools and pass real loopback authentication, approval, executor, postcondition, idempotency, replay, SQLite, audit, redaction, telemetry, CSP, and dashboard checks.
- Built two independent 36-entry, 468,870-byte zipapps; both are exact at SHA-256 `99f2e8b3276e1220073603f3101f02b5f733e5d91b521e2a8f5bac646d64a3de`. The checksum file is SHA-256 `605845394835d877167738311c733a13d9dfb9a7ddf3eead8c2d3e944d991e15`.
- Accepted complete source and package 1440 by 1000 dashboards at SHA-256 `8cb243b13bb4caa679e2be0a861012c559ac0a75101484fca7dd76cbdda47edc` / `f2d10a9a21ca6b3645519b0f8288271761ea919f96b789b2aea9bf6cc8f6e30f`. The package's first original-detail viewer frame showed only the incident-table strip, but the unchanged PNG rendered completely at high detail; the viewer artifact remains retained.
- Parsed 390 JSON files and 11,590 records across 136 JSONL files. High-signal credential scanning has zero findings after excluding only package-contract pattern definitions, and no model artifact file is present. The 6,188-byte candidate receipt is SHA-256 `435ea00039d53c5b81aa5bd3252308c465743b02ef91362ce416980714460f7e`.
- A post-candidate trace check first used unsupported `--report` and `--trace` flags. The corrected positional invocations verify the selected source/package 204-event anchors exactly; the command-interface failure remains retained.
- Committed and pushed exact public candidate `d978cc95ff0c2a0b4e779638a97bd767ee063ca3`, then cloned it over HTTPS with `--no-local --single-branch` into `C:\Projects\Verification\runbook-sentinel-baseline-0021-clean-d978cc9-20260810T185305Z`. The clone began clean with no object alternates.
- The first clean-clone wrapper probe passed `--help` to a script with no help-only path and timed out under a 20-second shell window. It produced no authoritative result; the unchanged rerun passed 21 validators, all 39 tests in 170.181 seconds, and a fresh 120-attempt source evaluation with an exact 204-event anchor.
- The clean clone rebuilt the selected 36-entry, 468,870-byte zipapp twice at exact SHA-256 `99f2e8b3276e1220073603f3101f02b5f733e5d91b521e2a8f5bac646d64a3de`. Its source report/trace hashes are `86962f5ac37616212b08259db9a907cf48aa314b063fca1ece09bb1551d544bc` / `10398843f921e5ced20633ef756bee2889d923338b65993bb537663cd78ca63e`; package hashes are `df17d6bf397c29c35db3cec6fd1880b8b3e23ac0546adae58c6485566235a256` / `a00d37e51091a67132635312b49e87e74617d1aad44ff064ee57f48b0b50bdeb`.
- Source and package clean-clone MCP expose only three diagnostic/read tools and pass the real authentication, approval, executor, idempotency, replay, state, audit, telemetry, endpoint, CSP, and dashboard gates. The package image repeated the original-detail table-strip viewer frame; the unchanged PNG rendered completely at normalized high detail.
- Parsed 384 JSON files and 11,803 records across 91 JSONL files post-verification. All 11 broad credential-pattern matches are frozen package-contract definitions; zero high-signal matches and zero model artifacts remain. The 7,270-byte clean-clone receipt is SHA-256 `2533a13cb2b31091af34a210f7fceaab92bc37f9712f6a9078c580b075ba2663`. UNIT-004 is complete; premerge release audit and exact public review are next.
- Premerge release audit evaluates `verified` with no warnings. Draft PR `#19` contained the exact 10-commit, 63-path pre-audit inventory; after the one-path audit-only commit it contained 11 commits and 64 paths, reached ready/CLEAN/MERGEABLE with no configured checks, and merged only expected head `deb68dc7cbe2812c8b647ee324aa1f99725e47ca` as history-preserving merge `ce7c95f33dfce10e678000df85961d12e987a1f0`.
- The first merged-main clone command selected `C:\Projects\Verification` before a repository-scoped fetch and stopped before target creation. The corrected no-local, main-only public clone began clean at exact merge `ce7c95f` with no object alternates.
- Public main passes 21 validators, all 39 tests in 165.347 seconds, source/package 120-attempt evaluations and exact 204-event anchors, two exact selected-archive rebuilds, bounded MCP, real authentication/approval/executor/replay/state/audit/telemetry, 386 JSON and 11,803 records across 91 JSONL files, zero credential/model findings, and complete dashboard inspection.
- Merged-main source report/trace hashes are `b67ff87abd852b57c933bcefa6edd711597c02998c41db9e56738eea05f7776f` / `e36d722993c98d7efc7f03f0727d143cb3eca4e48df856f97c896427e6bc449f`; package hashes are `582e03ad3094f383f424c5b92712cb864acf889905f2f1f14fd4145a8269f5a7` / `db39a1e476c0734beb6ab22c7bf8f77b03a3ef5617521dd4d66dbf4df59b3864`. Both archive rebuilds remain exact at the selected SHA-256.
- The merged-main source original-detail frame and package normalized-high frame each showed only the table strip; the same unchanged source bytes rendered completely at normalized high detail and package bytes at original detail. The renderer diagnostic and viewer behavior remain recorded in the 7,341-byte receipt at SHA-256 `3f71e36cfb3377d0c89626d5aa8bd2a4622072b872c553ef2464979fdec68398`.
- Final release audit evaluates `verified` with active inherited authority, all nine real surfaces verified, no missing, stale, unknown, or failed checks, and no warnings. It authorizes only the exact release closure, annotated `v0.0.21`, selected 468,870-byte zipapp and checksum, and immediate public reconciliation.
- Published and reconciled the release closure: remote `main`, annotated and peeled `v0.0.21`, non-draft GitHub release, selected archive/checksum assets, downloaded public bytes, rendered repository/release/tag pages, and a fresh no-alternates public-tag clone agree exactly.
- BASELINE-0021 is complete. Forty scenarios close the measured 16-cell adversarial topology/split gap but do not satisfy the separate at-least-48-case v0.1.0 target. The next checkpoint remains intentionally unchosen until a fresh run of public v0.0.21 identifies one measurable weakness.

## 2026-08-08 - BASELINE-0019 started

- Resumed from verified public v0.0.18 release closure at `5ac099f`, ran a fresh source evaluation outside the repository, and retained its passing 150-event trace.
- Measured one bounded evaluation weakness: aggregate eight-domain coverage was 1.0 while development had no observability case and held-out test had no database case, leaving split-aware coverage at 14/16 or 0.875.
- Source-gated the primary ACL CheckList publisher record for the narrow proposition that aggregate results can hide capability failures. No external code, paper bytes, data, model, package, service, credential, or other asset was imported.
- Froze `topology-split-coverage-v1` and a separate pre-change identity record before product changes. The strengthened freeze canonically binds all 28 existing scenario and terminal-state records; the earlier freeze commit remains preserved as superseded evidence.
- Implemented domain-by-split counts, exact missing-pair reporting, separate development/test topology coverage and four hard gates; added only the frozen development-observability and test-database cases and a rendered dashboard metric. Runtime behavior and authority are unchanged.
- Retained one direct validator invocation that omitted `PYTHONPATH`, then passed under the project environment. Retained the first full unit attempt with two harness failures: a one-second timeout killed manifest freezing and the exact-outcome fixture lacked the two frozen case IDs. The corrected 71-file manifest and complete 36-test suite pass.
- The first immutable full reveal passes all 30 scenarios across three trials. Both new cases and every pre-change case are exact; topology split coverage is 16/16 with both splits at 1.0; all prior gates remain passing.
- Source attempt 001 report/trace SHA-256 values are `5bbfd45b3d2cddc3837f5bb0e30d7c124f4847a010f6a415c143d26a1baad0f5` / `10dd32144fcfc96d0cc7e272958a235b36064fa067cb467ad48c6acd38c73712`. Its 156-event anchor ends at `d8531756ac8c74ab95a05fd390e27c53ea5bb18e72d075994101ec5f477dabf1`.
- Recorded the immutable candidate result in the frozen contract and updated its independent verifier to bind exact report, manifest, trace, case, coverage, trajectory, terminal-state, and anchor evidence. Package and real-surface verification are next.
- One post-candidate standalone trace-verifier invocation omitted `PYTHONPATH` and stopped at import; the corrected project-environment invocation verified the unchanged 156-event anchor exactly.
- Froze the v0.0.19 package contract before archive build at SHA-256 `e059cf9e70637cce8217135b7ffb08107def5900409734d9dfa2044604666336`. Its 34-entry allowlist adds only the topology contract to the v0.0.18 package surface, retains zero runtime dependencies, and preserves every no-go boundary.
- Froze and verified the final pre-build 72-file evaluation manifest at SHA-256 `adf637300cfbbda77b8c2b239313535899098f5a131a5ded7fdf334974b8ecbc` before creating any v0.0.19 archive.
- Built two independent 34-entry, 423,787-byte zipapps; both are byte-identical at SHA-256 `f840d5a1ff4da1b1a1e61f0719c65925075af3a776cb52d29b890749848a70ab` and pass the complete frozen archive verifier.
- Final-manifest source attempt 002 and package attempt 001 pass the complete 90-attempt evaluation and exact 156-event anchors. Source report/trace hashes are `feb778b3a1be6fdf2bb9a0187d78edbf30d85ff749758a9667bed9263518801f` / `bcd06b282981003cc509b9eb015b48ed172d039371164de8444ca4ebf9cb39de`; package hashes are `b0e328cbacf0922177437a3cd3ef368ae892c693819ab0c6699acf264b9d5e8e` / `0b7e35d5a66f231df685afa5bd9df1db6af50edf9793807ab36e57c44046adb0`.
- Source and packaged MCP report version 0.0.19 and expose only three diagnostic/read tools. Source and package real API, authentication, approval, executor, idempotency, replay, SQLite, audit, redaction, chained telemetry, endpoint anchor, and native-inspection checks pass.
- Original-detail review accepted the complete source dashboard. The first package image frame showed only the lower incident-table strip, but later pixel inspection proved the unchanged 1440 by 1000 PNG contained the complete dashboard and normalized high detail rendered it fully. The initial frame and exact PNG remain retained as a non-byte-changing viewer artifact rather than a product failure.
- Selected source/package dashboard SHA-256 values are `0039ca11bb7b0cdbe0be5801bb38b29082936bb673a0a8ce9b517eaf2b694b70` / `4924748f1571780024d997a2dfb061c926f4f31723fb6cd9beb550a3c931a124`; the failed package capture is `e85cf8fb871c694b6702ab31fd8c47bf38055e30c87cf693057626a9d198c95f`.
- Parsed 346 JSON files and 9,575 records across 121 JSONL files without error. A broad secret-shaped scan matched only nine package-contract definition files; the high-signal scan excluding exactly those definitions has zero matches, and no model artifact file is present. Docker remains unavailable at its desktop-linux pipe.
- Committed and pushed exact candidate `4dd8ea92157bbd28cd02e2bf8301e9098da3dd67`, then cloned the public branch with `--no-local --single-branch` into `C:\Projects\Verification\runbook-sentinel-baseline-0019-clean-4dd8ea9-20260808T203003Z`. The clone began clean, has no object alternates, and independently passes 17 validators, 36 tests, fresh source/package 90-attempt evaluations, exact 156-event anchors, bounded MCP, real API/authentication/approval/executor/state/telemetry, complete dashboards, artifact parsing, and security scans.
- Clean-clone source report/trace hashes are `3f6373791df30fd17502a55323fac003f907eccc71f8553558c0e4dcd70a4e9c` / `54ca493973ddf065197ce3781367c68257564bc4cab6b89951ddb24e2bc417ad`; package hashes are `62a558d064a01fcb6280f44e36a0b9bbbba3b5d39db0cfb87a61554bd25aebe1` / `26ea9afd53c66eb3888da021a8aef209868e5cfb8d77fc5f5dfa07aae6498544`. Two independent clone builds reproduce the exact selected 34-entry, 423,787-byte archive.
- The clone parses 347 JSON files and 9,706 records across 80 JSONL files. Its first high-signal scan matched only `eval/package-contract.json`'s frozen private-key pattern; excluding all ten package-contract definition records leaves zero hits and zero model artifacts. A first post-build verification command named nonexistent `scripts/verify_package.py`; the corrected `scripts/verify_package_contract.py` passes both unchanged archives. Both observations remain in `artifacts/verification/clean-clone-baseline-0019.json`.
- The clean clone's original-detail package preview again showed only the table strip. Pixel-level comparison proved the PNG is complete and structurally matches the full source dashboard outside expected incident identifiers; normalized high detail displayed the full surface. This confirms a viewer artifact rather than nondeterministic dashboard bytes.
- The first combined evidence-only regression wrapper reached its 124-second shell ceiling while verbose output remained buffered, so it was terminated without a complete result. The unchanged-tree rerun completed in 168.6 seconds: all 17 validators pass, two complete 36-test executions pass, and fresh source attempt 003 passes all 90 scenario attempts with exact 156-event anchor. Its report/trace hashes are `454d11ed1c0760d991e434b4d8c0fbf2a8720613ba6444504a178196270ad8a5` / `e1d7961e412781059115a350d9fde215e0450b824ec0f47565635fbf96c10eed`; the final event is `9a5550734ef7eba6ac4ea31004412d1a0dcb53ce36d56dbc35074982e3d5e551`. The timeout remains retained rather than rewritten.
- Premerge release audit computes `verified`. Draft PR `#17` contained the exact 65-path inventory and seven commits, reached ready/CLEAN/MERGEABLE with no configured checks, and merged under expected-head lock `8530515266165ffd0d68021659ba5261fc42d154` as history-preserving merge `40ae2230834ba03a665d72b0308b8c64ea544b36`.
- The first merged-main clone command incorrectly selected its not-yet-created target as the process working directory and failed before execution. The corrected no-local, main-only public clone began clean at exact merge `40ae223` with no object alternates.
- The public-main clone passes 17 validators, 36 tests, source/package 90-attempt evaluations and exact 156-event anchors, two selected-archive rebuilds, bounded source/package MCP, real authentication/approval/executor/replay/state/audit/telemetry, 350 JSON files and 9,862 records across 81 JSONL files, zero credential/model findings, and complete source/package dashboard inspection.
- Merged-main source report/trace hashes are `53856838a1093e88f5fbb896aeea93865e3c7eae4519c41ac1f011dd61e2a88c` / `3b8f58e166ebcdc261ce92b97423b09f0ae15f1e5598e0e8fa10df83087e6274`; package hashes are `0f0b09b909a2a181096a8714305a21bade6691f7a6bda7e96db9723488abd371` / `1b5fbbe172cae3352d72e26b9e98de298fde3ff9972b6458dfc9df01699f1dfa`. Both archive rebuilds remain exact at the selected SHA-256.
- The merged-main package dashboard's first normalized-high view showed only the known table-strip viewer frame; the unchanged original-detail view displayed the complete dashboard. The artifact remains recorded in `artifacts/verification/merged-main-baseline-0019.json`.
- Final release audit computes `verified` and authorizes only the exact release closure, annotated v0.0.19 tag, selected zipapp/checksum assets, and immediate public verification under the standing goal.
- Published and reconciled the release closure: remote `main`, annotated and peeled `v0.0.19`, non-draft GitHub release, selected 423,787-byte archive, checksum asset, downloaded public bytes, rendered repository/release/tag pages, and a fresh public-tag clone agree exactly.
- BASELINE-0019 is complete. Thirty scenarios close the measured 16-cell split-topology gap but do not satisfy the separate at-least-48-case v0.1.0 target. The next checkpoint remains intentionally unchosen until a fresh run of public v0.0.19 identifies one measurable weakness.

## 2026-08-08 - BASELINE-0018 started

- Resumed from the verified public v0.0.17 release closure. Exact branches, annotated and peeled tag, non-draft latest release, selected assets, anonymous downloads, rendered pages, and a fresh no-alternates public-tag clone reconcile at `7bf9846`.
- The fresh public-tag clone passes source/package `84+9+6+10+10+10`, 33 tests per runtime, exact archive rebuild, MCP, API, approval, executor, replay, state, audit, live endpoints, artifact parsing, security scan, and original-detail dashboard inspection. The external receipt is `C:\Projects\Verification\runbook-sentinel-v0.0.17-public-7bf9846-20260808T173100Z\public-release-baseline-0017.json` at SHA-256 `52b83dfb088e1ad17b13c062644016eba6f99861a0d1a8055dc2d0e9bd9598d4`.
- Selected one measurable evaluation weakness: the retained local-model candidate has 49 `schema_invalid` failures and only 5 valid outputs across 54 attempts, but the parser and evaluation expose no stable content-rejection reason. Benign utility and `pass^3` remain 0.0, no proposal was accepted, and the candidate remains excluded.
- A non-destructive pre-change probe confirms invalid JSON raises `JSONDecodeError`, while key mismatch and out-of-context evidence raise generic `ValueError`; none carries a machine code. No released or runtime evidence changed.
- Refreshed the existing local-source gate against official Ollama 0.32.6 and current Ollama/Meta records. The exact publisher-signed executable, unchanged model manifest, and all six blobs pass identity, rights, provenance, integrity, fitness, and privacy/security checks. No external asset was acquired.
- Froze `model-output-failure-taxonomy-v1` before adapter changes with 17 exact codes, 19 cases, eight development and eleven held-out test cases, two valid controls, digest-only raw-output retention, and unchanged fail-closed semantics. The independent validator passes with candidate results absent.
- Implemented the closed taxonomy without changing parser acceptance or failure results. Non-content failures retain a null code; content rejection records one code; aggregate and split evaluation now expose exact counts, classification rate, and unclassified count under report schema 2.4.
- Development-only attempt 001 passes all eight allowed cases exactly at SHA-256 `671e120fb59c0ef722d234b2905a71eda028507b6ae055c2301bbda91c2019b8`. All 35 tests pass, including aggregate/split metric checks and exclusion of a unique raw-content marker from both report and trace. The implementation is ready to seal before the first full reveal or local-model call.
- Generic implementation seal `8ce268d` was pushed before any held-out execution or current local-model invocation. The first immutable full reveal then passed all 19 frozen cases exactly, with both splits at 1.0, classification and valid acceptance at 1.0, zero unclassified failures, and no raw content retained. The 8,680-byte report is SHA-256 `6db141ba9cdfe00e58fb6b35503060b641a02502df03c8b9f9750306c4d360d3`.
- Froze and verified the 65-file baseline-0018 experiment manifest at SHA-256 `8bbcead24e4679b759b255ab3bf7140f1227fa1b4222d74073766162f913f62e`. The exact bytes were copied beside the model attempt before the sole current local-model evaluation.
- The sole current local-model attempt completed 84 frozen calls in 896.9 seconds. It produced 9 valid and 75 schema-invalid outputs; the new taxonomy classifies all 75 as 67 invalid diagnosis identifiers, 7 nonempty proposal-argument objects, and 1 out-of-context evidence citation. No content failure is unclassified.
- A same-manifest deterministic control passes every gate, with utility and `pass^3` 1.0 and 47.675 ms median latency. The candidate has diagnosis accuracy, proposal exactness, utility, and `pass^3` of 0.0 and 10,173.578 ms median latency. It accepted and executed no action; policy remained 1.0 and attack success 0.0 only through fail-closed external enforcement.
- The independent comparison verifier passes. It retains `deterministic-control-v2` as default and excludes the measured model candidate; no prompt, schema, parser acceptance, model options, or held-out expectation was adapted after the result.
- Versioned the bounded candidate as `0.0.18` / `baseline-0018` and froze a 33-entry package contract before archive build. The contract passes at SHA-256 `18c48dfa2d22ea62aef6464b72df576e7233ee58f202faa16fd3248728dcd3cd` and includes the model-output failure contract without adding a dependency, model artifact, credential, runtime state, or real-infrastructure connector.
- Froze and verified the first 67-file release manifest at SHA-256 `f925c014d4eb24fba15189cec920760d06b81181bbba140b41627cc46e05f944` before source evaluation or archive creation.
- Complete source attempt 001 passed all 14 validators, 35 tests, and 84 deterministic trials. The first two builds were byte-identical at 410,293 bytes and SHA-256 `878c58e602e06b331566a56708fce66847b9bbf2b1b5fb914dd18ece6e1a5d3b`; package attempt 001 also passed the evaluation gates.
- The first real HTTP source run passed runtime and product behavior but failed its rendered checkpoint check because `verify_live_api.ps1` still asserted baseline 0017. The failure receipt and all attempt-001 reports, traces, manifests, and archive bytes remain retained; no passing result was substituted for that attempt.
- Corrected only the stale verifier identity, added the stale baseline to its exclusion inventory, and refroze the 67-file release manifest at SHA-256 `c787fc879e89ead00cd6555fc4d7c21bac1d3fcbe55fd561784753b23234cffd`.
- Complete source attempt 002 and package attempt 002 pass all validators, 35 tests, and `84+9+6+10+10+10` evaluation gates. The selected source report/trace hashes are `a1cd9c2e7fa86c3b4234fc1a03dd4a6d6da8a1792a4912bf556eb98846a7c6f0` / `34f44df1e4394f25ff35723624344be4d02c9a46539da25e08e1105dc07b0ede`; package hashes are `52788050cee73dd3149dd8485611035f02d0a29f49a3ec52b6cba3169be9ad34` / `beb8e25a9c375899b1138924bc1754d66dd60d2f50cfd8477b8bb96c52dfad52`.
- Two corrected builds are byte-identical: 33 entries, 410,293 bytes, SHA-256 `e370c208bc6598cf6217f963bc6ea567f05df5757a371bda67fc5157e23a21d0`. The first archive is recoverably retained under `dist/retained` and superseded.
- Both source and selected package pass MCP protocol `2025-11-25` with version `0.0.18`, exact three-tool diagnostic/read authority, no approval or execution tool, and anchored two-event live traces.
- Both source and package pass the real loopback HTTP approval, executor, postcondition, idempotency, replay, SQLite, audit, redaction, telemetry, live endpoint, CSP, and dashboard checks. Independent native receipts pass, and original-detail inspection confirms complete baseline-0018 dashboards with authenticated external-operator execution and disconnected real infrastructure. Source/package screenshot hashes are `04d0c71e5e730d06016432c19215a3b09415176d22d37ade4c92aa301f553cbf` / `622c507e6a073cb3f2ec7aac5c8a4644d18fd5d9b6b17e2b8d9931021a8f8630`.
- Committed and pushed exact candidate `300575b686b29620d2609ca310ccef4799ab57ae`, then cloned that public branch with `--no-local --single-branch`. The clone began clean, has no Git object alternates, and independently passes all 14 validators, 35 tests, fresh source and package repeated evaluations, bounded MCP, real API/approval/executor/state/telemetry surfaces, artifact parsing, and model/credential exclusion.
- The clean clone independently rebuilt the selected 33-entry, 410,293-byte archive twice at exact SHA-256 `e370c208bc6598cf6217f963bc6ea567f05df5757a371bda67fc5157e23a21d0`. Its source/package evaluation reports are SHA-256 `97d4d279ddde011cfc54a2e9ce04680dcf837b9dd69ab6c604ca9345edcf9e5d` / `d7be8a83195125e228776db1877a229d7e14b23833084568c93b1d0407e82e51` with independently verified 150-event anchors.
- The first high-signal secret scan matched only eight frozen package-contract pattern definitions; excluding exactly those definition files produced zero hits. The package dashboard's first original-detail viewer frame showed only the lower table strip; normalized high detail displayed the unchanged complete 1440 by 1000 surface and passed visual inspection. Both observations are retained in `artifacts/verification/clean-clone-baseline-0018.json`.
- Opened draft PR `#16`, verified its exact 68-path scope, six expected candidate commits, clean merge state, zero configured checks, and exact remote head, then committed the verified premerge release audit. The audit-bearing head `f13c0c26e3e3892b84675952d65da173513ea507` reached ready, `CLEAN`, and `MERGEABLE`.
- Merged PR `#16` under expected-head lock with history preserved as `b3773d672c32115e8cf94f6735d5582b0be27901`. Its parents are exact prior main `7bf9846f262e8e75f1ed2f45d4c9d4d7573f1d8f` and reviewed head `f13c0c26e3e3892b84675952d65da173513ea507`; remote `main` matches.
- A fresh remote public-main clone began clean with no alternates and passes all validators, 35 tests, fresh source/package repeated evaluations and exact anchors, two selected-archive rebuilds, bounded MCP, real API/approval/executor/state/telemetry, parsing, model/credential exclusion, and full dashboard inspection. The first high-detail package view showed only the lower table strip; original detail displayed the complete unchanged image, and the viewer artifact remains retained.
- The final release-truth audit passes every changed-risk, taxonomy, model-comparison, package, evaluation, real-surface, clean-clone, repository-integrity, GitHub merge, documentation, and recovery check. It authorizes only the exact release-closure commit, annotated `v0.0.18`, selected 410,293-byte archive, adjacent checksum, and immediate public verification.
- Fast-forwarded public `main` to the release closure, created and pushed the annotated tag, published the non-draft release with only the selected `.pyz` and `.sha256` assets, and verified tag peeling, release target, anonymous downloaded-byte identity, rendered public pages, and a fresh public-tag clone.
- BASELINE-0018 is complete. The measured local model remains excluded; the next checkpoint remains intentionally unchosen until a fresh run of public v0.0.18 identifies one measurable weakness.

## 2026-08-08 - BASELINE-0017 started

- Resumed from verified public v0.0.16 rather than reopening architecture. Remote main, annotated and peeled tag, non-draft release, selected archive and checksum, anonymous downloads, release page, commit-bound rendered README, raw main/tag, and a fresh no-alternates public-tag clone reconcile at release closure `465f4e2`.
- The downloaded public archive and fresh tag-clone source/package gates pass `84+9+6+10+10`, 28 tests, exact 358,711-byte archive rebuild, bounded MCP, all 86 API checks, all 49 state/audit/telemetry checks, parsing, credential/model exclusion, and dashboard inspection.
- Retained public-verification observations: the first tag-clone image view showed the known all-dark preview before unchanged bytes rendered correctly at high detail; the generic repository landing fetch served cached v0.0.15 text while the commit-bound rendered README, raw main and tag, release page, refs, and tag clone were current.
- Inspected the fresh public-tag live API trace. Its five events form a valid chain, but there is no durable live endpoint-anchor file; only completed evaluations have a separately persisted report anchor.
- A non-destructive in-memory probe built one canonical public-package event after the exact live prefix. The six-event full stream and five-event suffix-truncated stream both verify unanchored. The truncated stream fails exact count and final-digest checks only when the omitted full endpoint is supplied. No released, repository, or runtime evidence byte changed.
- Selected durable live endpoint anchoring as the next bounded measurable weakness. It changes no agent result, retrieval, action, model, approval, executor, authority, secret, or infrastructure boundary.
- Official Python 3.12.13 `os` and `tempfile` documentation passes the eight-criterion source gate for narrow citation and project-authored flush, fsync, secure temporary creation, and same-directory replacement. No external code or asset was imported.
- Froze `live-trace-anchor-v1` with four development and six held-out cases plus an independent contract validator before implementation. The active milestone validates with `UNIT-003` as the only authorized ready unit.
- Implemented only the generic anchor boundary and explicit live CLI/API/MCP wiring after freeze. Each trace append flushes and fsyncs before a secure same-directory temporary anchor is flushed, fsynced, closed, and replaced; resume verifies the exact pair and fails closed.
- All four frozen development cases pass, as do the complete 32-test regression suite, compilation, and a direct CLI real-file probe with an exact one-event endpoint. Held-out cases have not been run; the generic implementation is ready to seal before the first full reveal.
- Added the contract-driven evaluator and reran the development split at 4/4 exact without reading the six held-out outcomes. Its first bare invocation omitted the documented `PYTHONPATH=src` and failed before case execution; the corrected project-environment invocation and complete 32-test regression suite pass.
- The first immutable full reveal passes all ten live-anchor cases exactly: both splits are exact, invalid states append nothing, truncation and extra suffixes are detected, and valid restart/resume is exact. The 10,147-byte result is retained at SHA-256 `449e93aef8c9b27cfc2429576a5f86c0aa6ec2664439fcaca54cdef942c4eb96`.
- Versioned the candidate as v0.0.17/baseline-0017, froze a 62-file manifest and 32-entry package contract, passed 33 tests and the full source and package `84+9+6+10+10+10` gates, and built two 392,893-byte archives at the same SHA-256 `b43d1a9f6ec7771fcaa07140e4b50ea77e1e4b6991753edaacf17d778cfa11ed`.
- The first packaged HTTP run served the source evaluation pointer; its runtime binary and checks passed, but the receipt was superseded and rerun against the immutable package report. A separate PowerShell UTC display used an unavailable `Get-Date -AsUTC` option before the manifest generator independently wrote and validated a live UTC timestamp.
- Machine checks passed the first source/package dashboard candidate, but visual inspection showed the added endpoint metric pushed the persisted-incidents table below the 1440 by 1000 evidence frame. The exact first archive, source/package screenshots, and native receipts remain retained as superseded attempt 001; only responsive layout density is changing before a new manifest and package build.
- A no-local-object clone of candidate `773c539` passed its source/package evaluation, exact archive rebuild, MCP, API, live-anchor, state, audit, and native checks, but original-detail visual inspection showed the incident row still clipped at the lower edge. That clone is not promoted. The exact second archive, source/package screenshots, and native receipts remain retained as superseded attempt 002.
- Reduced only project-authored dashboard spacing and type density. The renewed 62-file manifest is SHA-256 `e0425726005421c45e76d290fcd9aa5e29025c49da136a1419975b72e24d0467`; source and package attempt 003 pass every `84+9+6+10+10+10` gate with exact 150-event anchors, and two independent 392,954-byte archives are identical at SHA-256 `271242d6eabffd4205f8b386028073f6d041b6ec5cc9369a524e45cacc9fe1ef`.
- Renewed source and packaged MCP, real API, authentication, approval, executor, replay, state, audit, live-endpoint, telemetry, and native receipts pass. Original-detail inspection confirms both selected 1440 by 1000 dashboards show the complete incidents table with no clipped row. An accidental `--help` call to the non-argument baseline runner stopped at the expected pre-freeze manifest mismatch, and the non-argument MCP verifier ran once; neither result was selected or promoted.
- A remote-only clone of exact candidate `8929ed5fe4370660235045929769f9fa16813939` began clean with no object alternates. It passed all validators, 33 tests, fresh source and package `84+9+6+10+10+10` evaluations, independent 150-event anchors, and an exact selected-archive rebuild.
- The clean-clone package exposes exactly three diagnostic/read MCP tools and passes the real API authentication, approval, executor, replay, state, audit, live API/MCP endpoint, telemetry, artifact-parsing, model/credential exclusion, native, and original-detail dashboard gates. Headless Edge emitted its known fallback-provider warning after writing the valid complete PNG; the warning and the superseded `773c539` visual failure are retained in the clean-clone receipt.
- The first premerge truth scan caught one stale README phrase that still called the now-passing clean-clone gate pending. The documentation-only correction changes no runtime, contract, manifest, evaluation, package, authority, or selected asset byte; remote PR truth will be reread at the new exact head before audit promotion.
- Premerge release audit `BASELINE-0017-PREMERGE` computes `verified` with no errors, warnings, failed checks, missing surfaces, stale surfaces, or unknown surfaces. PR `#15` became ready only after the exact audit head was pushed.
- GitHub reported audited head `5c7d2cb2536e366066dcc57c7afc1a3a5fed1fba` `CLEAN` and `MERGEABLE` with no configured checks. Expected-head-locked merge commit `1cf1ddb5c2c2f8107ea5959cfdc6f32a8003508f` preserves exact prior-main and reviewed-head parents.
- A fresh remote-only public-main clone began clean with no alternates. It passes all validators, 33 tests, fresh source and package `84+9+6+10+10+10` evaluations, exact 150-event anchors, the exact 392,954-byte archive rebuild, three-tool MCP, real API/state/audit/live endpoints, artifact parsing, model/credential exclusion, and original-detail dashboard inspection. The known Edge fallback-provider warning followed a valid PNG write and remains in the merged-main receipt.
- The first final-audit draft omitted the schema-required `candidate.commit` while separately recording the identical merged-main evidence commit, so the audit evaluator correctly computed `blocked` before any publication action. The exact 15,967-byte invalid draft remains retained at SHA-256 `8d3a179256c11f4de42b6444cb6db4482dc473c01b927996ca0af7c25b83ef97`; the selected audit adds only the missing identity field and must independently compute `verified`.
- The selected final audit independently computes `verified` with no errors, warnings, failed required checks, missing surfaces, stale surfaces, unknown surfaces, or unrun surfaces. It authorizes only the exact release closure, annotated `v0.0.17` tag, selected 392,954-byte archive and checksum, and immediate public verification.
- Prepared the release closure for annotated `v0.0.17`, the selected zipapp at SHA-256 `271242d6eabffd4205f8b386028073f6d041b6ec5cc9369a524e45cacc9fe1ef`, its adjacent checksum, a non-draft public GitHub release, downloaded-byte verification, rendered public pages, and a fresh public-tag clone. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.

## 2026-08-08 - BASELINE-0016 started

- Resumed from the verified public v0.0.15 release closure and ran its downloaded 326,418-byte archive in `C:\Projects\Verification`. All prior `84+9+6+10` gates passed.
- Measured one bounded weakness: changing an in-memory execution event from verified to failed postconditions preserved valid JSON, expected event names, every current telemetry check, and the evaluation pass disposition. The released bytes were not changed.
- Reviewed official RFC 5848 and Python 3.12 `hashlib` and `json` documentation through a source gate. The review authorizes only narrow citation and a project-authored standard-library design; it imports no code, asset, dependency, data, model, service, secret, or key.
- Froze `trace-integrity-v1` with four development and six held-out cases before candidate implementation, then committed the freeze as `c86049a`.
- Implemented exact event schema, sequence, predecessor digest, canonical SHA-256, fail-closed prefix validation, exact resume, completed-evaluation event-count/final-digest binding, and an independent report-to-trace verifier. The generic implementation was sealed as `c4dd4e0` after development-only checks passed.
- The first held-out reveal passed all ten cases with seven of seven corruption cases detected and both splits exact. Its immutable 7,046-byte result SHA-256 is `cf6bb931fd2869fa396604d0ffd4a4d0248a9e4c30cd6aed56e4c895bc7db80b`.
- Versioned the release surface as 0.0.16/baseline-0016 and froze a 57-file manifest plus 30-entry package contract before archive build.
- The source gate passes 28 tests and `84+9+6+10+10`. Its independently verified 150-event trace ends at `dbe3cee18785bc8686f21086df3df2977a888037b08a9c0d6fdbfab8a95612df`.
- Two independent 358,711-byte archives are byte-identical at SHA-256 `9c04e2815f4bb536904a803f8bf64079342eab4f694039ea8c61105453b8344f`. The packaged `84+9+6+10+10` gate passes and its exact 150-event anchor ends at `76051073b636528fba402098108e47c03d5a1ba7503bc1026595d05e01b7e770`.
- Source and packaged MCP expose only three diagnostic/read tools and emit valid chains. Both real API paths pass authentication, approval, execution, replay, persisted state, audit, live-chain, evaluation-anchor, log, and dashboard checks.
- The source and package dashboard PNGs are byte-identical at SHA-256 `fd99b986e63db29c7ba5075ba448004bfadfe48ae13981761929a8229cc07184`. Visual inspection confirms Baseline 0016, evaluation pass, trace integrity 1.0, authenticated external operator, and disconnected real infrastructure.
- The next gate is a no-local-object clean clone. No public release claim is made yet, and the unkeyed chain is explicitly not writer authentication, hostile-writer resistance, a signature, immutable storage, non-repudiation, or RFC 5848 conformance.
- A no-local-object clone of exact candidate commit `8c088c2cbe68e7bdb30363cf094cb6e37025067c` started clean with no Git alternates. Its first 120-second source-gate wrapper timed out after the complete immutable source evaluation was written; the report, every gate, and its 150-event anchor independently verify pass and were not overwritten.
- The clone reproduced the exact selected 358,711-byte archive, passed a fresh packaged `84+9+6+10+10` evaluation and exact anchor, exposed only the three bounded MCP tools, and passed real authentication, approval, executor, replay, state, audit, live-chain, evaluation-anchor, artifact-parsing, and credential-scan gates.
- The first clone dashboard view rendered as an all-dark preview for the same valid SHA-256. Reopening the unchanged 70,224-byte PNG at original detail displayed the complete correct Baseline 0016 dashboard. Release review is now the next dependency.
- Draft PR `#14` became ready only after the exact premerge audit reported `verified`. GitHub reported the audited head clean and mergeable with no configured checks; the expected-head-locked merge preserved history as `848a7ae1c3dd8dfec6d40bbfe5196263e99cb90e` with exact prior-main and reviewed-head parents.
- A fresh `--no-local --single-branch` clone of public `main` began clean with no object alternates. Twelve validators, 28 tests, a new source `84+9+6+10+10` evaluation, independent 150-event anchor verification, and an exact 358,711-byte archive rebuild pass.
- The merged-main package passes a new `84+9+6+10+10` evaluation and exact 150-event anchor, bounded three-tool MCP, all 86 API/dashboard checks, all 49 persisted-state/audit/telemetry checks, 266 JSON parses, 65 JSONL parses with 7,492 records, model and credential-shaped-value exclusion, and original-detail dashboard inspection.
- Retained merged-main tooling observations include a truncated shell result while the evaluator continued correctly, one missing package import path before an unchanged anchor passed, PowerShell treating normal unittest stderr as an error before explicit 28/28 capture, and a ripgrep pattern beginning with hyphens before the corrected scan found only frozen definitions and then zero values.
- Release-truth inspection caught one documentation-only mismatch: README still said 28 package entries even though the frozen v0.0.16 contract and selected archive contain 30. The release-closure branch corrects that claim before final audit; no runtime, evaluation, package, or selected asset byte changed.
- `artifacts/verification/merged-main-baseline-0016.json` binds the public merge, fresh clone, exact package identity, evaluations, traces, real surfaces, and retained tooling observations. Final pre-publication audit is the next gate; v0.0.16 remains untagged and unreleased.
- Final pre-publication audit `artifacts/verification/release-audit-baseline-0016.json` computes `verified` with no errors, warnings, failed required checks, missing surfaces, or stale surfaces. It authorizes only the exact release-closure commit, annotated v0.0.16 tag, selected archive and checksum, and immediate public verification.
- Prepared the release closure for annotated `v0.0.16`, the selected 358,711-byte zipapp at SHA-256 `9c04e2815f4bb536904a803f8bf64079342eab4f694039ea8c61105453b8344f`, its adjacent checksum, a non-draft public GitHub release, downloaded-byte verification, rendered public pages, and a fresh public-tag clone. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.

## 2026-08-07 - BASELINE-0015 started

- Resumed from the verified public v0.0.14 release closure and downloaded public bytes instead of restarting architecture work.
- The annotated tag, remote main, downloaded 283,148-byte archive, checksum, fresh public-tag clone, nine validators, 22 tests, exact rebuild, source/package 84+9+6 evaluations, bounded MCP, real API/state/telemetry checks, and rendered dashboards reconcile.
- Retained public-verification tooling observations: package help was invoked before the ignored archive was rebuilt; a post-pass byte comparison used unsupported PowerShell 5.1 `-AsByteStream`; a successful release creation was followed by an unsupported `gh` JSON field; the generic repository landing fetch served stale cached v0.0.10 text while the commit-bound rendered README, raw main, release page, refs, and tag clone were current; and an initial background-process probe was blocked before launch. Corrected verification paths passed without changing release bytes or product behavior.
- Inspected the 150-event public-package trace. All 33 approval events use the evaluator's caller-supplied actor `frozen-evaluation-harness`; no metric authenticates or authorizes that actor or grades separation of duties.
- Probed the released loopback HTTP API with `actor: sentinel-agent-self-declared`. The API created an approval token with HTTP 201 and accepted it for HTTP 200 execution; the worker became healthy, restart count became 1, and postconditions passed.
- The first two probes completed the product flow but their reporters failed after execution by assuming a nonexistent health version and then a nested incident response. Their SQLite and trace evidence remains external and hashed. A corrected third report passed without changing the product request or expected finding.
- The gap is approval provenance, not token hashing or executor escape: approval tokens remain hashed at rest and absent from traces, the model and MCP receive no approval capability, and only disposable synthetic loopback state changed.
- An honest enforcement fix requires a no-cost local operator credential or an OS-authenticated access boundary. Both cross the explicit secret/access gate in the standing goal. A label-only correction would remove the unsupported `human approval` claim but would not enforce identity. `contracts/milestone-0015.json` therefore waits at `UNIT-002` before contract freeze or implementation.
- The first controlled-milestone schema validation failed because the project-authored record used descriptive milestone, authority, and unit states outside the skill's fixed vocabulary and omitted required `forbidden_work` and `reversible_actions` keys. No product or evidence byte changed. The contract now uses active/inherited/ready/planned states, retains the explicit withheld action classes and human gate, and requires a new validation result.
- The corrected controlled-milestone record validates with active inherited authority, `UNIT-002` as the only ready human-gated unit, and zero authorized ready units. This preserves the pause rather than misrepresenting the secret/access choice as inherited authority.
- Reviewed official RFC 9110, RFC 6750, RFC 9700, and Python 3.12 `secrets`, `hmac`, and license documentation through source gates without importing code, content, services, assets, or secrets. Generic HTTP semantics and standard-library feasibility are ready; direct Bearer/OAuth use is retained as blocked because Bearer requires TLS and OAuth is unnecessary for the bounded local surface.
- The first validations of the two ready source records failed because their decision action phrases did not exactly match the inherited authority and planned-action strings. The blocked source decision was already valid. Normalizing only those action strings made all three records valid without changing a source fact, fitness result, product byte, or authority boundary.
- The user replied verbatim `yes, implement the recommended option`, selecting the per-launch operator capability and unblocking the explicit secret/access gate. The active goal automatically returned to active status.
- The first human-review prepare rejected extra provenance keys outside its closed schema. A second prepare produced a blank with zero decisions. The first lock then rejected a generic candidate filename because it lacked the exact response-hash prefix. Renaming the unchanged bytes allowed an exact lock and aggregate reconciliation: one selected per-launch capability, zero alternatives, no fabricated decision, and no downstream authority invented by the review.
- Accepted ADR 0011 and froze `operator-authentication-v1` at ten ordered cases before candidate runtime implementation. The independent validator passes. Missing, wrong, malformed, Bearer, duplicate, prior-launch, caller-actor, authorized utility, restart, no-mutation, identity, and secret-exclusion expectations are now immutable for this checkpoint.
- The first three focused operator tests passed the real development authentication and approval/execution flows, but the fail-closed contract test found that the independent validator did not compare the complete secret-exclusion list. The frozen contract was unchanged. The validator now exact-compares the full secret and terminal-state sections before the focused rerun.
- The first full 24-test run then passed 23 tests but intermittently aborted a Windows HTTP connection during the malformed-body development case. Authentication had failed before JSON parsing as required, but the handler left the small unauthorized body unread before returning 401. The candidate now discards at most 65,536 raw unauthorized body bytes without parsing or mutation before the error response; the failed run remains retained here.
- Five repeated real-API development runs and the renewed full 24-test suite pass after the bounded discard. CLI help exposes only hidden-prompt or standard-input capability custody and no actor or capability-value option. A follow-up source scan initially passed a pattern beginning with `--actor` to `rg` without the `--` separator and failed as a command-line parse error; the corrected scan found only frozen Bearer rejection tests, the dashboard's negative legacy-label assertion, server-side actor persistence, and the explicit caller-actor rejection.
- Committed the generic candidate and evaluator before held-out execution. The first and only ten-case reveal passes development and test exactness, authentication denial, authorized utility, denied no-mutation, server-derived identity, capability exclusion, and prior-launch rejection at 1.0. Its immutable 17,091-byte result SHA-256 is `45b437b0862a2bb023bc8fa5c09aa2726d0a79cd5a2d4fdebb2d0ce85ecec89a`; median and p95 combined approval/execution latency are 12.034 and 24.325 ms, with zero model calls and cost.
- Integrated the authentication plane into evaluator schema 2.2 and the dashboard, versioned the candidate as 0.0.15/baseline-0015, and froze a 28-entry package contract before archive build. The first integrated test retained the expected old baseline-0014 identity because the candidate manifest had not yet been frozen; no result was promoted.
- Frozen source attempt 001 passed 51 manifest files, 24 tests, and all 84+9+6+10 cases. An initial direct package invocation set only the verifier's `RUNBOOK_SENTINEL_PYTHONPATH` helper rather than Python's import path and failed before evaluation; the documented zipapp invocation then passed.
- The first ad hoc source/package comparison was intentionally too strict and detected different per-run SQLite and trace fingerprints. The declared parity comparison excludes those run-specific bytes and latency, then passes every gate and declared semantic metric.
- The first real source API receipt observed the correct HTTP 401 challenge for missing and wrong capabilities and HTTP 400 for caller `actor`, but Windows PowerShell 5.1 left the three early-response `ErrorDetails` bodies null. The product checks passed; the receipt failed. Adding the same response-stream compatibility fallback already used by prior checks changed only evidence extraction, so the old manifest, source/package evaluations, and 326,418-byte archive at SHA-256 `6edf04a29e871008f948b52bc9e403979ab21a6b53a5f2a4afbd5e8acf805fba` remain retained as superseded attempt 001.
- Corrected manifest attempt 002 passes at SHA-256 `72a180546c6ddbd4ee36fb61b2d49406d0c0666821b7a368b5c73cd4f858225c`. Source and package attempts 002 pass 84+9+6+10, all 24 tests pass, and source/package MCP expose only the same three diagnostic/read tools.
- Two renewed 326,418-byte, 28-entry archives are byte-identical at SHA-256 `0e4d12cd449c8e198ec9434fd12ba3bffc10b8baa609f18c6f71d0d4200da4df`. The first immutable bytes were moved to explicit local attempt-001 names rather than overwritten.
- Source and package real-surface receipts pass authentication, approval, execution, state, audit, telemetry, log, capability-exclusion, and dashboard checks. Both rendered PNGs are byte-identical and visually show Baseline 0015, operator authentication 1.0, authenticated external operator, and disconnected real infrastructure.
- A no-local-object public-branch clone of exact head `fcae2740f968db0ef7f35936feebb30cb156e5a5` began clean with no alternates, passed the full source gate, reproduced the exact selected archive, and passed packaged MCP and real API/state/telemetry/dashboard verification. UNIT-005 is now the next dependency.
- The first pre-merge high-signal scan matched only five package contracts' literal private-key forbidden-pattern definitions. Excluding exactly those definition files left zero credential-shaped hits across 393 tracked files. A following CLI custody scan first used ripgrep lookahead without `--pcre2` and failed as a command parse; the corrected scan found no actor or capability-value CLI option. Both tooling failures remain retained.
- GitHub PR `#13` opened as a draft at exact head `2119b3b42fe43794479ee463e3de798b8e50c886`. Its eight commits and 65 changed paths match the local diff exactly; GitHub reports CLEAN and MERGEABLE with zero configured checks. The pre-merge release audit reports `verified` and authorizes only an exact-head merge followed by merged-main verification before publication.
- The first release-audit schema evaluation rejected descriptive risk-class phrases and `pass` truth-surface states outside the skill's closed vocabularies. Normalizing only those labels to canonical risk names and `current` left every fact, check, evidence item, authority boundary, and decision unchanged; the corrected audit validates.
- PR `#13` merged under expected-head lock `613ff9348e74c015ca13990b567a1ba86e654ae0` as history-preserving merge `824ad5468d420c29d8d0416b15c011fcb531d8e4`, whose parents are exact prior release closure `0dcd245dc286df297d2b431fa7079f65c6793c39` and reviewed head.
- A fresh no-alternates public-main clone began clean and passes 24 tests, all contracts, source and package 84+9+6+10 evaluations, exact archive rebuild, package MCP/API/state/telemetry/logs, parsing of 245 JSON and 59 JSONL files containing 6,885 records, model and high-signal-secret exclusion, and dashboard inspection.
- The first exact-resolution image view displayed an all-dark preview for the same PNG hash previously inspected successfully. Reopening the unchanged file at high detail showed the full correct Baseline 0015 dashboard; this viewer artifact remains retained in the merged-main receipt.
- The final pre-publication audit validates `verified` and releases only the merged-main audit commit, release-closure commit, annotated `v0.0.15` tag, selected `.pyz` and checksum publication, and immediate public verification.
- Prepared the release closure for annotated `v0.0.15`, the selected 326,418-byte zipapp at SHA-256 `0e4d12cd449c8e198ec9434fd12ba3bffc10b8baa609f18c6f71d0d4200da4df`, its adjacent checksum, a non-draft public GitHub release, downloaded-byte verification, rendered public pages, and a fresh public-tag clone. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.

## 2026-08-07 - BASELINE-0014 started

- Resumed from the verified public v0.0.13 release closure rather than restarting architecture work.
- Downloaded and ran the selected public zipapp in `C:\Projects\Verification`. All 28 scenarios, 84 attempts, and existing gates pass; the report and trace remain external with exact SHA-256 receipts.
- Inspected 150 trace events: 84 run, 33 approval, and 33 execution. All approval actors are `frozen-evaluation-harness`; no metric tests whether a cached execution result still requires approval authorization.
- Reproduced a released real-API weakness after one valid execution: wrong and missing approval tokens each returned HTTP 200 with the exact same-key cached success. The unauthorized calls caused no second executor, SQLite, audit, or trace mutation, but disclosed the result and falsely represented authorization success.
- Retained the exact public version, archive, external database and trace paths, hashes, identifiers, terminal counts, and the first command-policy-blocked launch attempt in `artifacts/verification/idempotency-authorization-gap-baseline-0014.json`.
- Selected a bounded policy: a same-proposal cached result requires a token hash matching a consumed approval for that proposal. The original consumed token may retrieve its completed result even after expiry; a new key remains replay-rejected.
- Froze six exact development and held-out cases plus ordered SQLite and trace fingerprints in `eval/idempotency-authorization-contract.json` before runtime implementation. Wrong-token, missing-token, and original-token results are revealed development evidence; the other-proposal, expired-completed, and new-key cases remain held out.
- The independent contract validator passes. `contracts/milestone-0014.json` holds `UNIT-002` in progress and excludes changes to the agent, retrieval, actions, capabilities, executor, schemas, dependencies, external assets, credentials, and real infrastructure.
- The official RFC 9110 source gate passes only for narrow terminology and an explicitly labeled project inference. The official IETF Datatracker labels revision 07 of the Idempotency-Key draft expired and archived; its separate gate is blocked and the document is excluded from downstream use.
- Committed the contract, source decisions, ADR, gap receipt, and milestone as `d88e52d` before changing runtime code.
- Implemented the bounded candidate as one same-proposal cache-hit approval lookup. A cached result now requires a hash-matching approval whose `consumed_at` is non-null; direct execution, expiry, action-hash, executor, and schema paths are unchanged.
- Added development-only checks for wrong and missing same-key tokens, original consumed-token retry utility, exact persisted-table and trace stability, and fail-closed contract corruption. Held-out candidate cases remain unrevealed.
- Both focused development tests, compilation, the independent contract validator, and all 22 existing tests pass before held-out reveal.
- Implemented the isolated six-case real-loopback evaluator without running it. It fingerprints all ordered mutable tables and trace bytes around each retry and reports authorized utility, unauthorized denial, no-mutation, replay, and split exactness separately.
- A static evaluator import check first failed because the ad hoc command omitted `src` from Python's import path. No evaluator case ran and no output file existed. The corrected read-only mapping check passed before reveal; the tooling failure remains retained here.
- The first held-out reveal passed all six frozen cases. Authorized cache utility, unauthorized cache denial, retry no-mutation, new-key replay rejection, development exactness, test exactness, and overall exactness are 1.0. The immutable 10,782-byte result SHA-256 is `90ec001f063d97755014d32c84832687c67b5a3130aca89e57b3c427a26d3306`; no raw approval-token field is present.
- Integrated the isolated plane into evaluator schema 2.1 and the dashboard while preserving separate metrics and a fail-closed release disposition. All 22 tests pass after integration.
- Versioned the candidate as `0.0.14` / `baseline-0014` and froze `eval/package-contract-0014.json` before any archive build. Its 25-entry allowlist adds only the project-authored idempotency-authorization contract and evaluator to the v0.0.13 package surface; contract SHA-256 is `b6d6544e9bf98d1c8201b0970daeb6b5775c8fc92cd6d62f0ef58e3101d29b47`.
- Froze the completed 45-file evaluation surface at manifest SHA-256 `aae568de2095570d6d142bdf9e17828cb77c51e7ced9efef46d82836349cca10`. Compilation, PowerShell verifier parsing, 22 tests, manifest verification, and prebuild package-contract verification pass.
- Immutable version-bound source attempt 001 passes all 84 scenario trials, nine lifetime cases, six cache-authorization cases, and every prior and new hard gate. Its report, trace, and manifest SHA-256 values are `098be4dab2aff8585f9f252de356492ec82b5f6f2d2de881a2b202a8b196164f`, `a29e6d98d0880ba11857e9e54f485345831b72caaac44ecbb9ace601bc13f6f0`, and `aae568de2095570d6d142bdf9e17828cb77c51e7ced9efef46d82836349cca10`.
- Source MCP reports version 0.0.14 and exposes the same three diagnostic/read tools with no approval or execution tool. The real source HTTP/dashboard passes 66 checks, including exact wrong-token and missing-token HTTP 409 errors; 35 SQLite, audit, trace, manifest, and image checks pass.
- Built two independent 283,148-byte zipapps with exactly 25 allowlisted entries and no runtime dependency. Both are byte-identical at SHA-256 `9c9dbcba3b44fe0abb5ef83ac64d413112a64438d8776320f037493db55a3e6f`; the archive validator and CLI help pass.
- Packaged attempt 001 passes all 84+9+6 evaluation cases. Source and package results are exact after removing only latency and per-run idempotency-evaluator database/trace fingerprints. Packaged MCP, 66 HTTP/dashboard checks, 35 state/telemetry checks, and visual inspection pass.
- A no-local-object clone of exact commit `aa0c70b54962594b4c14d2fd5bae390a7c22c0f1` at `C:\Projects\Verification\runbook-sentinel-baseline-0014-aa0c70b-20260807T221516Z` has no alternates and began clean.
- The first manual clone validator sequence omitted `src` from `PYTHONPATH`, so the stale-payload validator failed to import before evaluation. It was rerun with the documented source path and passed; this tooling failure is retained in the clean-clone receipt.
- The clone passes compilation, nine validators, 22 tests, fresh source and package 84+9+6 evaluations, two exact rebuilds at SHA-256 `9c9dbcba3b44fe0abb5ef83ac64d413112a64438d8776320f037493db55a3e6f`, both MCP surfaces, 66 HTTP/dashboard checks and 35 runtime checks per surface, artifact parsing, model/secret exclusion, and protected-boundary comparison.
- Visual inspection confirms the clean-clone packaged dashboard accurately shows Baseline 0014, evaluation pass, cached result authorization 1.0, approval lifetime 1.0, human approval, disconnected real infrastructure, and a mitigated persisted incident. Headless Edge emitted one fallback renderer warning after writing the valid source PNG; the independently written package PNG is also valid.
- Final PR `#12` review bound seven commits and 51 exact paths to head `6a1e166046311f9944fead99cc25e67293fe00c6`. With no configured checks, the independently verified repository gates remained authoritative; the PR was CLEAN and MERGEABLE and merged under exact-head lock as `da084e469534d0a952375a1852b20818480adfd1` with history preserved.
- A fresh public-main clone at the merge has no local object sharing or alternates and began clean. Nine validators, 22 tests, fresh source and package 84+9+6 evaluations, the exact selected archive, both bounded MCP surfaces, 66 HTTP/dashboard checks and 35 state/telemetry checks per runtime, artifact parsing, model/secret exclusion, protected-boundary comparison, and package-dashboard visual inspection pass.
- The first merged-main credential-shaped scan matched only four package contracts' declared forbidden-pattern literals. Excluding exactly those definition files left zero hits; the initial result remains recorded rather than rewritten.
- `artifacts/verification/merged-main-baseline-0014.json` and the final release audit record the exact merge, public clone, archive and evidence hashes, real-surface results, retained Edge renderer warnings, and a `verified` pre-publication decision.
- Prepared the release-closure record for annotated `v0.0.14`, the selected 283,148-byte zipapp at SHA-256 `9c9dbcba3b44fe0abb5ef83ac64d413112a64438d8776320f037493db55a3e6f`, its adjacent checksum, a non-draft public GitHub release, downloaded-byte verification, rendered public pages, and a fresh public-tag clone. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.

## 2026-08-07 - BASELINE-0013 started

- Resumed from the verified public v0.0.12 release closure instead of restarting architecture work.
- Ran the downloaded public zipapp in a fresh external output directory. All 84 existing attempts and every prior metric gate pass.
- Inspected the 150-event trace and found 84 run, 33 approval, and 33 execution events but no invalid approval-lifetime coverage.
- Reproduced a released real-API failure: `ttl_seconds: -1` is accepted with HTTP 201 before mutation, creates an already-expired approval, makes execution fail, prevents reapproval because the proposal is no longer pending, and leaves the incident open.
- Retained the released failure, exact external paths, hashes, terminal state, and an earlier command-policy-blocked launch attempt in `artifacts/verification/approval-lifetime-gap-baseline-0013.json`.
- Selected a bounded policy: approval TTL must be a JSON integer excluding booleans, from 1 through 300 seconds inclusive, with 300 as the default. Invalid values must return HTTP 400 before approval-related mutation.
- Froze nine exact development and held-out cases in `eval/approval-lifetime-contract.json` before changing runtime code. The known negative-TTL case is development evidence; held-out candidate results remain unrevealed.
- Created `contracts/milestone-0013.json` with resume point `UNIT-003`. The agent, retriever, proposal schema, capabilities, executor, idempotency, replay, postconditions, and real-infrastructure boundary remain out of scope.
- The first frozen nine-case reveal passed every development and held-out real-API, SQLite, audit, trace, no-mutation, and exact-lifetime check. Its immutable result remains at `artifacts/evaluations/runs/baseline-0013-approval-lifetime-attempt-001.json`.
- The first two integrated full-test attempts then failed on adjacent verifier false positives: substring assertions treated the boolean check name `approval_token_hashed` as raw approval material in the metric and full report. The retained evaluation contains no approval token value; both assertions were narrowed to reject an actual serialized `approval_token` field before rerunning the full suite.
- Versioned the candidate as v0.0.13/baseline-0013 and froze `eval/package-contract-0013.json` before any archive build. The contract adds only the project-authored approval-lifetime module and frozen contract to the existing dependency-free zipapp allowlist; it preserves the v0.0.12 package contract and explicitly records that runtime implementation and the nine-case reveal already preceded this packaging contract.
- Version-bound source attempt 001 passed the 40-file manifest, eight validators, 21 tests, all 84 scenario attempts, and all nine approval-lifetime cases. An earlier launch lost its output after the orchestration shell timed out while child processes continued; no immutable attempt files existed, so the completed captured launch became attempt 001.
- Live API verifier attempt 001 then failed before grading the product because Windows PowerShell 5.1 left the HTTP 400 `ErrorDetails.Message` null and `ConvertFrom-Json` rejected it. The failure is retained at `artifacts/verification/live-api-baseline-0013-attempt-001-failed.json`; the verifier now reads the response stream as a compatibility fallback, which requires a new frozen manifest and source attempt.
- Version-bound source attempt 002 passed under corrected 40-file manifest SHA-256 `249513aca8911a1a574a48e2546458db0b3d1e37ecdd4416db40cbb8638bc4c9`. Source MCP, 58 HTTP/dashboard checks, 31 persisted-state/telemetry checks, and visual inspection pass.
- Built two independent 257,847-byte zipapps with exactly 23 allowlisted entries and no runtime dependencies. Both are byte-identical at SHA-256 `c14a4559f3cfc4f53d5ce501115747252c9f33e7f299eb34f088c605930fbd41`.
- The packaged evaluator passes all 84 scenario attempts and all nine approval-lifetime cases. Source and package results are exact after removing only declared latency fields. Packaged MCP, 58 HTTP/dashboard checks, 31 persisted-state/telemetry checks, CLI help, and visual inspection pass.
- The first ad hoc precommit secret scan falsely matched the package contracts' literal forbidden-pattern labels. JSON/JSONL parsing, the package validator's content-aware secret exclusions, and a corrected scan requiring credential-length tokens or an anchored private-key header pass; no secret-shaped value was found.
- The first feature-branch push failed before remote mutation when the shell temporarily could not resolve `github.com`. DNS recovered on the read-only reachability check and the exact branch push then succeeded.
- A fresh public-branch clone of exact candidate `2f50f5e8d2098d593fea1d5fefa2ca846422fe9f` has no object alternates and passed compilation, eight validators, 21 tests, fresh source and package evaluations, exact archive rebuild, packaged MCP, 58 HTTP/dashboard checks, 31 runtime checks, and visual inspection. The retained receipt advances the candidate to GitHub review.
- A second no-alternates clone of final review head `536378ced68d0e145e3be836adbe25d2b7f83535` passes eight validators, 21 tests, and exact selected-archive rebuild. GitHub PR `#11` is open, non-draft, mergeable, contains five expected commits, lists the same 49 paths as the local diff, and has zero configured commit statuses.
- A direct read-only `git ls-remote` check then hit a transient GitHub DNS-resolution failure while the authenticated GitHub PR surface remained available. The failed probe is retained in the task record; the remote and receipt-only PR head must be rechecked before merge.
- The pre-merge release audit computes `verified` and releases only the inherited receipt commit, push, expected-head merge, merged-main verification, and later publication actions. It does not treat GitHub's provisional open-PR merge SHA as a completed merge.
- Evidence-only audit head `5c4a358a3f85c21ccb27c19efcb791e5b06be283` reconciled across local, remote branch, and GitHub with six expected commits, 50 expected paths, zero configured statuses, and a conflict-free merge tree. GitHub briefly returned non-mergeable during post-push recalculation, then returned to mergeable without a source change.
- PR `#11` merged under the exact expected-head lock with history preserved as `54c56411ea0ff3e1b17743fcbd8ebc225dabaabb`; its parents are exact prior main `9829f4e2309b9ebde83f7e1fc4f53f9e7e408b2a` and reviewed head `5c4a358a3f85c21ccb27c19efcb791e5b06be283`.
- A fresh no-alternates public-main clone passes compilation, eight validators, 21 tests, 202 JSON files, 53 JSONL files with 5,985 records, model and corrected high-signal-secret exclusion, source and package 84+9 evaluations, exact archive rebuild, packaged MCP, 58 API/dashboard checks, 31 state/telemetry checks, and visual inspection.
- The first merged-main interface-discovery command passed `--help` to a script without a help parser, began verification, and was stopped by the short discovery timeout before an immutable result. The explicit complete rerun passed. Headless Edge later emitted a renderer warning only after writing the correct PNG; both observations remain retained.
- The final pre-publication audit computes `verified` with no unresolved changed-risk or truth-surface discrepancy and releases only the inherited final-audit, release-closure, annotated-tag, asset-publication, and immediate public-verification actions.
- Prepared the release-closure record for annotated `v0.0.13`, the selected 257,847-byte zipapp at SHA-256 `c14a4559f3cfc4f53d5ce501115747252c9f33e7f299eb34f088c605930fbd41`, its adjacent checksum, a non-draft public GitHub release, downloaded-byte verification, rendered public pages, and a fresh public-tag clone. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.


## 2026-08-06 — BASELINE-0001 started

- User approved the long-running goal with the SRE domain.
- Live inspection found the OneDrive workspace contained only an unborn Git shell and no authoritative project history.
- User directed active work away from OneDrive and toward `C:\Projects` plus GitHub ownership under `drwbkr1`.
- `C:\Projects\STORAGE_POLICY.md` required the authoritative worktree under `C:\Projects\Active\runbook-sentinel`.
- Initialized an empty `main` repository at the policy-compliant path and left the OneDrive shell intact.
- GitHub CLI is authenticated as `drwbkr1`; remote creation is paused on the required visibility decision.
- Docker Desktop start was requested after live inspection showed the daemon was off.
- Began a standard-library-only deterministic control baseline. No local model or external package has been used.

### Retained verification attempt 001

- Milestone contract validation failed because the active unit used `active`; the validator requires `in_progress`.
- All six tests reached their assertions, but Windows cleanup failed because SQLite context managers committed without explicitly closing connections.
- Disposition: `remediate`. The failed output remains in the Codex execution record; both infrastructure defects were corrected before rerun.

### Retained container source-gate attempts

- Docker Desktop became live at engine version 29.4.3.
- The first parallel Scout scan attempt contended on its cache and was rejected as evidence.
- Sequential scans excluded three immutable Docker Official Python image digests: slim Trixie and Bookworm for two critical plus two high unfixed Perl findings each, and Alpine for two high unfixed SQLite findings.
- Disposition: `defer`. Removed the unaccepted Dockerfile and Compose file; the native baseline remains eligible. Exact evidence is in `artifacts/verification/container-source-gate.json`.

### Retained live-API tooling attempt

- A long inline PowerShell verification command was rejected by the shell safety layer before execution.
- No server or synthetic mutation occurred in that attempt.
- Disposition: `remediate` by moving the check into the reviewable `scripts/verify_live_api.ps1` workflow.

### Source-review hardening before checkpoint freeze

- Bound each idempotency key to one proposal and serialized the cache check inside the execution transaction.
- Moved execution audit insertion into the same transaction as approval consumption and state mutation.
- Restricted the baseline HTTP server to loopback addresses.
- Made raw evaluation attempts immutable and separated them from the mutable latest-passed pointer.

### Native candidate verification

- Regenerated and verified a nine-file frozen manifest after hardening.
- Six focused unit/integration tests passed.
- Immutable attempt 002 passed all 27 repeated evaluations across nine scenarios; it was promoted to the latest-passed pointer.
- Live CLI correctly rejected stale deployment state as sufficient evidence.
- Real MCP stdio negotiation and tool calls passed with no approval or execution tool.
- Real HTTP approval and execution passed hash binding, idempotency, replay rejection, pre/postconditions, and trace-redaction checks.
- Rendered and visually inspected the 1440x1000 dashboard.
- Reconciled SQLite, atomic audit records, JSONL traces, evaluation, manifest, and rendered artifact into `artifacts/verification/native-baseline.json` with disposition `pass`.

### Pre-commit whitespace gate

- `git diff --check` found trailing blank lines in newly authored text files.
- Disposition: `remediate`. Preserved attempt 002's manifest, normalized only reported files, and required a new frozen manifest and evaluation attempt before commit.

- Attempt 003 passed all gates against the normalized nine-file manifest and became the accepted latest result. Attempts 001 and 002 remain retained as superseded evidence.
- Generated evaluation, trace, manifest, and verification-receipt paths are binary/byte-preserved in `.gitattributes` so Git cannot invalidate their recorded hashes or treat carriage returns as source whitespace during commit checks.

### Verified checkpoint closure

- The complete staged baseline passed Git whitespace validation and a repository secret-pattern scan.
- Created root candidate commit `c5c78d1` (`bootstrap deterministic control baseline`).
- Cloned that candidate without local object sharing to `C:\Projects\Verification\runbook-sentinel-baseline-0001-c5c78d1-20260806145334`.
- The clean clone passed compilation, all six tests, frozen-manifest integrity, milestone-contract validation, MCP stdio negotiation, live loopback API approval/execution/replay checks, dashboard rendering, SQLite persistence inspection, and JSONL telemetry inspection.
- `BASELINE-0001` disposition: `pass`. GitHub publication remains stopped at the explicit repository-visibility gate. Container packaging remains `defer`, not silently omitted.

### Public repository gate released

- The user explicitly selected public visibility on 2026-08-06.
- Created `https://github.com/drwbkr1/runbook-sentinel` under the authenticated `drwbkr1` account with public visibility.
- Pushed verified closure commit `061f0aefa0a4c61e0ed14d589e2d3bcee5ec413e` to the new `main` branch and verified the repository visibility, default branch, and remote commit through GitHub.
- No license grant was added; public visibility and open-source licensing remain distinct.

## 2026-08-06 — BASELINE-0002 started

- Began from verified public release `v0.0.1`; local tag, remote tag, and remote `main` all resolve to `5a3cf1e708d6e21c17a0c3c0587db9c271e8ff9d`.
- The accepted nine-case suite still passed all 27 orientation trials, but it had no explicit topology coverage metric and no gateway or configuration cases.
- Precommitted schema 1.1 with 16 cases across gateway, API, worker, database, cache, deployment, configuration, and observability before changing the implementation.
- The unchanged control failed all 21 trials belonging to the seven new cases. Attempt 001 retained exact match and `pass^3` of 0.5625, benign utility of 0.5556, adversarial safe outcome of 0.5714, policy compliance of 1.0, and proposal attack success of 0.0.
- Disposition: `remediate`. The change remains bounded to non-action diagnoses and exact evaluation coverage; executor actions and capabilities are frozen.
- A no-replace PowerShell guard emitted a parameter-binding error because it repeated `-LiteralPath`; the three targets were independently absent and each immutable evidence file was created once.

### Retained verification attempt 002

- Attempt 002 passed all exact scenario, split, topology coverage, policy, security, and reliability gates.
- Live CLI inspection then showed the service emitted `deterministic-control-v1` while the evaluator declared `deterministic-control-v2`; the artifact is therefore superseded rather than accepted.
- The live API database and trace also contained records from prior runs because the verifier reused generated files. The verifier now removes only its exact generated database, sidecars, trace, and log files before startup.
- Disposition: `remediate`. Updated the service identity and required a new immutable evaluation plus isolated real-surface run.

### Candidate verification attempt 003

- Attempt 003 emitted and declared `deterministic-control-v2` and passed all 48 trials across 16 cases.
- Development and test exact gates each passed at 1.0; topology coverage reached 8 of 8 domains; policy compliance remained 1.0; proposal attack success remained 0.0.
- Median latency was 7.771 ms and p95 latency was 19.739 ms with zero model calls and zero estimated spend. These single-run values do not support a performance-improvement claim.
- The isolated live API run produced exactly one incident, run, proposal, approval, idempotency record, and execution; replay returned HTTP 409 and traces contained no approval token.
- Live CLI, MCP stdio, rendered dashboard, SQLite, audit, JSONL telemetry, manifest binding, JSON parsing, secret-pattern scan, and unchanged executor policy checks passed.
- Preserved checkpoint-specific v0.0.1 and baseline-0002 dashboard and runtime receipts before updating the living pointers.

### Clean-clone candidate gate

- Committed the complete implementation and evidence candidate as `c080e83617636b81e67c48956be27056972d71f4`.
- Cloned that branch without local object sharing to `C:\Projects\Verification\runbook-sentinel-baseline-0002-c080e83-20260806153016`.
- The exact commit passed compilation, six tests, manifest integrity, both milestone contracts, MCP, isolated API approval/execution/replay, dashboard rendering, SQLite, telemetry, JSON parsing, secret scanning, and diff checks.
- Publication remains in progress; a clean clone is necessary but does not by itself prove the GitHub branch, pull request, merged `main`, tag, or release.

### Verified checkpoint publication

- Pushed final branch head `da3ba22e76ccb068a132f29e1c55c04f88f67035` and verified the remote branch identity.
- Opened GitHub pull request `#1` as a draft, inspected its 42-file server-side scope, confirmed `CLEAN` and `MERGEABLE` state, then marked it ready.
- Merged with history preserved as `53fc4ce1189218fc0ea13899aa8bb83552ffa4af`; the merge parents are `v0.0.1` release commit `5a3cf1e` and verified branch head `da3ba22`.
- Fast-forwarded the authoritative local `main` to the remote merge commit and reran tests, manifest validation, MCP, isolated API approval/execution/replay, dashboard rendering, SQLite, audit, and telemetry checks successfully.
- `BASELINE-0002` disposition: `pass`. Failed attempt 001 and superseded attempt 002 remain retained; container packaging remains `defer`.

## 2026-08-06 — BASELINE-0003 started

- Resumed from public `v0.0.2` at `a320d36a933e80e5f22b5776be4ce39fa40cd530`; release identities and a fresh 48-attempt orientation evaluation passed.
- Four instruction-bearing poisoned runbooks were retrieved into the context passed to the agent in all four labeled attack cases, for exposure rate 1.0, even though proposal attack success remained 0.0.
- Rechecked the AgentDojo and CaMeL primary papers. They support measuring attacks over untrusted tool data and separating untrusted data from trusted control flow; the proposed evidence-only decision context is a narrower project-specific application, not a claim to implement CaMeL.
- Froze explicit attack-document identities before changing the evaluator or runtime.
- The orientation PowerShell guard repeated invalid `Test-Path` syntax; ignored target paths were absent and created once. The defect is retained and the syntax will not be reused.

### Retained full-context control attempt 001

- Extended the exact evaluator to label instruction-bearing attack documents and measure whether they enter the decision context.
- The unchanged v0.0.2 service passed every prior quality, reliability, and action-security gate but exposed an attack document in all 12 trials across four labeled cases.
- Full-retrieved-context-v1 results: exposure 1.0, required evidence recall 1.0, exact trajectory 1.0, `pass^3` 1.0, policy compliance 1.0, proposal attack success 0.0.
- Disposition: `remediate`. The control artifact and manifest are immutable and were not promoted to the latest-passed pointer.

### Candidate evidence-only attempt 002

- Added a deterministic context policy with reproducible `full-retrieved-context-v1` and `evidence-only-context-v2` configurations.
- The candidate retains full `retrieved_document_ids`, records excluded runbook identities as guidance, and passes only telemetry/status documents into the decision agent.
- Attempt 002 passed all 48 trials. Instruction-bearing decision exposure fell from 1.0 to 0.0 while recall, generation, exact trajectory, utility, policy compliance, adversarial safe outcome, and `pass^3` remained 1.0; proposal attack success and cost remained 0.0.
- Live CLI and MCP verified that the poisoned worker runbook remained auditable but absent from `decision_document_ids`. Redacted traces recorded the selected context policy and document counts.
- Isolated API approval/execution/replay, dashboard, SQLite, audit, telemetry, and manifest-binding checks passed. The first baseline-0003 runtime receipt is retained; attempt 002 adds the explicit decision-context check.
- The selected boundary depends on trusted project-authored document-kind metadata in this synthetic baseline. It is not a general sanitizer and does not yet validate hostile misclassification.

### Complete-gate regression attempt 003

- Compilation, seven tests, the frozen manifest, all three milestone contracts, JSON and JSONL parsing, executor-policy identity, secret-pattern scanning, and Git whitespace checks passed.
- The complete gate created a separate immutable attempt rather than overwriting attempt 002. Attempt 003 passed all 48 trials with decision exposure 0.0, proposal attack success 0.0, exact quality and reliability metrics 1.0, median latency 7.391 ms, and p95 latency 18.075 ms.
- Reran MCP and the isolated API against the promoted attempt. Full retrieval audit remained present, the poisoned runbook stayed outside the decision context, approval/execution/replay/postcondition checks passed, and the rendered dashboard, SQLite state, audit log, and telemetry reconciled.
- Preserved attempt-003-specific evaluation, trace, manifest, dashboard, and runtime receipt. Attempt 001 remains `remediate`; attempt 002 remains the accepted comparison candidate.

### Clean-clone candidate gate

- Created candidate commit `0aa4fc68bfcf208c2f64a3578e54727a55002ef3` and cloned it without local object sharing to `C:\Projects\Verification\runbook-sentinel-baseline-0003-0aa4fc6-20260806160053`.
- The exact commit passed compilation, seven tests, frozen-manifest integrity, all three milestone contracts, unchanged executor-policy identity, MCP negotiation, isolated API approval/execution/replay, rendered dashboard, SQLite, audit, telemetry, JSON and JSONL parsing, secret-pattern scanning, and diff checks.
- The live verifier changed only the clone's living dashboard and runtime-receipt pointers, as expected. GitHub review, merged-main verification, tag, and release remain pending.

### GitHub review and merged-main verification

- Pushed final branch head `c5fc8f5cdb80436af5ebead18a665b6878f8db28`; the remote branch matched exactly and passed a no-local-object exact-head regression.
- Opened pull request `#2` as a draft, inspected its server-side four-commit and 45-file scope, confirmed `CLEAN`, `MERGEABLE`, and no required checks, then marked it ready.
- Merged with history preserved as `995dcefb1cef284b936a5b336c4601c54bad2462` and fast-forwarded the authoritative local `main` to the exact remote commit.
- Merged main passed compilation, seven tests, manifest integrity, all contracts, unchanged executor policy, MCP, isolated API approval/execution/replay, postconditions, rendered dashboard, SQLite, audit, and telemetry checks.
- Preserved merged-main dashboard and runtime receipts. The exact closure commit is eligible for the `v0.0.3` tag and public release.

## 2026-08-06 — BASELINE-0004 started

- Resumed from exact public `v0.0.3` closure `fa2eb78eb497617b1c775ebd2b609f1216acb8d8` after tag, remote `main`, release API, rendered README, and corrected rendered release notes agreed.
- Identified the next measurable gap: the accepted deterministic agent has no stochastic structured-generation comparison.
- Applied the external-source gate before model use. Ollama 0.32.5 is signed by Ollama Inc. and responds only through the approved loopback endpoint. The local `llama3.2:3b` manifest is `a80c4f17acd55265feec403c7aef86be0c25983ab279d83f3bcd3abbcb5b8b72`; all six referenced blobs match their declared SHA-256 digests.
- Official Meta license, AUP, and model-card evidence and Ollama release, license, and registry evidence passed for synthetic evaluation only. No download, package, account, secret, remote API, model-weight redistribution, tool access, approval access, executor access, or real infrastructure is authorized.
- The first UTC timestamp command used unsupported Windows PowerShell `Get-Date -AsUTC` syntax and failed without mutation. Replaced it with `ToUniversalTime()` and retained the failure in the milestone contract.
- Source gate disposition: `ready`. The gate authorizes a standard-library loopback adapter and frozen synthetic invocation; it does not establish model quality or permission to operate critical infrastructure.

### Retained pre-model specification regression

- The first 18-case regression failed before any model invocation. A period immediately after `telemetry_coverage=0.12` was captured as part of the value by the existing fact lexer and could not be converted to a float; the MCP inventory test also retained the old 16-case assertion.
- Disposition: `remediate`. Replaced prose-boundary periods with semicolon delimiters in both new in-band records and updated the exact MCP inventory assertion to 18. The failed test output remains recorded here and in the milestone contract.

### Deterministic comparison control

- Committed the model contract and 18-case frozen schema before any model invocation. The two new cases place allowed-action injection prose inside telemetry/status records, so decision exposure is intentionally 1.0 rather than hidden by guidance filtering.
- Immutable attempt 001 passed all 54 deterministic trials. Development and test exact results, policy compliance, benign utility, adversarial safe outcome, and `pass^3` were 1.0; in-band and overall proposal attack success were 0.0.
- Median latency was 8.160 ms and p95 latency was 28.367 ms with zero model calls and zero estimated monetary spend. The retained manifest SHA-256 is `ae322324f034595c4374fdf24e3d285e678f7d52e91ced0be030bf40fc33b7fe`.

### Pre-model adapter boundary verification

- Implemented an optional standard-library Ollama adapter while retaining `deterministic-control-v2` as the default. The adapter accepts only the frozen direct loopback endpoint and disables proxies, redirects, streaming, and tools.
- The exact parser accepts only the six frozen top-level fields, evidence identifiers from the supplied decision context, and one of three server-known action/capability pairs with empty arguments. Invalid, unavailable, or timed-out output becomes an abstention and never falls back to the deterministic control.
- Redacted trace and immutable evaluation records identify the model manifest, runtime, contract, system prompt, request payload, raw output digest, parse disposition, tokens, durations, and end-to-end latency without storing raw generated text in telemetry.
- Twelve tests passed before the first model invocation. They cover valid proposal integration, malformed output, mismatched capabilities, timeout, missing model identity, remote endpoint rejection, trace redaction, all 18 deterministic scenarios, and candidate-level metric accounting.
- Before invocation, the evaluation record was extended to preserve the complete validated semantic decision separately from the raw-output digest; raw response bytes remain absent from traces.
- Review identified that the standard URL client would otherwise follow redirects. Redirects and environment proxies were disabled before any model invocation so the direct loopback boundary is enforced by transport behavior as well as contract validation.
- Executor policy remains byte-identical to the `v0.0.3` checkpoint at SHA-256 `1b23a56b14527347ec723a4c83595414987f9ef7288b532afda49fb4fb7bd1aa`.

### First local-model smoke call

- Invoked the exact source-gated local model on `dev-worker-backlog` only after committing the adapter and candidate manifest. The request contained one frozen synthetic telemetry record and no tools, credentials, approvals, executor access, remote service, or real infrastructure.
- The model response had raw-output digest `b267a73ec4a4391d2a69fe8554f925497c5b3982fb168c857c69054c48464c98` but failed the exact semantic parser. The adapter returned `abstain / model_output_invalid`, persisted no proposal, and did not substitute the deterministic result.
- End-to-end latency was 25138.148 ms, including 6681307500 ns reported load duration. The result disposition is `remediate`; it remains retained independently of the complete repeated comparison.

### Complete local-model comparison and selection

- Ran all 18 frozen cases three times through the exact committed adapter and model contract. The report, 54 redacted traces, and candidate manifest are immutable; their SHA-256 digests are `c3cd0f15435a2e4f993c7cbb6f16a12ad4e8bba35f585e221c1fa2a87f5c3433`, `94accdb4b2a74d0c4b2b925e4fe08bdccc1d4c1adcee39ae8bf2733fb0abcf84`, and `edc4cd30f18863260331c04ccad80a692481da86ec4e6afc48d7fe566b2138d1`.
- Candidate parse success was 5/54, outcome accuracy 0.1481, diagnosis accuracy 0.0, exact trajectory 0.0, benign utility 0.0, and `pass^3` 0.0. Development and held-out test exact trajectories both remained 0.0.
- Retrieval recall remained 1.0. Policy compliance remained 1.0 and proposal attack success remained 0.0 because 49 invalid outputs failed closed and the five valid outputs produced no accepted action proposal. This is boundary evidence, not a model-safety claim.
- Candidate median latency was 12448.711 ms and p95 latency was 19687.913 ms across 54 calls, versus control values of 8.160 ms and 28.367 ms. External API billing was zero; local hardware and energy were not estimated.
- The candidate produced 27 distinct raw-output digests. Nine cases were byte-semantically stable by digest across all three trials and nine varied. Raw generated text is absent from traces; validated semantic decisions remain in the evaluation artifact.
- Disposition: `exclude`. The deterministic control remains the default and the latest-passed pointer remains byte-identical to its passing report.

### Native release-candidate verification

- Bumped versioned runtime surfaces to `0.0.4` and the dashboard/API checkpoint to baseline 0004 without changing the executor action set. Regenerated the 11-file manifest and ran deterministic attempt 002 against the exact current runtime.
- Attempt 002 passed all 54 trials with exact trajectory, policy compliance, benign utility, adversarial safe outcome, and `pass^3` at 1.0; proposal attack success remained 0.0. It became the latest-passed pointer. Median latency was 9.009 ms and p95 was 20.500 ms.
- The real CLI handled the held-out in-band observability action injection by requesting evidence. MCP negotiated protocol `2025-11-25`, reported server version `0.0.4`, retained full retrieval audit, excluded poisoned guidance from the decision context, and exposed no approval or execution tool.
- The isolated API reported baseline 0004 and served the passing deterministic evaluation. Hash-bound approval, synthetic execution, exact postconditions, idempotent replay under the same key, HTTP 409 rejection under a new key, and approval-token trace redaction all passed.
- Visually inspected the 1440 by 1000 rendered dashboard. It accurately shows Baseline 0004, passing deterministic v2, evidence-only context, human approval, disconnected real infrastructure, and one persisted mitigated incident.
- SQLite contained exactly one incident, run, proposal, approval, idempotency record, and execution plus three ordered audit events. The approval stored a 64-character hash, was consumed, and matched the proposal action hash.
- Docker Desktop 4.74.0 and Engine 29.4.3 were live. Container packaging remains `defer`: daemon availability does not override the retained failure of all reviewed base-image candidates to pass the source gate.

### Clean-clone candidate gate

- Created candidate commit `1b886c5557d311a272997866a715c2f8f815d76e` and cloned it without local object sharing to `C:\Projects\Verification\runbook-sentinel-baseline-0004-1b886c5-20260806210458`.
- The exact clone passed compilation, 12 tests, the 11-file manifest, all milestone contracts, the model source gate, candidate and selected evaluation artifact hashes, unchanged executor policy, model-weight exclusion, JSON and JSONL parsing, and diff checks.
- Real MCP and API verification passed again. A fresh dashboard render was visually inspected, the held-out in-band CLI case requested evidence, and the only tracked post-verification change was the intentionally regenerated dashboard PNG containing a new synthetic incident identifier.
- GitHub review, merged-main verification, exact release closure, tag, release, and rendered public-page verification remain pending.

### GitHub review and merged-main verification

- Pushed verified branch head `36cfcba4c7a1b333325b196481d9f9bea6357e35` and verified the remote branch identity. Pull request `#3` contained 9 commits and 45 expected files, was `CLEAN` and `MERGEABLE`, had no configured checks, and contained no model weights or expanded authority surface.
- Marked PR `#3` ready and merged with history preserved as `fca61e3f4b1a52e525477002c3977a15aab0cd8f`. Its parents are the exact `v0.0.3` closure and verified baseline-0004 branch head.
- Fast-forwarded authoritative local `main` to remote `main`. Compilation, 12 tests, the manifest, all contracts, the model source gate, selected-evaluation identity, and unchanged executor policy passed.
- Cloned remote merged main into `C:\Projects\Verification\runbook-sentinel-merged-main-fca61e3-20260806210929`. MCP, API evaluation serving, approval, execution, idempotency, replay rejection, postconditions, SQLite, telemetry, and a fresh visually inspected dashboard passed again.

### Verified checkpoint publication

- Created one release-closure commit after merged-main verification. The annotated `v0.0.4` tag, peeled remote tag, remote `main`, and public non-draft GitHub release bind that exact commit.
- Verified the public repository README and release page after publication. Both describe the candidate as excluded, preserve the deterministic default, and make no claim of production readiness or useful model safety.
- `BASELINE-0004` disposition: `pass`; model candidate disposition: `exclude`; container disposition: `defer`. No model weights, runtime binaries, paid services, secrets, new executor capability, real infrastructure connection, or default model change shipped.

## 2026-08-06 — BASELINE-0005 started

- Resumed from exact public `v0.0.4` closure `e364425d08e8e931b2b2f5a7c6be83651dd4931c`; local main, remote main, annotated tag, peeled remote tag, public release, clean worktree, 12 tests, 11-file manifest, and Docker Engine 29.4.3 agree.
- Inspected the accepted evaluation and source. `trajectory_exact` is assigned directly from generation agreement, while `evaluation.py` contains no approval or execution call and the 18-case schema contains no expected terminal-state field.
- The gap affects five actionable cases, 15 repeated trials, and all three executor actions. The remaining 39 trials also lack an explicit exact no-mutation grader.
- Ran a temporary synthetic orientation harness across `restart_worker`, `rollback_deployment`, and `warm_cache`. Every action verified postconditions, exact mitigated state, same-key idempotency, and different-key replay rejection; approval material remained absent from the agent result.
- Rechecked the tau-bench primary paper. Its deterministic final-database-state comparison and repeated `pass^k` framing support the bounded grader; no external code, data, model, or dependency is imported.
- Corrected the living evaluation report's unsupported terminal-state claim without rewriting the immutable baseline-0004 artifacts. Disposition: `remediate` through an evaluation-only harness; runtime authority and policy remain frozen.
- Froze schema 1.4 before evaluator implementation. Every one of the 18 cases now names an exact full terminal state, incident status, and either the approved-execution or no-execution harness trajectory; the validator passes at 5 actionable and 13 no-action cases across all three existing actions.
- Preserved the accepted baseline-0004 report by hash in a separate pre-change control receipt. It records the favorable proposal proxy alongside the actual 0 approval calls, 0 execution calls, 0 terminal-state grades, and 0 explicit no-mutation grades.
- Bound the terminal contract, its validator, and the unchanged pre-change evaluator to an immutable 13-file manifest with SHA-256 `c458a2ed2af1a6b4324c58f1ac438bbd8cb816938201e5fc8f573b32f8329b8f`. UNIT-002 passed; UNIT-003 began.
- A verification command named a nonexistent `scripts/validate_milestone_contract.py` and failed without mutation. A first replacement assertion then expected a nonexistent nested `authority.status` rather than the contract's top-level status and also failed without mutation. The repository has no dedicated milestone validator; JSON, dependency ordering, inherited authority, unit state, terminal contract, manifest, and tests passed through the actual available checks.
- Implemented the evaluation-only harness against the real `approve` and `execute` service methods. It runs only after result persistence, uses disposable SQLite state, and records no raw approval token or idempotency key in the evaluation report or traces.
- Separated proposal, actual tool trajectory, terminal state, policy, security, reliability, end-to-end latency, and cost metrics. End-to-end latency now includes approval, execution, idempotency, replay, state, audit, trace, and boundary inspection instead of diagnosis alone.
- Added a focused adversarial grader regression: `rollback_deployment` actually executes in the in-band worker case and counts as terminal attack success, while the same proposal against an already-stable deployment is blocked by preconditions and does not count as terminal attack success.
- Thirteen tests pass. A 54-trial implementation smoke passed 15 of 15 expected-action executions across all three action types and 39 of 39 strict no-action checks; the report and all 84 trace events contained no approval-token literal.
- The first post-implementation manifest had 13 files and SHA-256 `0d77a9d669eb03ea9817eaeb1669bb08fbebb13843cee6824cfd6711401b12fa`. Pre-attempt review caught that `scripts/verify_baseline.py` still named baseline 0004; no immutable attempt had been created or promoted.
- Corrected the driver to baseline 0005, made terminal-contract validation mandatory, added the driver to the manifest, and refroze the final 14-file evaluation surface at SHA-256 `713361860a9d1896e0ce1375ba8578db3322e920c915340cb0d0382bd8aa1392`. Policy, service, API, and MCP hashes remain unchanged from v0.0.4. UNIT-003 passed; UNIT-004 began.
- Immutable attempt 001 passed all 54 trials and became the latest-passed pointer. All 15 expected-action trials executed across all three action types, all 39 expected no-action trials remained open and exactly unchanged, and development and held-out test `pass^3` were 1.0.
- The 84-event telemetry stream contains 54 runs, 15 approvals, and 15 executions. Report and traces contain no raw approval token or concrete `baseline-0005:{scenario}:{trial}` idempotency value. Report, trace, and manifest SHA-256 are `b3079ffcf29b8c6c44ebe0f1fda167cd7ffb6c32f9c15c37eca21b6f7546543e`, `9819b78a58ed31e58120b4ac9135b9a3be520a0b78f3ec8f85da383ddb3eb1e5`, and `713361860a9d1896e0ce1375ba8578db3322e920c915340cb0d0382bd8aa1392`.
- End-to-end median and p95 latency were 56.022 ms and 97.946 ms, versus diagnosis-only 5.288 ms and 14.148 ms. The measurement now includes the reliability work rather than presenting it as diagnosis latency. UNIT-004 passed; UNIT-005 began.
- Bumped package, API health, MCP server, CLI default, tests, README, and rendered-dashboard identity to `0.0.5` / baseline 0005. The dashboard now exposes actual trajectory and terminal-state exactness as evidence, while approval and execution remain absent from the agent and MCP surfaces.
- Expanded the release-candidate manifest from 14 to 17 files so it binds package metadata, package version, CLI, and the evaluation driver. The exact manifest SHA-256 is `c8a4797dbdde2bc53ff9057bd1953bbfda925149b28c8a538f1d155334757310`.
- Immutable attempt 002 passed the versioned 17-file surface: all 54 trials, 15 expected executions, 39 strict no-action results, three action types, exact security and reliability gates, median end-to-end latency 58.510 ms, and p95 108.435 ms. Report and trace SHA-256 are `b1e3eae0cf0ea8a558a7afc5bec4f82616ff811d81006c6f6c73156d8790a3ba` and `9b8aa00f2aa2619c2fd935f54d7d2184f93d53314a140ddaa8fe70c763cf55a1`.
- Before live verification, review found baseline-0004 paths and MCP version expectations in three real-surface verifier scripts. No live receipt had been created. Attempt 002 is retained as pass but superseded for release selection; the corrected API, MCP, persistence, telemetry, and dashboard verifiers will be bound into a 20-file manifest and require attempt 003.
- Immutable attempt 003 passed all 54 trials against the first 20-file manifest. The direct held-out CLI case requested evidence, while MCP negotiated protocol `2025-11-25`, reported version 0.0.5, retained full retrieval audit, excluded poisoned guidance from the decision context, and exposed no approval or execution tool.
- Two attempts to combine CLI, MCP, and nested PowerShell verification were rejected by local command policy before execution. Splitting the calls removed the tooling issue without changing project behavior.
- Live API attempt 001 completed the synthetic action: hash-bound approval, execution, exact postconditions, same-key cache, different-key HTTP 409 replay rejection, persisted mitigated state, three ordered audit events, and redacted three-event telemetry all passed. The dashboard rendered baseline 0005 and its terminal metric. The verifier still returned failure because its unnamed aggregate boolean evaluated false.
- Preserved the failed attempt's database, trace, stdout, stderr, and 1440 by 1000 dashboard under attempt-specific names and added `artifacts/verification/native-baseline-0005-attempt-001-failed.json`. No pass receipt was promoted.
- Replaced the ambiguous boolean array with named typed checks and an explicit receipt status. Refroze the manifest-bound verifier surface at SHA-256 `8f3e0a8710abdfd3894047c451ffc23f3a1488b836dbe510cfab7832b2549267`; attempt 004 is required before rerun.
- Immutable attempt 004 passed the corrected 20-file manifest and all 54 evaluation trials. It became the latest-passed pointer with report SHA-256 `132306bbf9f8619e61dee1d74f1a9e7ef208b0b8b178b672e51c42d3751b99f1`, trace SHA-256 `d6ae7e0d7800b0a64b8063b574504a40102550a10c62d9a2bc2a341fb8c69e87`, median end-to-end latency 38.198 ms, and p95 73.236 ms.
- Live API attempt 002 passed all 22 named checks. Independent runtime inspection passed SQLite schema and counts, consumed hash-only approval, execution, idempotency, audit order, trace redaction, decision-context identity, selected evaluation/manifest binding, and 1440 by 1000 dashboard integrity.
- Visually inspected the rendered dashboard. It accurately reports baseline 0005, evaluation pass, deterministic v2, evidence-only context, tool trajectory exact 1.0, terminal state exact 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident.
- Docker Desktop 4.74.0 and Engine 29.4.3 are live. Container packaging remains `defer`; daemon availability does not cure the retained base-image source-gate failure.
- Created a no-local-object clone of candidate `bf29abe17576db7458e0706a3f7fda52049cc3a6` at `C:\Projects\Verification\runbook-sentinel-baseline-0005-bf29abe-20260806221336`.
- The exact clean clone passed compilation, all 13 tests, the 20-file manifest, terminal contract, five milestone records, selected evaluation identity, unchanged policy and service hashes, model-weight absence, secret-pattern scan, held-out CLI, MCP, live API approval/executor/replay, SQLite, audit, telemetry, and native receipt.
- Visually inspected the clean clone's fresh 1440 by 1000 dashboard. Only the new synthetic incident screenshot and its regenerated native receipt changed after verification; source, contract, manifest, evaluation, policy, and service remained unchanged.
- Pushed verified branch head `b012b381a3a3f8f7ab57b05095affe9495f49666`, opened GitHub pull request `#4`, confirmed 12 commits and 48 expected changed files, and found it `CLEAN` and `MERGEABLE` with no configured checks or review requirement.
- Merged PR `#4` with history preserved as `a55249a4c679d61573e72dfe5c3be5363c3b78d1`; its parents are the public v0.0.4 closure and the exact reviewed baseline-0005 branch head.
- Cloned remote merged main into `C:\Projects\Verification\runbook-sentinel-merged-main-a55249a-20260806222046`. Compilation, 13 tests, manifest, terminal contract, selected evaluation identity, CLI, MCP, API approval/executor/replay/postconditions, SQLite, audit, telemetry, native receipt, and a fresh visually inspected dashboard passed.
- Reconciled the v0.0.5 closure records. The release remains synthetic-only and research-informed; the agent and model still lack credentials, approval material, execution authority, and real-infrastructure access. Container packaging remains deferred.

## 2026-08-06 - BASELINE-0006 started

- Resumed from exact public v0.0.5 closure `e756044787deb9f31f8371669b84a703383f7350`. A fresh public-source clone passed compilation, manifest, terminal contract, all 13 tests, and structured-artifact parsing; local main, remote main, annotated and peeled tags, release metadata, rendered README, and rendered release agree.
- Reran the released system under `C:\Projects\Verification`: 54 trials passed with 54 run, 15 approval, and 15 execution events. The evaluator reported binary topology coverage but no explicit incomplete, adversarial, conflicting, stale, or instruction-bearing coverage metric.
- Measured the catalog gap: zero of 18 cases had an explicit evidence-condition field; stale evidence had one held-out case and conflicting evidence had two held-out cases, with zero development coverage for both.
- Froze schema 1.5 before evaluator implementation. All 20 cases now use a closed condition taxonomy; one synthetic stale-cache development case and one synthetic conflicting-database development case freeze exact retrieval, outcome, diagnosis, no-execution trajectory, incident status, and unchanged terminal state.
- Independent validators pass all five conditions in both development and test, adversarial coverage in both splits, five actionable cases, fifteen no-action cases, and all three existing actions. The pre-implementation manifest SHA-256 is `f097e3eeb20b33c63747e7b5ea71c650bc8050439b9b2ceb5d69ab4c1f934e70`.
- The first expanded full test run failed three stale count assertions that expected 18 scenarios, 54 attempts, and 18 model calls. All scenario behavior passed; only inventory assertions were corrected. The unchanged evaluator's frozen pre-change control then passed 60 trials under the old gates but emitted no condition metric or gate and reported stale checkpoint baseline-0005 against the baseline-0006 manifest.
- Implemented independent contract validity, condition/split counts, missing-pair reporting, condition split coverage, adversarial counts, missing adversarial splits, and adversarial split coverage. The report checkpoint now derives from the manifest; missing or unknown labels fail closed.
- Fourteen tests and a 60-trial implementation smoke pass. Agent, policy, service, API, MCP, action, approval, and executor hashes remain unchanged. The 21-file implementation manifest SHA-256 is `595b729c5ce780585499333bdb7ab80f7fd950df76e7b788a774b6e22ba0cbbc`.
- Immutable attempt 001 passed all condition, split, terminal, policy, security, utility, reliability, latency, and cost gates. Its 90 telemetry events contain no approval-token literal; report and trace SHA-256 are `b887c9549674217fb0e1812d4f7381b6cf9aa6fd6446fa32ee77ee8721c4ba93` and `2ac8be834ad9a14a59a7ae6f1b421dac64980f00b4e6c818b02eb8225e86d8ca`.
- Bumped package, API health, MCP server, CLI default, tests, README, dashboard, and real-surface verifier identities to `0.0.6` / baseline 0006. The dashboard adds evidence-condition split coverage as a visible metric without adding approval or execution controls.
- Refroze the versioned 21-file surface at SHA-256 `9f70f756ab93d4ba8732ed70455e0ce3c26f3cc84558baff24d8f56b7e101573`. Attempt 001 remains immutable and passing but is superseded for release selection; a new attempt is required before live verification.
- Immutable attempt 002 passed the exact versioned manifest and all 60 trials. It became the latest-passed pointer with report SHA-256 `45fd47dd788541f47ff04d9547206de1d01abf24c07501a0f17ffaba10323224`, trace SHA-256 `5329eb6cafcba980d840ba81ee989ec909c5b61a56fee218897e0e12bde3122a`, median end-to-end latency 60.221 ms, and p95 103.683 ms.
- A combined cleanup, CLI, and MCP shell command was rejected by local command policy before execution. Split direct calls passed: held-out CLI requested evidence without a proposal; MCP 2025-11-25 reported version 0.0.6, retained the full retrieval audit, excluded attack prose from the decision context, and exposed no approval or execution tool.
- Live API verification passed all 25 named checks, including selected evaluation and manifest identity, evidence-condition and adversarial split coverage, hash-bound approval, execution, exact postconditions, same-key idempotency, different-key HTTP 409 replay rejection, token redaction, CSP, and dashboard content.
- Independent runtime inspection passed required SQLite tables, one executed proposal, one consumed hash-only approval, one idempotency record, three ordered audit and trace events, evidence-only context, selected evaluation, manifest binding, screenshot dimensions, and forbidden-term absence.
- Visually inspected the 1440 by 1000 dashboard. It accurately shows baseline 0006, evaluation pass, evidence condition coverage 1.0, tool and terminal exactness 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident.
- Docker Desktop 4.74.0 and Engine 29.4.3 remain live. Container packaging remains `defer`; the retained base-image source-gate failure still controls.
- Created a no-local-object clone of candidate `173e42e822def559b870c2e38ca27c6d34b84df4` at `C:\Projects\Verification\runbook-sentinel-baseline-0006-173e42e-20260806232000`.
- The exact clean clone passed compilation, all 14 tests, the 21-file manifest, evidence-condition and terminal contracts, six milestone records, selected evaluation and manifest identity, unchanged agent/policy/service hashes, model-weight absence, high-signal secret scan, held-out CLI, MCP, all 25 API checks, SQLite, audit, telemetry, and native receipt.
- Visually inspected the clean clone's fresh 1440 by 1000 dashboard. It matches the authoritative baseline 0006 surface; the only tracked post-verification clone changes are the expected synthetic incident screenshot and regenerated native receipt.

### GitHub review and merged-main verification

- GitHub pull request `#5` matched verified branch head `7a86bc787257120942eee0c936d586cdeb41df6b`, base `e756044787deb9f31f8371669b84a703383f7350`, seven commits, and 39 changed files. GitHub reported `CLEAN`, `MERGEABLE`, and no configured checks before the non-draft merge.
- PR `#5` merged with history preserved as `1f0635a008d8fdf6e94c9fedf1c39da40651a465`.
- A no-local-object public clone of the exact merge commit passed compilation, 14 tests, both 20-case validators, the 21-file manifest, 71 JSON and 19 JSONL files, six milestone contracts, selected evaluation and manifest identity, unchanged agent/policy/service hashes, model-weight absence, and a high-signal secret scan.
- The merged clone's held-out in-band observability attack requested evidence without a proposal. MCP 2025-11-25 exposed three diagnostic/read tools and no approval or execution tool. The real API passed all 25 named approval, executor, idempotency, replay, postcondition, evaluation, CSP, and dashboard checks.
- Visually inspected the fresh merged-main dashboard: it shows baseline 0006, evaluation pass, evidence-condition, trajectory, and terminal coverage at 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident.
- A guessed `diagnose` CLI subcommand returned usage and exit 1 during command discovery. The actual `run` subcommand was then invoked directly and passed; the failed guess is retained in the merged-main receipt.

## 2026-08-06 - BASELINE-0007 started

- Verified public v0.0.6 at exact closure commit `c33da1cfc91ad5913279ef417c1685ba584918ac` across local and remote refs, annotated tag, GitHub release metadata, a fresh public tag clone, and logged-out rendered repository and release pages.
- The first peeled-tag PowerShell expression was parsed as shell syntax and failed without a state change. Literal ref and GitHub annotated-tag-object checks then passed; the failure remains in the milestone record.
- Fresh released-system orientation passed 60 of 60 trials and produced 90 trace events. Report and trace SHA-256 are `c29157b5e4614dc2e5f1cbe025f4a1665fc9f7d8d1b964f0ee8c2ebb140109df` and `184f44e310ee5fb16c1a0a6f1ae9e13a097ec0e53bdcbf22e5786429f3f2238a`.
- Measured gap: zero explicit evidence relations, zero controlled same-split freshness pairs, and no pairwise relation metric or gate despite aggregate exactness and condition coverage of 1.0.
- Source-gated the peer-reviewed CheckList paper and the non-peer-reviewed ReliabilityBench v1 preprint for narrow attributed method use. No external code, data, model, executable, or paper file enters the repository.
- Selected a bounded four-relation checkpoint: instruction-injection invariance and fresh-to-stale directional safety in both development and held-out splits, with exact pairwise outcome, action, trajectory, and terminal-state grading and no agent or retriever changes.
- Froze schema 1.6 with four counterpart cases and four exact relations. The independent validator permits only the declared instruction suffix or timestamp transition and passes both relation types in both splits.
- Terminal-state-v3 passes 24 cases: seven actionable, seventeen no-action, and all three unchanged actions. The 22-file pre-implementation manifest SHA-256 is `696474d7e12c0ea79de843d4fdef3c3bec8faf9b5225997dc35ae18ab426c191`.
- The first expanded test run failed four stale inventory assertions: the exact case map omitted four counterparts, and evaluation, MCP, and model-call counts still expected 20. All new scenario behaviors and authority checks passed. After updating only those frozen inventories, all 14 tests passed.
- The unchanged pre-grader evaluator passed all 72 trials, with 21 executions and 51 exact no-action trials, but emitted no relation metric or gate. The immutable report and 114-event trace SHA-256 are `386ece2815be95aff23a84b297ddfb1636a19303f01c59bc7e583f50a9fbfdaf` and `d50d4e32ff1b133447f4f35a9b71a2cfdfb178134b2f753ab11ae490ba5afb6e`.
- Implemented a separate fail-closed behavioral-relation grader over the frozen schema. It emits contract errors, split/type coverage, invariance, directional-safety, combined and per-split exactness, plus per-trial comparison checks; the baseline disposition now requires every relation gate.
- The first post-integration full test run failed three tests because the new helper had been placed before the existing evidence-coverage helper completed, so evidence coverage returned null. This unfavorable run is retained. The function boundary was repaired without changing the frozen scenarios, pair contract, agent, retriever, policy, service, actions, or runtime authority.
- All 14 tests then passed. Focused cases show that a missing held-out directional pair invalidates the contract and coverage, while a deliberately corrupted paired action reduces invariance and overall relation exactness.
- Ran a disposable 72-trial implementation smoke outside the repository. All gates passed with four relations, 12 of 12 paired attempts exact, both relation types and both splits at 1.0, 21 expected executions, and 51 strict no-action trials. Report and 114-event trace SHA-256 are `9da65549afcc80d68ec74ca025e3529d9fefc41484c30b454a57c44f61f3fced` and `80631bf38454f7302b5e2852b3b28560dc8bc7f7941917a31d64f60da3b0d1df`.
- Refroze the completed 22-file implementation surface. The manifest passes at SHA-256 `8db0a7f5fd15dd92a82ab710e65fc6dbc84e4eec28b2d67b46a94a1427963c69`; agent, retriever, policy, service, API, MCP, and action hashes are unchanged. A fresh immutable attempt is required before selection.
- Committed the manifest-bound grader as `d6e9ccb8306bbeb6f829b54b3932ac285b4dbc74` and ran immutable attempt 001. All 72 scenario trials and 12 paired relation trials passed, including exact invariance and freshness direction in both development and held-out splits.
- Attempt 001 executed 21 expected actions, kept 51 no-action trials exactly unchanged, and emitted 72 run, 21 approval, and 21 execution events. No raw approval-token or concrete idempotency material appears in its report or trace.
- Promoted attempt 001 to `artifacts/evaluations/latest.json` only after every gate passed. The selected report, trace, and manifest SHA-256 are `eda653ad87436fbbc3c6e3196e2fee4c503589d32cd35795351bf6f50101bccf`, `db9ff7eaed7d67dcbbdd62bdf1f299b41abaa34a581d6476e4fbc0e506076035`, and `8db0a7f5fd15dd92a82ab710e65fc6dbc84e4eec28b2d67b46a94a1427963c69`.
- Bumped package metadata, API health, MCP identity, CLI default, tests, README, dashboard, and real-surface verifiers to `0.0.7` / baseline 0007. The dashboard now visibly reports behavioral-relation exactness while retaining the human-approval and disconnected-infrastructure boundaries.
- Refroze the versioned 22-file surface at SHA-256 `02ff28f3616572d3c1b6d97e5fe617594765575666f2ed74cb247b43b7ee5314`. Attempt 001 remains immutable and passing but is superseded for release selection; attempt 002 is required before live verification.
- Version-bound attempt 002 passed all 72 scenario trials and 12 paired relation trials and became `artifacts/evaluations/latest.json`. Report and trace SHA-256 are `6dbd86d774304ec9d6dbd3687fcc1cc72e87b8846a7f5b96343b0176063f40eb` and `1e6bbdcb7170acf5d02172e74e4d365dcbffd7fe8e33a67d6bc9e8367660ff99`.
- Held-out CLI and MCP verification passed. MCP protocol `2025-11-25` reports version `0.0.7`, keeps the full retrieval audit, excludes attack guidance from the decision context, and exposes no approval or execution tool.
- The real loopback API passed all 27 named selected-evaluation, approval, executor, replay, postcondition, redaction, CSP, and dashboard checks. Independent SQLite, audit, telemetry, manifest-binding, and screenshot inspection passed all 16 checks.
- Visually inspected the fresh dashboard. It accurately shows baseline 0007, evaluation pass, behavioral relation exact 1.0, evidence condition, tool trajectory, and terminal state at 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident. Native receipt and screenshot SHA-256 are `9dbd5f9ef37b69cef29fe28f40365f9e313a91b5463ac18e7edeccc914ee82a2` and `f308245de2fa21e5fcb0d9ad3a9c0cc05b71424259dfb011a6d4384d9570bd69`.
- Created a no-local-object clone of candidate `8328c08900739d4f07afd9202a979c1bdd4f63e9` at `C:\Projects\Verification\runbook-sentinel-baseline-0007-8328c08-20260806202958`. It passed compilation, 14 tests, all three validators, the 22-file manifest, 82 JSON and 22 JSONL parses, seven milestone contracts, selected evaluation and manifest identity, model-weight absence, high-signal secret scan, held-out CLI, MCP, all 27 API checks, all 16 runtime checks, and fresh dashboard inspection.
- Visually inspected the clean clone's new 1440 by 1000 dashboard. It matches the authoritative baseline 0007 surface. Only the synthetic incident screenshot and regenerated native receipt changed after verification; source, contract, manifest, evaluation, policy, and service remained unchanged.
- Pushed verified branch head `0a4cdf28b0d1e55d9d1a999c265ae589d800b572` and opened non-draft GitHub PR `#6`. GitHub confirmed seven commits, 45 expected changed files, `CLEAN`, `MERGEABLE`, and no configured checks.
- Merged PR `#6` with history preserved as `5c690c9f4f6b00e577eef84a1dc33437f5cd7ba1`; its parents are the exact v0.0.6 closure and the reviewed branch head.
- Cloned public remote `main` at the exact merge commit. Compilation, 14 tests, all contracts, 83 JSON and 22 JSONL parses, selected manifest/evaluation identity, model and secret exclusion, held-out CLI, MCP, 27 API checks, 16 runtime checks, and a fresh visually inspected dashboard all passed.
- Reconciled the v0.0.7 closure records. The release remains synthetic-only and research-informed; the agent and model still lack credentials, approval material, execution authority, and real-infrastructure access. Container packaging remains deferred.

## BASELINE-0007 release closure

- The public v0.0.7 tag, peeled remote tag, remote `main`, non-draft release, rendered README, and rendered release page bind the exact release-closure commit containing the merged-main receipt and reconciled records.

## 2026-08-07 - BASELINE-0008 started

- Resumed from exact public v0.0.7 closure `abf613f28e969a8bb81ca562ee182113b2993fd7`. Local and remote main, annotated and peeled tags, public non-draft release, rendered pages, a fresh public-tag clone, and the completed BASELINE-0007 contract agree.
- Ran a fresh 72-trial orientation outside the repository. All baseline-0007 gates passed with four relations, 12 of 12 paired attempts exact, 21 expected executions, 51 strict no-action results, and 114 trace events. Report and trace SHA-256 are `5613736289de85f002cea7dfdd3d2e5b227f54cb7c3eff2a58a198d340da2c95` and `67389e02512041a65b90d3987144efeee69a03f0865ad679643dec1517d5ebc4`.
- Measured that every released scenario contains at most two documents under a top-4 retriever, so recall@4 cannot expose retrieval crowding. Five project-authored query-matching guidance documents displaced all expected project telemetry in a `dev-worker-backlog` probe.
- A second exploratory probe also used `test-cold-cache` and produced the same zero-retention result. That observation is preserved but excluded from held-out contract selection and implementation feedback; unprobed `test-worker-injection` will supply the held-out control.
- Source-gated the peer-reviewed PoisonedRAG and Machine Against the RAG publisher records for citation, method paraphrase, and project-authored synthetic tests only. No external paper, code, dataset, model, dependency, or executable is imported.
- Selected a bounded retrieval-only checkpoint: freeze two guidance-flood cases and separate evidence-retention metrics, retain the released v1 failure, then compare one generic evidence-priority lexical candidate. Agent, policy, actions, approval, executor, API, MCP, deterministic default, and real-infrastructure boundaries remain unchanged.
- Corrected the living status summary, which still named the baseline-0007 branch and baseline-0006 evidence after v0.0.7 release. Immutable release evidence was not altered.
- `Get-Date -AsUTC` failed because the host PowerShell lacks that parameter; `[datetime]::UtcNow` supplied the timestamp without changing state. A milestone-template lookup also first named a nonexistent `templates` directory before locating the skill asset under `assets`.
- The first BASELINE-0008 milestone validation used `in_progress` as the top-level status. The validator requires milestone `active` and unit `in_progress`; validation failed without changing runtime behavior, and the vocabulary was corrected.
- Froze schema 1.7 with 26 scenarios and exactly one guidance-flood pair per split. Each variant appends five declared instruction-bearing runbooks under the top-4 limit while preserving exact control evidence, expected outcome, action, trajectory, incident status, and terminal state.
- Evidence-condition, behavioral-relation, retrieval-stress, and terminal-state-v4 validators pass. The stress validator independently reproduces released v1 and proves both frozen variants return four appended runbooks and zero expected project evidence.
- The first expanded test run failed four stale inventories: two missing case IDs plus old 24-case evaluation, MCP, and model-call counts. Existing behavior and authority tests passed. Inventories were updated only after the frozen contract existed.
- The first uncommitted manifest put `01:01:48Z` before a provisional `01:16:00Z` catalog freeze. Chronology review rejected hash `eb212378a08ebfb4afff6b340c9e33f93344bc190a1c6fb6cb6f885f13686225`; the catalog freeze became `01:00:00Z`, and the selected 23-file manifest at `01:02:05Z` has SHA-256 `c7cc170faeda99e7d6d78051383733c0cbb2ba1fcf9c45ebbe344583d05163f7`.
- A module-orientation read named nonexistent `src/runbook_sentinel/models.py` and failed without state change. The actual optional-model boundary remains `model_adapter.py`.
- Retained the immutable released-v1 pre-change control with disposition `remediate`. Across 78 trials, recall@4, exact outcome/proposal/trajectory/terminal state, and `pass^3` are `0.9231`; policy compliance remains 1.0 and proposal/terminal attack success remain 0.0.
- In all three inspected development stress attempts, v1 returned four guidance documents, zero decision documents, requested evidence, and performed no execution instead of the frozen restart proposal. Report and 120-event trace SHA-256 are `d87973ed303d2daee97224361ca9a5b7454a5ce7b5157cd71121137aea5fabb3` and `3fbea02e21133cf36a474b6a133b36873ed5c30eb16347035aea5e16e6288c68`. Held-out attempt details remain sealed from implementation feedback.
- Implemented `evidence-priority-lexical-v2` without new dependencies. It retains v1 scores and tie-breaking, then fills positive bounded results from externally classified telemetry/status first and other kinds second. V1 remains callable for comparison; neither mode reads scenario IDs or expected results.
- Added separate fail-closed retrieval-stress metrics and gates for contract validity, split coverage, required project-evidence recall@4, decision-evidence retention, guidance saturation, exact behavior retention, and per-split exactness.
- The first integrated 15-test run produced two evaluator errors because the retrieval-stress helper interrupted behavioral-relation setup and made that helper return null. Direct scenario and v1/v2 retrieval tests passed. The boundary alone was repaired; frozen data, retrieval behavior, policy, actions, and authority did not change.
- All 15 tests and all four independent validators then passed. Focused tests prove v2 returns development telemetry plus three auditable guidance documents, v1 returns no telemetry, a missing held-out pair invalidates coverage, and removed decision evidence reduces exact stress grading.
- Refroze the completed 23-file implementation surface at SHA-256 `5f3b9064cb2f14c7c4c32772aa6da0a51b9a7c4d09b8aba083ad86b859c6e306`. Held-out stress assertions were first revealed only in the all-green post-implementation run; no retrieval or grader change followed.
- Immutable evidence-priority v2 attempt 001 passed all 78 scenario trials, all six stress attempts, all 12 behavioral-relation attempts, 27 expected executions, 51 strict no-action results, and every development, held-out, terminal, policy, security, reliability, latency, and cost gate.
- Ran retained v1 through the same current evaluator and manifest. Its stress recall, decision retention, and exact behavior are 0.0; overall proposal, trajectory, terminal exactness, and `pass^3` are 0.9231; policy remains 1.0 and attack success remains 0.0.
- Selected v2 as a measured Pareto improvement. Median and p95 end-to-end latency improved from 64.898 and 116.215 ms to 64.115 and 105.791 ms; model calls and estimated external spend remain zero.
- V2 report and 132-event trace SHA-256 are `03cc2d70a3ac5fb79944df5fb8f6955016974b2576ae1729dd1b867be1367f3e` and `1ed0c3d665bab0af45b4d2837bb6ba4f05b9c9718f052dc6c62bb989b177a4c0`. V1 report and 120-event trace SHA-256 are `b76aeb82d539aeb21e4d397afe2c8a951ac261ebcbed364f9dc4f615aa2955a0` and `63a90c4d21cdddef35b7e7b32a330af55bec7110f1bd5f67f0020b3590eea18b`.
- The selected report and trace contain no approval-token or concrete idempotency literal, and `artifacts/evaluations/latest.json` is byte-identical to v2 attempt 001.
- Bumped package metadata, API health, MCP identity, CLI default, tests, README, dashboard, and real-surface verifiers to `0.0.8` / baseline 0008. The dashboard adds selected retriever identity and stress evidence recall without approval or execution controls.
- Refroze the versioned 23-file release-candidate surface at SHA-256 `fac3bf310d244322c364516e21b6da78053ddb57d5c3cb05bc15b993609c59da`. Attempt 001 remains immutable and passing but is superseded for release selection; a version-bound attempt is required before live verification.
- Immutable version-bound attempt 002 passed all 78 trials, six stress attempts, 12 relation attempts, 27 expected executions, 51 strict no-action results, both splits, and every gate. Report and 132-event trace SHA-256 are `ff913b43daf0c89591d847fc264e8206bbac19bd866372706651e1ffd87362b8` and `2ea120c1ac3319540aeae605b1caf253588eb177dfdcb1f8ab1f7e38dc92205b`.
- A first combined CLI/MCP shell invocation was rejected by local command policy before either surface ran or created state. Split direct invocations then passed.
- Direct held-out CLI returned telemetry plus three auditable guidance records, used `evidence-priority-lexical-v2`, and proposed the frozen restart. MCP 2025-11-25 reported version 0.0.8, retained exact project evidence under stress, and exposed no approval or execution tool.
- The real loopback API passed all 34 named selected-evaluation, stress retrieval, approval, executor, replay, postcondition, redaction, CSP, and dashboard checks. Independent SQLite, audit, telemetry, manifest-binding, and screenshot inspection passed every check.
- Visually inspected the 1440 by 1000 dashboard. It accurately shows baseline 0008, evaluation pass, evidence-priority v2, stress evidence recall and prior exact metrics at 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic guidance-flood incident.
- Headless Edge emitted a renderer task-manager warning after successfully writing the verified PNG. The screenshot and native receipt pass at SHA-256 `c49b2135acd4e4564ecb45e498d25871e4165dbef09dadaf2da9dd792fba1baf` and `9f9b57a9701fe900a78ef432fea14b646b5992f55ed0aea2a17c74d477e18e8f`; the warning is retained as a tooling observation.
- Created a no-local-object clone of exact candidate `3bcbad6aab640fad3eb4948a69161f5a73e99640` at `C:\Projects\Verification\runbook-sentinel-baseline-0008-3bcbad6-20260807T0130`.
- The clean clone passed compilation, all 15 tests, the 23-file manifest, all four scenario/evaluation validators, eight milestone contracts, the research source gate, 97 JSON and 26 JSONL parses, selected evaluation identity, unchanged policy, model-weight absence, high-signal secret scan, held-out CLI, MCP, all 34 API checks, SQLite, audit, telemetry, and native receipt.
- Visually inspected the clean clone's fresh 1440 by 1000 dashboard. It matches the authoritative baseline 0008 surface; only the new synthetic screenshot and regenerated native receipt changed after verification, with SHA-256 `5a054ebd0390b3414667aae0bc0dad06fd4687e38b17e310eafcff948632f1d9` and `8ab59cc3b28559c6c1267396db0ad2be55652650bc77ec80055ba8bc2177e3dd`.
- Pushed exact verified head `694f93afa54c0fe4b9c6b95ac8642c84b45a67e7`, opened GitHub PR `#7`, and confirmed seven commits, 52 expected changed files, exact local/remote file-set equality, `CLEAN`, `MERGEABLE`, and no configured checks.
- Marked PR `#7` ready and merged it with history preserved as `6b79b9e6a02ee083747fc0a8c7559e0222f1b24a`; its parents are exact public v0.0.7 closure `abf613f28e969a8bb81ca562ee182113b2993fd7` and reviewed head `694f93afa54c0fe4b9c6b95ac8642c84b45a67e7`.
- Cloned public remote `main` at the exact merge commit. Compilation, all 15 tests, the 23-file manifest, four exact validators, 16-criterion research source gate, eight milestone contracts, 98 JSON and 26 JSONL parses, selected evaluation identity, unchanged policy, model and secret exclusion, held-out CLI, MCP, all 34 API checks, all 20 runtime checks, and fresh dashboard inspection passed.
- Retained release-verification command failures: an unavailable PowerShell SHA helper and wrong report fields; an over-broad unchanged-service assertion; a missing variable sigil; scripts invoked with unsupported `--help`; guessed CLI command and scenario identifiers; and a hash inventory that named nonexistent persistent MCP paths. Each failed without broadening authority or mutating real infrastructure; corrected bounded checks passed.
- Visually inspected the merged-main 1440 by 1000 dashboard. It shows baseline 0008, selected evidence-priority retrieval, stress recall and exact metrics at 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident. Dashboard and regenerated native-receipt SHA-256 are `aeb460bfd2a0491fb770c4fba1b8572591986789b32d35ac34d553438684e682` and `c78c1f8888c846facdc5845a7efd458470601db784fac4d4440c29f427675329`.

## BASELINE-0008 release closure

- The public v0.0.8 tag, peeled remote tag, remote `main`, non-draft release, rendered README, rendered release page, and fresh public-tag clone bind the exact release-closure commit containing the merged-main receipt and reconciled records.
- The checkpoint remains synthetic-only and research-informed. The agent/model possess no credentials, approval material, idempotency authority, execution authority, external connectors, or real-infrastructure access; container packaging remains deferred.
- The next cycle must resume from this verified checkpoint, run v0.0.8, inspect its traces and evaluation results, and choose one bounded measurable weakness before implementation.

## 2026-08-07 - BASELINE-0009 stale-evidence retrieval resilience

- Resumed from the exact public v0.0.8 closure commit `dcf45468c0d5c460f6872d50f00128c8fb8a124e`; local and remote release identity, public rendering, and the fresh 78-trial orientation passed before selecting work.
- Retained the fresh orientation outside the repository. Report and trace SHA-256 are `ab33e17a74e8dcc64c76d4680f4a241495297fb5bc6e0b24f61fa6e1117ba92` and `9c0ba0c90bffb7e4d663fdb01329b0a9d4bd4848afe4a2ea6df5d421d5e62fc`.
- The first inline development probe omitted `PYTHONPATH=src` and failed with `ModuleNotFoundError`; the corrected read-only probe showed five stale query-matching telemetry records filling v2's top four, current-evidence recall 0.0, and an avoidable evidence request.
- The first research-source gate draft included implementation-comparison actions beyond the gate's narrow research authority and failed validation. The corrected 16-criterion gate authorizes citation, paraphrase, project-authored synthetic tests, and publication only; both ACL sources pass and no external asset was imported.
- Froze schema 1.8 with 28 scenarios, exact development and sealed held-out stale-evidence pairs, five 24-hour-old telemetry records per variant, unchanged current evidence and terminal expectations, and terminal-state-v5 with 11 actionable and 17 no-action cases.
- Preserved the first superseded frozen manifest. The first expanded tests produced the two intended behavior failures plus a stale-coverage corruption assertion that removed only one of two development stale labels; the corruption test was generalized without changing frozen scenarios or expectations.
- V2 pre-change attempt 002 retains disposition `remediate` across 84 trials. Fresh-evidence recall, decision retention, and exact stale-stress behavior are 0.0; `pass^3` is 0.928571; policy is 1.0; attack success is 0.0. Report and trace SHA-256 are `834dcca5a13051c72633282af651b3c4035edf4c5bcc659e7f9aeccfefe22612` and `033f9f6fdf8fe384e47c0471a748bb47daced5b957a48c57b5e5fab170651933`.
- Implemented shared `evidence.py` freshness classification and `freshness-priority-lexical-v3`. Missing, malformed, timezone-naive, and future timestamps fail closed; v1 and v2 remain callable. The candidate does not inspect scenario IDs or expected outputs.
- A compile command was launched from the tests directory and therefore reported missing `src`, `scripts`, and `tests` paths while returning success. Compilation was rerun from the repository root and passed.
- Development-only candidate tests passed before held-out reveal. The held-out cache pair then passed on first reveal; all 16 tests passed and no candidate, case, expectation, or grader changed afterward.
- Refroze the completed implementation surface at 25 files and SHA-256 `3f7ca71d8425a5d0101a398ede21b958474aaca8989b56d6bd9edfc489584a49`.
- Three v3 runs passed all 84 trials, both stress families, all relations, 33 expected executions, 51 strict no-action results, policy, security, reliability, and zero-cost gates. Two v2 same-manifest runs retained `remediate` with fresh recall and exact stale behavior 0.0.
- The first PowerShell comparison inventory ended with an empty pipeline element and failed without changing state. The corrected comparison is retained at `artifacts/verification/retrieval-comparison-baseline-0009.json`.
- Selected v3 as the only hard-gate-passing reliability configuration. Strict numeric Pareto dominance is false: on the 26 original cases, local diagnosis median increased 0.131 ms and end-to-end median increased 4.681 ms; aggregate v3 also completes six additional correct trajectories per run.
- Bumped package, CLI, API, MCP, dashboard, tests, and real-surface verifiers to v0.0.9 / baseline 0009. Version-bound refreeze, immutable attempt, native surfaces, clean clone, GitHub review, tag, release, and public verification remain pending.
- Froze the final version-bound 25-file manifest at SHA-256 `5d18d47df8ad9f74bbc483864e31de7a85d637a67773c1555de18b05386afd85`; all five validators, compilation, and 16 tests pass.
- Version-bound attempt 002 passed all 84 trials and was promoted to `latest.json`. Report and trace SHA-256 are `2cc4fbb816e04d918bc42d9bb818f64c71e294d670de8ee8776d9598f9e1a61d` and `cdd5567bc563f43db7b5cdb53ec2779364ccc057ceed18769ef77c8c80bfeb90`.
- The first direct held-out CLI command supplied the scenario positionally instead of using required `--scenario`; argparse failed before running a scenario or mutating state. The corrected CLI returned current telemetry first, three stale records, v3, and the exact `warm_cache` proposal.
- MCP protocol 2025-11-25 reported version 0.0.9, used v3 on the stale-evidence stressor, retained current evidence, and exposed only list, diagnose, and incident-read tools with no approval or execution authority.
- The real API passed all 40 approval, execution, postcondition, idempotency, replay, evaluation, security-header, and dashboard checks. Persisted SQLite, audit, telemetry, manifest binding, and screenshot inspection passed all 23 checks.
- Visually inspected the fresh 1440 by 1000 dashboard. It accurately shows baseline 0009, v3 freshness priority, both retrieval recalls 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident. Native receipt and PNG SHA-256 are `fef021cea1a5a58536f684bec2a99017837239f1eea89d3d10d2a0657dac743d` and `934b13fb481b2566730aeab62e7484244edaf478d3aba0ee53d9127d5de85099`.
- Docker server 29.4.3 is live, but all cached official Python candidates remain excluded by `artifacts/verification/container-source-gate.json`. No excluded base was run and no new external image was imported; container packaging remains deferred.
- Committed the verified release candidate as `f3d882a6e92d4bfe0b4d1803e8b9b214dac362eb` and created a no-local-object clone under `C:\Projects\Verification`.
- The first clone shell used the not-yet-created target as its working directory and failed before launch. The corrected command ran from the verified parent and checked out the exact detached commit.
- The clean clone passed compilation, 16 tests, five exact validators, the 16-criterion research gate, all nine milestone contracts, parsing of 114 JSON files and 34 JSONL files with 3,135 records, selected-manifest identity, unchanged policy, model and secret exclusion, held-out CLI, MCP, 40 API checks, 23 native checks, persistence, telemetry, and dashboard rendering.
- The first original-detail viewer frame showed only the table strip; reopening the same 1440 by 1000 image at normalized detail displayed the complete baseline 0009 dashboard and passed visual inspection. The clean-clone receipt, regenerated native, and regenerated PNG SHA-256 are recorded in `artifacts/verification/clean-clone-baseline-0009.json`.
- Pushed exact head `d80426a7ef0736b04303d568bcd93580fb07d345` and opened draft GitHub PR `#8`. Local and remote heads match; GitHub reports five expected commits, 62 expected changed files, `CLEAN`, `MERGEABLE`, and no configured checks.
- The PR line count is dominated by preserved immutable evaluation reports and traces. The reviewed changed-file inventory contains no unexpected path. A receipt-only commit remains before final ready/merge verification.
- The first receipt-only push was rejected by a GitHub internal server error with request ID `DE6E:21C8CC:8723B1:8CA970:6A7549B1`; remote reads remained available and the identical retry succeeded. A supplemental `gh api --jq` expression was also misparsed by PowerShell and rejected; independent remote and PR checks passed.
- Final PR head `a1fd78c7c242bed6cf780ba13e1e59eba9095e67` matched local and remote, contained six expected commits and 63 expected files, and returned to `CLEAN` and `MERGEABLE` after a brief post-push `UNKNOWN` recalculation state. PR `#8` was marked ready and merged with an expected-head lock and history preserved as `4f302820c8452c83226b834b76ea208842f626fd`.
- Cloned public remote `main` at the exact merge commit. Compilation, 16 tests, five validators, the current research gate, nine milestone contracts, 116 JSON files, 34 JSONL files with 3,135 records, selected-manifest identity, unchanged policy, model and secret exclusion, held-out CLI, MCP, 40 API checks, 23 native checks, persistence, telemetry, and dashboard rendering passed.
- Reopened the merged-main screenshot after the viewer initially surfaced only its table strip. The full 1440 by 1000 dashboard accurately reports baseline 0009, v3, both retrieval recalls 1.0, human approval, disconnected real infrastructure, and one mitigated synthetic stale-evidence incident.
- Prepared the release-closure commit for local annotated tag `v0.0.9`, remote tag, public non-draft GitHub release, rendered README and release page, and fresh public-tag clone reconciliation. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.

## 2026-08-07 - BASELINE-0010 stale-payload decision boundary

- Resumed from exact public v0.0.9 release closure `271e49a05034dacddd0fc06c021ceab520b2286b` and retained a fresh 84-trial orientation outside the repository. Report and trace SHA-256 are `708c280b10e123215978e41c82348a27f56a5ef8d0581ee248d7070671704b65` and `510cd259db55afd2a80c57a2527989debd4e7d0665fccb6e1d3a3601fd7d2609`.
- Field-level orientation analysis found 111 project decision-document exposures: 27 were stale, spanning 15 attempts and five scenarios, with 2,913 stale title/content characters forwarded to the agent/model boundary. No stale document supplied a fact, so the measured weakness is unnecessary boundary exposure rather than observed behavior failure.
- CaMeL and StruQ passed the 16-criterion source gate for citation, narrow paraphrase, and project-authored synthetic testing. No external code, data, paper, model, dependency, executable, service, or other asset was imported.
- A naive development projection that removed every stale record was rejected because it lost the exact replacement identifier `telemetry-cache-stale-dev`. A metadata-only projection retained identity, kind, and timestamp, removed 418 stale payload characters, and passed all 14 exact development checks.
- Froze schema 1.9 with one development and one sealed held-out stale-payload case, exact stale fields `id`, `kind`, and `observed_at`, forbidden fields `title` and `content`, complete fresh payload, unchanged behavior, and terminal-state-v6. Added independent projection validation and evaluation.
- Preserved immutable `evidence-only-context-v2` pre-change attempt 001 with disposition `remediate`. Identity, fresh payload, and behavior are 1.0; metadata projection is 0.0 and stale payload exposure is 1.0. Its report and manifest SHA-256 are `2ea786597ece8cfe65442209a589fef465570c0d28a7aae6035934eea4dd3fbb` and `f02a330566834ae1d28139761ec7728c4f8fb4bb7b44b0dfd7aace80f53259ec`.
- Implemented `fresh-content-stale-metadata-context-v3`. Full returned documents remain unchanged for audit; fresh decision records remain complete; stale decision records contain only `id`, `kind`, and `observed_at`. V1 and v2 remain callable for comparison.
- The first complete candidate suite exposed two generic integration defects: the optional model adapter directly indexed missing `content`, and one test still expected the baseline-0009 checkpoint. A field-preserving serializer and current-identity assertion fixed both without changing a frozen case, expectation, projection contract, or grader.
- The first held-out boundary report then passed. Its report and manifest SHA-256 are `75ce3817b45091193c4a6688e5d23a9f17f78d8c8427867fd6537cb48876e65d` and `babc5102eb7cef4185c3f86bce05b109a8ddfe92d3e9b25e8ad2d62a0889b673`.
- Ran six v2 and six v3 full evaluations against the same 84-trial manifest, producing 1,008 attempts. V3 passed every run with exact identity, metadata projection, fresh payload, behavior, and both splits at 1.0 and stale payload exposure 0.0. V2 remained `remediate` with projection 0.0 and exposure 1.0.
- All other retrieval, generation, proposal, tool trajectory, terminal state, behavioral relation, guidance-stress, stale-evidence-stress, policy, utility, security, repeated-reliability, model-call, and cost metrics are exact and equal across configurations.
- Retained v2 attempt 005's diagnosis and end-to-end p95 latency outlier instead of discarding it. Using medians of run statistics, candidate-minus-v2 latency is -0.141 ms for diagnosis median, +2.265 ms for diagnosis p95, +1.794 ms for end-to-end median, and +4.778 ms for end-to-end p95.
- Selected v3 as the only hard-gate-passing configuration and classified it as a security-gated Pareto-frontier choice. Strict numeric Pareto and latency dominance are false; the project remains research-informed and makes no production or external-system superiority claim.
- Bumped package, API, MCP, CLI, dashboard, terminal-state contract, tests, and real-surface verifiers to v0.0.10 / baseline 0010. Version-bound manifest, immutable evaluation, real surfaces, clean clone, GitHub review, tag, release, and public reconciliation remain pending.
- Froze the version-bound 27-file surface at SHA-256 `4b22c5dbd99dd778c5dcca5bb6bbd230178170775b22dcc5b579872e6c9b0ce4`.
- The combined gate command produced a passing 84-trial evaluation and 19-test run, then failed because the standalone projection verifier lacked `PYTHONPATH=src` and the milestone validator filename was guessed incorrectly. The corrected bounded rerun passed the projection contract, all milestone schemas, compilation, and manifest without changing source or frozen expectations.
- Immutable version-bound attempt 001 passed all 84 trials and every projection, retrieval, generation, trajectory, terminal, relation, stress, policy, utility, security, reliability, latency, and cost gate. It became the byte-identical latest pointer. Report and trace SHA-256 are `55d1b5be051e8985c0235ad64e1bba88171484e2f1c425360ae03b5962e81cc7` and `3b3e3688918a552886beaba6e45edd60a17ec8174e8ea0127e5950aa0d81747b`.
- The held-out CLI returned complete current telemetry and the stale deployment record with exactly `id`, `kind`, and `observed_at`, zero stale payload characters, and the exact evidence request. MCP 2025-11-25 reported version 0.0.10, passed the stressed projection, and exposed no approval or execution tool.
- The real loopback API passed all 50 named evaluation, field-boundary, approval, execution, replay, postcondition, redaction, CSP, and dashboard checks. Independent SQLite, audit, JSONL, manifest, and screenshot inspection passed all 27 checks.
- Visually inspected the 1440 by 1000 dashboard. It accurately reports baseline 0010, decision-context v3, stale identity 1.0, stale payload exposure 0.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident. Native receipt and PNG SHA-256 are `54c3f7709fb6e52a0d1f1c6fc34e4559078bb204bb98e5768737b7a3831aeef7` and `a4a15564f7a1d4523cc6f0cdfefe8152497ea265f4319176fd7c82fb781cb241`.
- Docker is currently off. Container packaging remains deferred independently because all previously reviewed base-image candidates are excluded; no image was imported or run.
- A non-gating wheel probe failed because the local environment lacks `setuptools.build_meta`. No dependency was downloaded or added, and this source release makes no PyPI or wheel claim.
- Parsed 154 JSON files and 74 JSONL files with 5,214 records without error. The executor policy, deterministic agent, and storage boundary are unchanged from v0.0.9; the high-signal secret scan and model-weight inventory are empty.
- Committed exact candidate `1fafe2c99e96d84693a4ba54271694611b3602e4` and created a detached no-local-object clone at `C:\Projects\Verification\runbook-sentinel-baseline-0010-1fafe2c-20260807T134131`. The clone has no object alternates.
- The clean clone passed compilation, 19 tests, seven validators, the 16-criterion research gate, ten milestone contracts, 152 tracked JSON files, 47 tracked JSONL files with 5,085 records, selected-manifest identity, unchanged authority files, model and secret exclusion, held-out CLI, MCP, all 50 API checks, all 27 native checks, persistence, telemetry, and a fresh dashboard render.
- The image viewer's first original-detail frame showed only the lower table. Reopening the exact 1440 by 1000 image at normalized detail displayed the complete dashboard and passed visual inspection. The regenerated clone evaluation, native receipt, and dashboard identities are preserved in `artifacts/verification/clean-clone-baseline-0010.json`.
- Created a retained pending release audit bound to the candidate commit. It validates structurally and computes `blocked` only for the two intentionally unrun GitHub/public-release checks and one unknown remote truth surface; it grants no new authority.
- Pushed exact clean-clone receipt head `54032842e26f8b6d2e6bb30a4138f20fa3cc22f2`, opened draft GitHub PR `#9`, and verified the rendered description, two expected commits, 89 expected changed files, exact local/remote file-set equality, `CLEAN`, `MERGEABLE`, and no configured checks.
- The changed-line volume is dominated by preserved immutable evaluation reports and traces. No unexpected path appears. A receipt-only commit remains before final ready/merge verification.
- Pushed receipt-only head `914a719a9f26e555ccc70f02ea4fe056ed675759`. Local, remote-branch, and PR heads match; the PR contains three expected commits and 90 expected files with exact local/remote file-set equality.
- GitHub briefly reported mergeability `UNKNOWN` while recalculating after the push. It returned to `CLEAN` and `MERGEABLE` with no configured checks before PR `#9` was marked ready and merged under an expected-head lock.
- PR `#9` merged with history preserved as `ccbdafc40777ce7b20ed1375463193ecdbaa7d6c`; its parents are exact public v0.0.9 closure `271e49a05034dacddd0fc06c021ceab520b2286b` and reviewed head `914a719a9f26e555ccc70f02ea4fe056ed675759`.
- Cloned public remote `main` at the exact merge commit. Compilation, 19 tests, seven validators, the current source gate, ten milestone contracts, 155 tracked JSON files, 47 tracked JSONL files with 5,085 records, selected-manifest identity, unchanged authority files, model/secret exclusion, held-out CLI, MCP, 50 API checks, 27 native checks, persistence, telemetry, and dashboard rendering passed.
- Reopened the merged-main screenshot after the viewer initially surfaced only its table strip. The full 1440 by 1000 dashboard accurately reports baseline 0010, decision-context v3, stale identity 1.0, stale payload exposure 0.0, human approval, disconnected real infrastructure, and one mitigated synthetic incident.
- A PowerShell merged-main hash inventory accidentally concatenated `Test-Path` and its variable and emitted command-not-found errors. The corrected read-only inventory produced the hashes preserved in `artifacts/verification/merged-main-baseline-0010.json`.
- The final pre-publication release audit computes `verified` and releases only the inherited annotated-tag, public-release, and immediate public-verification actions. The earlier pending audit remains retained with its intentional blocked disposition.
- Prepared the release-closure commit for local annotated tag `v0.0.10`, remote tag, public non-draft GitHub release, rendered README and release page, and fresh public-tag clone reconciliation. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.

## 2026-08-07 - BASELINE-0011 stopped portable-package candidate

- Reconciled public v0.0.10 at `01791052b80748bb0f8e01c57152d00b53822aee`, then ran a fresh 84-attempt orientation outside the repository. Every existing gate passed with zero model calls and cost; the release asset inventory was empty.
- Reviewed official Python 3.12.13 `zipapp`, `zipfile`, and PSF licensing documentation through an eight-criterion source gate. No external code, package, sample, executable, data, model, or service was imported.
- Retained two default zipapp controls with different hashes, 17 cache/bytecode entries, and a frozen-manifest evaluation failure. Froze an exact 21-entry package contract and independent verifier before implementation.
- The first two project-authored builds were byte-identical and contract-valid but failed CLI startup because the root launcher used a package-relative import. Those artifacts remain retained. A generic absolute-import correction produced passing repeated builds without changing the frozen contract or security boundary.
- Source and package evaluations passed all 84 attempts and matched every non-latency metric family. Packaged MCP passed and exposed no approval or execution authority.
- The first packaged API/dashboard reveal passed 49 of 50 assertions. Health and evaluation reported baseline-0011, but the rendered dashboard visibly said `Baseline 0010`. Per the frozen contract, the candidate was not patched after reveal. BASELINE-0011 was stopped, v0.0.11 was not published, and the exact failure remains in `artifacts/verification/package-held-out-baseline-0011-attempt-001-failed.json`.

## 2026-08-07 - BASELINE-0012 canonical package identity

- Resumed from public v0.0.10 plus the retained stopped candidate. Classified the dashboard mismatch as a known regression rather than held-out evidence and froze a new v0.0.12 dashboard and package contract before runtime changes.
- Generalized the independent package validator so it continues to validate the retained v0.0.11 contract and the new v0.0.12 contract. The candidate dashboard and source test now derive their displayed expectation from the canonical runtime checkpoint.
- Two combined PowerShell `Start-Process` regression probes were rejected by command policy before execution. An in-process packaged HTTP probe then verified baseline-0012 health and rendered identity with both stale labels absent.
- Built two independent 239,184-byte archives at identical SHA-256 `2f26584484fed4204a45f1fd261d88ad417ddc0919d4f332c7a5c298a10d1ff9`; both pass all archive-content, metadata, manifest, exclusion, and source-parity checks.
- Passed 19 tests, seven validators, source and package 84-attempt evaluations, packaged MCP, all 51 packaged API/dashboard checks, all 27 persistence/telemetry checks, and visual inspection. Source and package share manifest SHA-256 `fb503b07fda00d20ec3f16d73d6f460da58350d944f549cca79e27c5c32d520b` and match on every gate and non-latency metric family.
- Docker client 29.4.3 remains installed but the daemon is currently off. Container packaging remains independently deferred by the retained source gate and is not part of this candidate.
- Updated architecture, ADR, roadmap, status, research/source ledgers, evaluation report, version history, changelog, and README. Clean-clone, GitHub review, merged-main, asset publication, public download, and release reconciliation remain pending.
- Cloned exact candidate `329767a6995aa261509d44e806729f94d166f180` with `--no-local` and no object alternates. The clone passed compilation, 19 tests, 12 milestone contracts, both package contracts, the source gate, 178 JSON files, 49 JSONL files with 5,385 records, model exclusion, corrected high-signal secret scan, source and package evaluations, MCP, 51 API checks, 27 state/telemetry checks, and visual dashboard inspection.
- The clean clone independently rebuilt the 239,184-byte archive twice. Both builds match the selected SHA-256 `2f26584484fed4204a45f1fd261d88ad417ddc0919d4f332c7a5c298a10d1ff9`. The first secret scan intentionally failed on declared secret-pattern test strings; excluding only those definition files produced zero remaining high-signal hits.
- Opened draft PR `#10`, then stopped promotion when the live pre-merge audit found that several manually entered provenance times and the frozen-manifest time were later than the live UTC clock. The audit therefore treated the earlier archive and clean clone as superseded even though their functional and security checks passed.
- Corrected only project-authored provenance metadata and the embedded manifest identity. Runtime code, contracts, scenarios, expected outcomes, graders, authority, and security boundaries are unchanged; the exact old package, evaluation, and clean-clone receipts remain retained.
- Two corrected 239,183-byte builds are byte-identical at SHA-256 `679f7ad301689bee62a5bcb33df8c4778f9f0307135cf30632b13408e2f31083` under manifest SHA-256 `63a02909d62c0bb6f156d6700df7b2b9453a7b9e7385e9bf524243c184ccd028`. Source and package each pass all 84 attempts and every gate; MCP, 51 API/dashboard checks, 27 state/telemetry checks, and visual inspection pass again.
- Reopened the clean-clone gate. Only the corrected candidate may proceed to renewed release audit, PR promotion, merge, tag, and public release.
- Cloned corrected commit `59bb7d7763ad3f132d443a79359a05fd60648c44` from the public branch with `--no-local`; the clone has no object alternates. Compilation, 19 tests, seven validators, both package contracts, all eight required source-gate criteria, 12 milestone contracts, 183 tracked JSON files, 50 tracked JSONL files with 5,535 records, model/secret exclusion, and unchanged behavioral/security core pass.
- The first clone governance inventory used the wrong source-gate status field and stopped without mutation after its static suite passed. The corrected inventory evaluated `decision.status` and all required criteria. A later package wrapper incorrectly expected a separate manifest-copy file; the immutable package report and trace had completed and were reconciled against their embedded manifest hash without rerunning the experiment.
- Two clone builds reproduce the corrected 239,183-byte archive at SHA-256 `679f7ad301689bee62a5bcb33df8c4778f9f0307135cf30632b13408e2f31083`. Fresh source and package 84-attempt runs pass with exact gates and non-latency metric families; packaged MCP, all 51 API/dashboard checks, all 27 state/telemetry checks, and visual inspection pass.
- Visually inspected the fresh 1440 by 1000 packaged dashboard. It accurately reports Baseline 0012, evaluation pass, decision-context and retrieval v3, all exact metrics, human approval, disconnected real infrastructure, and one mitigated synthetic incident. The renewed clean-clone receipt is `artifacts/verification/clean-clone-baseline-0012-attempt-002.json`.
- The final PR head `12e2c71897a3965fc0d64c1f718e8f110cc4f7e1` matched local and remote, contained 11 expected commits and 63 exact files, and returned to `CLEAN` and `MERGEABLE` after brief post-push `UNKNOWN` recalculation states. PR `#10` was marked ready and merged with an expected-head lock and history preserved as `e4dcde1227d0f235f8725df3e15f91ad5675e7ab`.
- Cloned public remote `main` at the exact merge commit. Compilation, 19 tests, seven validators, both package contracts, the source gate, 12 milestone contracts, 185 tracked JSON files, 50 JSONL files with 5,535 records, selected manifest, unchanged behavioral/security core, model/secret exclusion, source/package evaluations, packaged MCP, 51 API checks, 27 native checks, persistence, telemetry, and dashboard rendering passed. Two builds reproduced the selected archive exactly.
- The image viewer's first original-detail frame showed only the bottom table strip. Reopening the same 1440 by 1000 image at normalized detail displayed the complete Baseline 0012 dashboard and passed visual inspection. The merged-main receipt is `artifacts/verification/merged-main-baseline-0012.json`.
- The final pre-publication release audit computes `verified` with no warnings or unresolved surfaces and releases only the inherited release-closure, annotated-tag, selected-asset publication, and immediate public-verification actions. Prepared the release-closure commit for local and remote `v0.0.12`, public non-draft GitHub release, `.pyz` and `.sha256` assets, rendered README and release page, downloaded-byte identity, and fresh public-tag clone reconciliation. The next cycle must resume from this verified checkpoint and select work only after a fresh run exposes a measurable weakness.

## 2026-08-08 - BASELINE-0020 contract freeze

- Independently verified public `v0.0.19` from a no-alternates tag clone, including source/package evaluations, exact archive reproduction, MCP, real HTTP approval/executor/state/telemetry, the rendered dashboard, and public asset bytes.
- Measured a new aggregate-versus-split gap: all three action types are covered globally, but held-out test has no `rollback_deployment` case, leaving action/split coverage at 5 of 6.
- Selected the bounded remediation: one frozen project-authored held-out rollback case plus generic per-split action metrics and fail-closed gates; no runtime capability or authority change.
- Froze `action-split-coverage-v1` before implementation with candidate results absent and exact composed identities for all 30 prior scenarios and terminal-state records. The independent validator passes.
- Implemented the generic action-by-split grader, four fail-closed gates, the dashboard metric, one focused unit test, and only the exact frozen held-out case. Static validation reaches 6 of 6 action/split pairs while all 30 prior scenario and terminal hashes remain exact.
- Retained the first broader static run, which stopped because the terminal validator still required 11 actionable cases. The contract-prescribed case makes 12; updating only that declared count passed the unchanged static suite. No held-out agent result has been revealed yet.
- Froze and verified the 76-file pre-reveal manifest at SHA-256 `d8980eb7c26b0f8dd3f8361d1f0a9a9157405d320670cb363113c37b74ad28d5` before the first full candidate run.
- Retained one timed-out full wrapper that wrote no candidate files, then isolated the 37-test suite. Three stale model-count assertions and one MCP inventory assertion were updated from 30 to 31; the complete suite passes without a product behavior change.
- The first immutable 93-attempt candidate passes every gate. The new held-out rollback case is exact in all three trials, action/split coverage reaches 6 of 6, and the 165-event companion trace is valid and anchored. Report/trace hashes are `5776de4fcf1ef5d6c19412ac68a18e3b2aa8b83f0609548fb0b588e76db2f210` / `c1d96cec5914156bd1f83201cdae02fd12dd052134525e88a8a1c5673fb689f0`.
- Froze the 35-entry, zero-dependency v0.0.20 package contract before archive build at SHA-256 `5959bd97d38a485fdc69dc337857ba2e4d99b34b23545c3049db9cacfe122df2`, then froze the final 77-file manifest at SHA-256 `639abdb7c7f084244a2193752006b0c6a01c9137b10c0caeef662d8661142c62`.
- Built two byte-identical 35-entry, 436,861-byte archives at SHA-256 `294e45519a5becfd8c39c5d8ff7fa277b7319864bcd9da7c125ac44a12966ded`. Final-manifest source/package evaluations pass all 93 attempts and exact 165-event anchors.
- Source/package MCP and real authenticated HTTP approval/executor/state/telemetry surfaces pass. Original-detail inspection accepts the complete packaged dashboard with the new action-split metric and unchanged authority labels. The candidate receipt is `artifacts/verification/candidate-baseline-0020.json`.
- Cloned exact public-branch candidate `a4a87c98e3869132b5fafbf8172d987e5b60aa2e` with `--no-local --single-branch`; the clone began clean and has no object alternates. It passes 19 validators, 37 tests, fresh source/package 93-attempt evaluations and independently verified 165-event anchors, two exact selected-archive rebuilds, three-tool MCP, real authenticated API/approval/executor/state/telemetry, structured-artifact parsing, credential/model exclusion, and a complete normalized high-detail dashboard.
- Clean-clone source report/trace SHA-256 values are `691406912486e8acc6e6115a43cc18c0e6b34ac21aac9de7074e7574613d90e4` / `7f72d6d70ce54814de231265378265a8df42c3a4ce911844879aa15c0ae2867d`; package values are `f4b65f1e715166b06f88f67ac2ba67d8b66e360b9ee95ce984edd4575e020e8b` / `41e340601e01b416077c284d42b69eaf824ced9dc3bbd9aa55aae4f8289bb8df`. Both clone builds match the selected 436,861-byte archive at SHA-256 `294e45519a5becfd8c39c5d8ff7fa277b7319864bcd9da7c125ac44a12966ded`.
- Parsed every structured file in the post-verification clone: 362 JSON files and 10,375 records across 84 JSONL files pass. No model artifacts and no credential-shaped material outside the ten frozen pattern-definition files are present.
- Retained the screenshot helper's post-write fallback diagnostic and the resumed inspection's first stale package-specific filename. Resolving `dashboard-baseline-0020.png` and opening the unchanged 1440 by 1000 bytes at normalized high detail passes. UNIT-004 is complete; pre-merge truth audit and exact review are next.
- The first post-documentation wrapper inherited a one-second command ceiling and was terminated before a result. Its unchanged-tree rerun passed all 37 tests. A first verifier-discovery expression then matched zero files because it searched `validate_*` instead of the repository's `verify_*` convention; this false zero-validator inventory is retained and was not used as evidence.
- The corrected exact 19-verifier inventory passes. The gate's baseline verifier intentionally produced and promoted fresh local source attempt 003; all 93 attempts and every frozen gate pass, its report/trace SHA-256 values are `06817fabc90471d84ac36a38e96208d7554d07355398a5213072c0ae57a19221` / `475775862fcc73bd2a603d8c5dbf42bc7b3233ad7b4253be47af5b6c8297d5cb`, and its 165-event anchor ends at `f9845bb66a6bfbb49889c0fabfb5a362c735a5d5eaeba7a777311e3354d2741e`.
- Opened draft PR `#18` at exact candidate head `a6f4c5ce43a085f0a01f0f78f48ff80983ff8ed6`. GitHub reported seven commits, 56 changed paths, CLEAN, MERGEABLE, no configured checks, and an exact local/remote path-set match. The first `gh` metadata read transiently failed to connect to `api.github.com`; git refs and the GitHub connector remained reachable, and the unchanged retry passed.
- Premerge audit `artifacts/verification/release-audit-baseline-0020-premerge.json` validates and computes `verified` without warnings or unresolved surfaces. Its audit-only commit produced exact reviewed head `55c68bb6c148922847b3a0b2fb139fbd9d1cbdc5`, eight commits, and 57 exact paths. PR `#18` was marked ready and merged under expected-head lock with history preserved as `df742fc21dfc9cb706896af10d31267ed408197f`.
- Cloned public remote `main` with `--no-local --single-branch`; exact merge commit, clean initial worktree, absent object alternates, compilation, 19 verifiers, 37 tests, source/package 93-attempt evaluations, exact 165-event anchors, two exact 436,861-byte archive rebuilds, MCP, real authenticated API/approval/executor/state/telemetry, parsing, scanning, and complete source/package dashboards pass.
- Merged-main source report/trace SHA-256 values are `ebbe977c0597fa4355f2f056649cb6e0a3b080d4e283bb84df6b1a5269883cc6` / `1e79a23ec4bb5c1879e43306bc888d3a4c69752e167d2bf1832f256e88c3bf47`; package values are `ae89ea7d18adb4301e642f87585bcd1cfeda422831fd8cf5f6f81fa09cd91096` / `efc3a83bcb7e70ac2986eec3a10ceacce71917a678c34596c3b1f1608f4cb2f9`.
- The first merged-main MCP wrapper set only the child runtime path. The verifier itself stopped at `ModuleNotFoundError` before MCP startup, while later API/copy steps caused the composite shell to return zero. Corrected explicit source and package `PYTHONPATH` values both pass; the orchestration failure remains retained. Headless Chromium also retained its post-write fallback diagnostic for each complete valid dashboard.
- Final release audit `artifacts/verification/release-audit-baseline-0020.json` validates and computes `verified` with no warnings or unresolved surfaces. The first push of its exact local commit failed on transient DNS resolution for `github.com`; the unchanged retry reached the exact release-closure branch and the failure remains retained.
- Prepared the release-closure commit for annotated `v0.0.20`, selected 436,861-byte zipapp and checksum assets, public non-draft GitHub release, downloaded-byte identity, rendered README and release page, and fresh no-alternates public-tag clone reconciliation. All public surfaces then verified against the exact closure; the next cycle must resume from public v0.0.20 and select work only after a fresh run exposes a measurable weakness.

## 2026-08-12 - BASELINE-0027 orientation and container contract freeze

- Resumed from release-reconciliation commit `d3c3ad23652ca1b7c69525dabd59cd90c9c77522`; remote `main`, public repository visibility, latest-release API, annotated `v0.0.26`, selected asset digests, and the public receipt reconcile. Four unrelated untracked BASELINE-0025 scratch artifacts remain untouched.
- A fresh exact-tag source run outside the repository passes 57 scenarios, 171 attempts, all 119 Boolean gates, and an anchored 261-event trace. Separate metric inspection confirms perfect bounded retrieval/generation/trajectory/policy/terminal/utility/reliability, zero proposal and terminal attack success, 65.052/112.491 ms median/p95, and zero model calls or external API cost.
- Retained probe failures rather than normalizing them away: unsupported `gh isLatest`; two unsupported PowerShell `ConvertFrom-Json -Depth` uses; a selection summary that queried nonexistent top-level metric fields; one parser-error cross-tab; and one syntactically valid but null-coercing cross-tab that misclassified missing attack-ID fields. Explicit nonempty-string filtering yields 39 adversarial cases: two guidance-not-retrieved, twenty guidance-retrieved-filtered, eight in-band-exposed, and nine non-instruction cases across twenty observed-valid stage/outcome/split cells.
- Docker client/server 29.4.3 are live. The prior three excluded official Python candidates remain unchanged. The exact cached and anonymously resolvable Chainguard Python index passes identity, authority, access, rights, provenance, integrity, fitness, and privacy-security for local build/run verification only. Local metadata identifies linux/amd64 manifest `sha256:15e66fa35e0b07095bbc4f4f0522718b780944709026687485f4e712cc6d5ae0`, Python 3.14.7, UID 65532, no shell/package manager, and a current 54-package scan with zero findings. No Cosign signature check ran and no signature claim is made.
- Selected the container surface over the metric-only candidate because Docker availability changed and the active goal explicitly requires container validation when available. The metric distinction remains the next candidate; no behavior failure was inferred.
- Froze exact Dockerfile/context bytes, two-build identity, base/added-layer checks, non-root/read-only/no-capability/no-new-privileges/network restrictions, 36 real-surface and release checks, and a no-export/no-push boundary in `container-runtime-v1`. The independent validator passes preimplementation with both Dockerfile and result absent. BASELINE-0027 validates with UNIT-003 ready and no human gate.
- The public v1 freeze then exposed a preimplementation incompatibility: Docker port forwarding cannot reach the intentionally container-local loopback API. Widening `create_server`, host networking, and a new proxy were rejected. The exact v1 and failure receipt remain. V2 changes only verification topology: `--network none`, actual HTTP calls through `docker exec`, exact dashboard-HTML extraction, and host rendering. No Dockerfile or image was created before the v2 refreeze.
- Implemented the exact v2 Dockerfile, context allowlist, v0.0.27 identity, and host-side container verifier without changing any of the 57 scenarios or decision/authority components. Static contract and compilation checks pass. The pre-manifest suite passed 46 of 47; only the expected checkpoint assertion failed because evaluation still reads the public baseline-0026 manifest. That failure is retained, no candidate evaluation or image ran, and the baseline-0027 manifest remains gated on a public implementation seal.
- Published implementation seal `b521d16d91f5c8e198449ef17bb52e9f8d7490d5`, then froze and exactly verified the 115-file baseline-0027 pre-reveal manifest at `2026-08-12T17:16:39Z`. The 13,687-byte manifest has SHA-256 `eff82128c034c7534ebf3c05fc7bec5cc4a04689d86f7304e40d0813b28d1523`; candidate evaluation, report, trace, and Runbook Sentinel image results remain absent until the manifest seal is public.
- The complete pre-reveal gate passes 47 of 47 tests in 85.580 seconds and 31 of 31 standalone validators in 2.131 seconds. No archive, image, candidate evaluation, report, or trace was created; the exact manifest and receipt are ready for their public seal.
- Published exact pre-reveal seal `8c486c6d9e0bc26c03749c45e9e5b89c1c72770a`. Local, remote-tracking, and live remote branch refs match; the commit contains manifest blob `0625717c9fffd481a6b18972a2ba4746b1bfd2c1` and preserves the local awaiting-public receipt as observation-time history. Immutable source attempt 001 is now authorized; package and image construction remain gated.
- Immutable source attempt 001 passes 57 scenarios, 171 attempts, all 119 Boolean gates, and exact 261-event trace verification. The report/trace SHA-256 values are `6ec1368c31d2f1a18a25a474ea98874ece035414e516dc7d87b32afc7bd4c64d` / `72e19641a92221e94a03b79af5d48de1acd17cd1be56200b1f50f4e2b539b1fe`; the final event is `7784f91350285dc65fab0dbf9c8fd1113f5d606b3847d6d3d63ae541b1f10975`.
- The post-candidate sweep passed 16 validators then retained `candidate_latest_pointer_mismatch` from the historical BASELINE-0026 exposure-stage validator. The bounded correction mirrors the proven predecessor rule: only a later numeric checkpoint with exact current or companion manifest, three trials per scenario, passing disposition, zero proposal/terminal attack success, and valid anchored trace is accepted. Package and image construction remained closed.
- The correction passes the previously failing validator, three focused pointer tests, all 48 tests in 91.926 seconds, and all 30 non-manifest validators in 2.140 seconds. Corrupt companion manifests and traces are rejected. The unchanged pre-reveal manifest retains exactly one expected corrected-validator hash mismatch; source attempt 001 and both receipts must be public before the package contract and final manifest close it.
- Published source attempt 001 and the bounded correction at `7221f89556573051513b91b5dd6c3e048964f65f`. Froze identical current/versioned 41-entry v0.0.27 package contracts at 14,968 bytes and SHA-256 `2eac29cedf0b422c0873629b7d3d682882ff403556059446ac232555479bf57e`, then froze a 116-file final manifest at `2026-08-12T17:42:57Z`, 13,795 bytes, SHA-256 `cdc9ced520421f89b87ea04629bbce1b4a80e7f875b4366a6359c987a009f67a`. No archive or image exists; the complete final-manifest gate and public seal come first.
- The complete final-manifest gate passes 48 of 48 tests in 101.783 seconds and 31 of 31 validators in 7.168 seconds. It creates no archive or image; the exact package contract, final manifest, and local receipt are ready for their public pre-build seal.
- Published exact package/final-manifest pre-build seal `22aeb0cfe3116521c93e1119d02ef600aa265086`. Local, remote-tracking, and live remote refs match; the commit contains both package contracts as one exact blob plus the final-manifest and predecessor-receipt blobs. UNIT-003 is complete, and UNIT-004 may begin two archive builds while the image gate remains closed.
- Built two independent 41-entry v0.0.27 zipapps at 589,981 bytes and SHA-256 `7721e1c6976bc1f7d5162ce4873e7fa101f0fef7143f30e6a6b5d3eeb2591493`. Exact byte identity, both package-contract validations, and the selected CLI launch pass.
- Retained and excluded final source/package evaluation attempt 001 after a malformed PowerShell `Test-Path ... -or ...` precondition produced a non-terminating error and allowed both runs to continue. A later trace-verifier probe omitted `PYTHONPATH` and stopped at import. No result is selected; attempt 002 must use an explicit fail-fast guard, and no image was built.
- Final source/package attempt 002 uses an explicit fail-fast immutability guard and passes both exact 261-event trace verifiers. Every gate, ten deterministic metric families, and both split records match at projection SHA-256 `d08823b3ddda041f24567f70f11326661b749d63ddc07dbdd1d160836c7ca820`; a full leaf audit finds only contract-declared runtime-varying fields.
- The first source-real-surface wrapper stopped before MCP startup because it set only `RUNBOOK_SENTINEL_PYTHONPATH`; the verifier itself also imports from `PYTHONPATH`. No output or image was created. The failure is retained and the unchanged rerun sets both exact source paths.
- Corrected source and package MCP/API/native verification passes. Both runtimes expose only the three diagnostic/read MCP tools, pass authenticated approval/executor/replay/state/telemetry gates, and render complete 1440 by 1000 Baseline 0027 dashboards at original detail.
- Retained an unsupported `System.Convert.ToHexString` parity-summary failure. The supported `SHA256.Create().ComputeHash(...)` plus `BitConverter` path proves exact container projection `d08823b3ddda041f24567f70f11326661b749d63ddc07dbdd1d160836c7ca820` and full normalized report `87b49f84253b05e672785dbb1be0e4f0041a2243c6a07104c5ec3da45188fbcb`.
- The post-package test gate passes all 48 tests in 88.002 seconds. The aggregate verifier then completed its designed evaluation-and-promotion tail, producing valid but unselected `baseline-0027-attempt-002` and repointing `latest.json`; all three outputs are retained and the pointer is restored exactly to selected final source attempt 002 before release selection.
- A first side-effect-free validator wrapper omitted `PYTHONPATH` and stopped after 21 passes at `verify_stale_payload_projection.py` import. The unchanged corrected wrapper binds the exact `src` path and passes all 31 validators in 2.611 seconds while the selected latest report remains exact. No image was built.
- The classified artifact audit parses 581 JSON files and 19,279 records across 180 JSONL files. It retains the exact previously classified empty BASELINE-0026 MCP failure; 22 broad credential-shaped files are exactly seventeen package-contract pattern definitions, three symbolic approval-token definition files, the container scanner containing those same definitions, and a retained historical rebuild containing the frozen symbolic roles. Zero unresolved credential value and zero model artifact remain. The first ripgrep call parsed its leading private-key pattern as a flag, and the first repository parser used unavailable `Path.GetRelativePath`; neither produced a security disposition, and both wrapper errors remain recorded in the receipt.
- Published the exact source/package payload at `bab9fc67b5633060d370f3151b09a7f88712b629`; local, tracking, and live remote refs match. The first frozen two-image run fails closed at image identity before runtime work: `e231ceed...` differs from `63f64522...`; its observation-time receipt records a twelve-layer candidate-common prefix and two differing COPY layers with image creation times 1.887 seconds apart. No runtime, MCP, API, scan, export, or publication followed. The later correction at the top of this log establishes the exact eleven-layer base boundary without rewriting this historical observation.
- Current official BuildKit and Docker reproducibility specifications passed the first narrow eight-criterion gate for use of the special `SOURCE_DATE_EPOCH` input. The bounded rerun supplied final-manifest time `2026-08-12T17:42:57Z` (`1786556577`) only in the caller environment and retained its failure; the successor v2 source gate and v3 contract at the top of this log replace that incomplete recovery.
# BASELINE-0028 implementation

The public freeze was verified before implementation. I added one additive grader driven by exact declared attack document IDs and each retained attempt's `retrieved_document_ids`. It reports four stages instead of silently treating non-retrieval as filtering. The old three-stage metric remains unchanged for history.

The first full regression found one honest compatibility defect: a stale-payload validator still expected catalog schema 1.17. That 58-of-59 result is retained. After reconciling current catalog validators to the permitted 1.18 contract-only bump, all 59 tests pass. The implementation also passes independent malformed, mixed-stage, and ambiguous-stage probes. No scenario, retriever, agent, policy, service, storage, authentication, approval, executor, action, dependency, secret, model, or real-infrastructure surface changed.

Next: publish the implementation seal, then freeze a new manifest and reveal one immutable source candidate. Do not reuse or relabel the old v0.0.27 archive.

The pre-candidate validator sweep also exposed an older portability dependency: the BASELINE-0024 contract validator required exact v0.0.23 release bytes that were absent from this clean worktree. Controlled intake fetched the project-owned public asset, verified its published size/SHA-256 plus ZIP and embedded package identity, and promoted it without replacement into tracked verification custody. The old validator now passes from portable controlled bytes; the artifact is historical evidence, not a v0.0.28 candidate.

The first 127-file baseline-0028 manifest passed exact verification and all 24 validators, then its 59-test run retained one identity-only failure: the evaluation report correctly emitted baseline-0028 while one test still expected baseline-0027. The test assertion was corrected, the failed manifest remains preserved by digest and receipt, and a renewed manifest is required before the manifest seal or any immutable candidate.

The renewed manifest freezes at `2026-08-13T04:08:00Z`, contains 127 unique exact paths, and is SHA-256 `5bbd08327b569aba4381f7ea7a0dd01e0c05fc07e0861dcd7e35788bee763a9a`. Manifest verification, 24 source validators, compilation, and all 59 tests pass. No baseline-0028 candidate report, trace, package, or container image was created before this pre-reveal seal.

The pre-reveal manifest was published and remotely verified at `04c1a2c3bd150ae7051336106c35f84341e3e5c9`. Immutable source attempt 001 then passed 57 scenarios, 171 attempts, all 126 Boolean gates, the twenty-cell additive split, exact 60 retrieved-filtered and 6 never-retrieved attempt counts, zero cross-trial ambiguity, zero proposal/terminal attack success, and an independently verified 261-event trace anchored at `74475121...`. Report and trace SHA-256 are `4fb4b38e...` and `2b37f040...`; `latest.json` is byte-identical to the report.

The first post-run inspection wrapper omitted `PYTHONPATH` for the trace verifier and queried legacy nested report paths. It made no product or evidence mutation. The unchanged independent verifier and schema-aware inspection passed; the wrapper error is retained in the candidate receipt. UNIT-003 closes pass. UNIT-004 must freeze and publish the package contract plus final manifest before archive or container creation.

Two post-candidate aggregate wrappers are also retained. The first timed out without a disposition; the second passed fifteen direct validators before PowerShell treated unittest's normal verbose stderr as a terminating native-command error. Neither is evidence of a test failure. A subprocess-based repetition records each real child exit before candidate publication.

UNIT-004 freezes a 42-entry dependency-free v0.0.28 zipapp contract: the only new archive entry is the project-authored retrieval-stage/outcome/split contract already used by source evaluation. Current and versioned package contracts are byte-identical and validate before any archive exists.

`container-runtime-v8` inherits exact public-v0.0.27 v7 evidence: the Chainguard index/platform digests and source gate, BuildKit/exporter/timestamp requirements, UID/GID 65532, read-only root, dropped capabilities, no-new-privileges, network-none and namespace restrictions, tmpfs byte extraction, real surfaces, clean clone, structured point-in-time scan, and no export/push/publication boundary. Only v0.0.28 package/checkpoint labels and an explicit 20-cell/60-versus-6/zero-ambiguity metric check advance. The first validator run failed closed when the draft contract accidentally added historical `WORKDIR`; the contract was corrected to the released no-WORKDIR sequence and historical/current constants were separated.

The first three-test package-freeze probe retained a fixture-only error after two passes: mocked validator success still attempted to hash the intentionally absent v0.0.28 archive. The self-contained fixture now mocks immutable hashes too, all three focused tests pass, and no v0.0.28 package or image exists before the pre-build seal.

The first full-test wrapper treated normal unittest stderr as a PowerShell native-command error; the explicit subprocess repetition passes all 59 tests in 145.033 seconds. The first validator inventory also invoked the retrieval-stage contract's default preimplementation mode after implementation; thirteen earlier validators passed, and the contract correctly failed closed. The corrected inventory uses `--require-implementation` and all 34 current non-runtime validators pass. Compilation and diff checks pass; no v0.0.28 archive or local image exists.

The final pre-build manifest freezes 129 exact files at `2026-08-13T04:48:00Z`, 15,511 bytes, SHA-256 `846f9ae732aee6fbb4dc9ea13fe7e11f95eaafff2309e36128a22045a94783df`. Package contract SHA-256 is `774c826b1e2c608aeb5263ae0496f1ec1b007789a004997f3da21ca9e099983c`; container-runtime-v8 SHA-256 is `b15f418ea7b0b872ebb4f0d2df76b00a2f2cf9ce89901558d1156ce54bc74344`. All must be public and remotely exact before any archive or image build.

On 2026-08-15 the resumed pre-build seal showed no drift: local HEAD and the live public branch remained exact at `2b992bba...`; manifest and contract hashes were unchanged; no v0.0.28 archive, checksum, container receipt, or local BASELINE-0028 image existed. A renewed 59-test run passed in 106.493 seconds and all 34 non-runtime validators passed. Docker Desktop is available, but package and image construction remain closed until this seal is publicly exact.

Published the pre-build content seal at `bf5a01e7878914a18216f4c315be3a1762c8ff4b`. The live GitHub branch matched immediately; the local remote-tracking ref initially remained at its predecessor because origin fetches only `main`, then an exact branch-refspec fetch reconciled local, tracking, and live heads without changing public state. GitHub reports the repository as public. Package, checksum, container receipt, and BASELINE-0028 image remain absent. Two independent archive builds are now the next gate; container construction stays closed until source/package real surfaces pass.

Two first-generation v0.0.28 builds reproduced exactly at 619,465 bytes and SHA-256 `b404565a...`; both package-contract checks pass. An unsupported global `--version` probe and a trace-verifier help call without `PYTHONPATH` are retained as wrapper failures. Source final attempt 001 used the wrong decision-context argument and correctly returned `remediate` on all four stale-payload gates; attempt 002 restored the sealed context and passes all 126 Boolean gates. The package evaluation repeats the exact passing metrics, split results, manifest, and normalized scenario/terminal/tool trajectory.

The first source HTTP/approval/executor/state/telemetry/dashboard run passed 116 of 117 checks. The earlier 109/110 statement remains in its public historical receipt and is superseded by a static recount of both the predecessor and corrected verifier blocks; both contain 117 checks and only `dashboard_baseline_exact` was false. Original-detail inspection confirms the rendered page says BASELINE 0028 and shows the new retrieval-stage metric, 60/6 attempt counts, stale payload exposure 0.0, authenticated external operator, and disconnected infrastructure. The exact failure and old-manifest package are retained. Only the verifier identity and a focused test change; the manifest must be renewed and public with no selected archive or image before rebuilding.

Moved the superseded selected archive and checksum, plus raw failed-run API state and telemetry, into exact local custody under `C:/Projects/Verification/runbook-sentinel-baseline-0028-superseded-package-20260815T153623Z`; the rendered failure dashboard remains tracked project evidence. Corrected only the verifier's current/stale checkpoint literals and added one focused test. The renewed 129-file manifest is 15,511 bytes at SHA-256 `f45f6f6270d6bca1ea11a250b4b5ce7e787d8ccc4f704e4535b99f72c18df50b`. Two focused tests, all 60 tests in 102.962 seconds, all 34 non-runtime validators, compilation, diff, and static package/container checks pass. `latest.json` is restored to the immutable source candidate; selected archive and image bytes are absent before publication.

Published the verifier correction and renewed manifest at `bf9e37eecbfa176328172f424ba2e17c1d0553c2`. Explicit branch fetch and live GitHub inspection reconcile local, remote-tracking, and live heads exactly; the manifest and verifier Git blobs match and the repository remains public. No v0.0.28 selected archive, checksum, container receipt, or BASELINE-0028 image existed at publication. Replacement archive construction is now open; the container gate remains closed.

Built two replacement v0.0.28 zipapps from public correction seal `ca03131...`; both are 619,465 bytes at SHA-256 `700f2b2781a2de0d4fa207e218f5863055e53e6b149ffab138d5d5227e662594` and pass all 42 package entries. Final source attempt 003 and package attempt 002 each pass 57 scenarios, 171 attempts, all 126 Boolean gates, exact 20/20 retrieval-stage cells, 60 retrieved-filtered versus 6 never-retrieved hostile-guidance attempts, zero ambiguity, zero proposal/terminal attack success, and valid anchored 261-event traces. Normalized cases, split metrics, and the retrieval/generation/trajectory/policy/terminal/utility/security/reliability/cost families match the immutable source candidate and each other.

Source and package MCP expose the same three diagnostic/read tools and no approval or execution authority. Both authenticated API runs pass all 117 checks; both native persisted-state/telemetry inspections pass all 63 checks. Original-detail inspection accepts both 1440 by 1000 dashboards with BASELINE 0028, 60/6 stage counts, stale payload exposure 0.0, authenticated external operator, and disconnected real infrastructure visible. A static recount corrects the earlier failure receipt's count-only 109/110 statement to 116/117 while preserving its sole false check and failed disposition. Wrapper-only path and parser errors remain retained. The post-surface gate passes 60 tests in 95.251 seconds, 34 validators, compilation, parsing, and bounded credential/model scans with no BASELINE-0028 image present.

Published the exact package/source payload at `8ccd96de8cef376f0750a031130de609abeb3725`. Explicit branch fetch and live GitHub inspection reconcile local, remote-tracking, and live heads; the package/source receipt and `latest.json` Git blobs match remotely. No BASELINE-0028 image existed at publication. This opens only the frozen local container-runtime-v8 gate; no image export, push, or publication is authorized.

Static inspection immediately before the first Docker build found the embedded container HTTP verifier still asserted `Baseline 0027` while its health and evaluation assertions correctly required baseline-0028. The v8 contract validator also lacked a marker rejecting that predecessor dashboard literal. The gate stopped before image construction; no container receipt, export, push, or publication exists. Moved the selected `700f2b27...` archive and checksum to exact superseded custody. The bounded correction changes only the container verifier identity, its validator, one focused regression test, and the manifest; product, retriever, decision context, policy, scenario, terminal, dependency, and authority behavior remain unchanged.

Corrected the container verifier to require `Baseline 0028` and strengthened the container-contract validator to require the current literal and reject the predecessor. Two focused tests pass. Renewed the 129-file manifest at `2026-08-15T16:12:05Z`, 15,511 bytes and SHA-256 `996a9ca54fd13883bc72e2acd65c6a5df2e8398093af7ecbe229699e427c629d`. `latest.json` is restored to the immutable source candidate. All 61 tests in 91.009 seconds, all 34 validators, compilation, milestone, and diff checks pass with selected package and every BASELINE-0028 image absent before publication.

Published the container-verifier correction at `97bd69fd3aa370de527091b4f9879fcaefab6ddd`. Local, remote-tracking, and live GitHub heads reconcile; renewed manifest, runtime-verifier, and contract-validator blobs are remotely exact. Replacement package construction is open, but Docker construction remains closed until the repeated source/package real-surface gate passes.

Built the post-correction package twice from public seal `8277865...`; both 42-entry archives are 619,465 bytes at SHA-256 `c770fc21ece71f3728d366575a9233100aeb5cbb40f5026be7814ae3c7977044`. Final source attempt 004 and package attempt 003 pass 57 scenarios, 171 attempts, all 126 Boolean gates, 20/20 retrieval-stage cells, 60/6 attempt counts, zero ambiguity, zero proposal/terminal attack success, and anchored 261-event traces. Normalized behavior and selected metric families match. Source/package MCP semantics match with no approval/execution tool; both APIs pass 117 checks, both native inspections pass 63 checks, and both complete dashboards pass original-detail inspection. No image exists; the post-surface regression and public payload gate remain closed.

The post-correction payload passes all 61 tests in 131.522 seconds, all 34 validators, compilation, milestone validation, diff checks, parsing, and bounded credential/model scans. The selected archive remains `c770fc21...`; no BASELINE-0028 image or container receipt exists. Exact public publication and remote reconciliation remain the only pre-Docker dependency.

Published the post-correction package/source payload at `9e585ed2298f712007078ba8025a679ba476eee6`. Local, remote-tracking, and live GitHub heads plus the package/source and selected-evaluation blobs reconcile exactly. Zero BASELINE-0028 images existed at publication. Frozen local container-runtime-v8 and clean public-branch reproduction are now open; image export, push, and publication remain prohibited.

Ran the corrected container-runtime-v8 gate from public Docker input `10def137bc1d2223ec1cdc818374d679d32383dc`. Two independent no-cache networkless builds reproduce exact image ID `sha256:9a62f5e1daf61089ef6c9ceb71afd930c72ea60874bcec4eba78c4fc35467e2e`. The local phase passes 43 of 44 checks, with only clean-clone reproduction intentionally pending. Hardened non-root/read-only/capability-dropped/no-new-privileges/networkless runtime, exact 57/171/261 evaluation, source/package/container metric parity, bounded MCP, authenticated API/approval/executor/replay/state/telemetry, verified tmpfs extraction, complete 1440 by 1000 dashboard, and Docker Scout 1.20.4 SARIF with zero high/critical findings all pass.

Cloned the exact public branch over HTTPS with no object alternates, supplied only the exact ignored `c770fc21...` archive, confirmed Git-clean state, and reproduced the same image ID at the frozen creation timestamp. The canonical result passes all 44 checks. Original-detail inspection accepts the complete container dashboard with BASELINE 0028, 60/6 counts, authenticated external operator, and disconnected real infrastructure. A JavaScript composition wrapper initially rounded four uncommitted event nanosecond integers; the canonical receipt restores the exact generated values and validates. No image bytes were exported, pushed, or published.

Committed the canonical container payload locally at `1dbc9e1...`. Its first GitHub push failed before verified remote mutation because DNS could not resolve `github.com`; the exact failure is retained and the unchanged commit remains the retry input.

Retried the unchanged publication successfully. Local, tracking, `ls-remote`, and GitHub API refs resolve to `9dc57f82e3fb3204853129a27246090c451d7739`; the public repository exposes canonical container receipt SHA-256 `1e592320...` exactly. UNIT-004 closes with all 44 checks true, three local builds at image ID `sha256:9a62f5e1...`, and no image export, push, or publication. UNIT-005 opens at the governed premerge release-truth audit.

The first UNIT-005 transition-validation wrapper used stale option names for both governing validators, and both stopped at argument parsing before reading or mutating evidence. Its corrected container invocation then guessed a nonexistent contract path. The process-invalid attempts are retained. The governing milestone validator now reports valid active inherited authority with UNIT-005 in progress and no error or warning; the authoritative v8 contract and canonical 44-of-44 receipt also pass unchanged.

Opened draft PR `#26` at exact candidate head `c823f602...`; GitHub reports it open, clean, and mergeable with no configured checks or reviews. The literal head passes all 61 tests in 131.334 seconds and all 34 current standalone/governing validators. Selected source/package trace anchors, package contract, required-result container contract, compilation, and corrected independent archive byte comparison pass. The first combined archive comparison guessed a nonexistent `dist` subpath inside build-D custody after the substantive validators passed; its exact process-invalid receipt is retained. Release-truth reconciliation found and corrected two living records that still described package and real-surface verification as pending. No runtime, manifest, package, or image byte changed.

Provenance reconciliation then found that a premerge `verify_baseline.py --help` probe had not displayed help: the script has no argument parser, so it ran the full validator/test/evaluation workflow and promoted a fresh passing report. The report, manifest, and 261-event trace are retained as process-invalid/excluded evidence with exact hashes. `artifacts/evaluations/latest.json` is restored byte-for-byte to predeclared final source attempt 004; no selected package, container, runtime, scenario, or authority byte changed.
